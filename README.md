# SYSU Welcome Interactive

中山大学智能工程学院迎新晚会创新互动网页项目。

正式产品采用 NFC 明信片邀请函作为主要体验入口，二维码和短网址作为设备兼容备用；三种方式进入同一 H5。Demo v0 只验证同一随机令牌的网页入口，实体制作后续确认。二维码不能解决网站或现场网络故障，相关情况由备用网络、轻量页面和离线流程处理。

## 当前状态

Demo v0 的 G0～G4 已由 D-022 绑定到提交 `63e3d5094c49a5635f4ebf7ccbd0c9da07b7d58a` 并完成干净工作树复验；该提交与后续 D-023/D-024、G5 实现现在都只作为 **协议 v1 历史基线**。D-025 已冻结不兼容的协议 v2：参与者按“身份激活与选色 → 档案提交或跳过 → 进入当前现场场景”入场，全场只运行 `ASSEMBLY`、`PROGRAM_SUPPORT`、`COOPERATIVE_LIGHT` 三个场景，并以最多 300 颗真实恒星的流动星系连接手机与大屏。

V2-00～V2-09 已完成：在协议、数据库、入场、三场景运行时和分流实时层之上，`/admin`、`/screen` 与 `/welcome` 都会先做能力发现，并只在 `V2_ACTIVE` 数据库中挂载对应 v2 页面。手机端已完成独立入场、最多 300 颗真实恒星的个人星系、单一底部操作坞、三场景互动、退出/草稿/反馈与权威终章；大屏继续提供三场景、匿名弹幕与礼物、人工胶囊插入和 OBS 透明节目层，节目视频与音频仍由 OBS/导播拥有。迁移 `0008_protocol_v2_foundation.sql` 只追加基础结构，V2-07/V2-08 分别追加 `0011_protocol_v2_screen_interactions.sql` 与 `0012_protocol_v2_program_selection.sql`；普通迁移都不会自动清空或激活 v2。维护操作必须按 [`docs/RUNBOOK.md`](./docs/RUNBOOK.md) 使用直接 CLI 命令。仓库实际 `.data` 尚未执行一次性切换，当前日常服务仍返回 `activationState='CONTRACTS_READY'`、`activeRuntimeVersion='1'`；只有隔离的 `V2_ACTIVE` 合成库会启用新三端。V2-10 的 30 分钟 Chrome 渲染 soak 已通过并建立安全的局域网临时预览入口；OBS 实际合成、三端局域网现场操作和 vivo X300 人工验收仍待项目负责人签核。所有开发数据继续使用合成内容，不包含真实名单、生产密钥、实体 NFC、公网部署或正式活动数据。

- `frontend/`：Vue 3 + Vite；含可操作的 `/welcome`、`/screen`、`/admin` 三端页面。
- `backend/`：Node.js/TypeScript + Fastify + SQLite 本地服务；运行数据库和凭据只生成在被忽略的 `.data/`。
- `packages/contracts/`：前后端共享的协议版本、错误、运行状态、健康检查、快照和实时事件契约。
- `docs/`：需求、决策、隐私、测试和现场手册。
- `tests/`：G1/G2 自动化测试、G3 浏览器端到端与恢复测试、G4 协议级负载、真实设备和最终验收说明。
- `.github/`：Issue、Pull Request 和自动构建模板。

## v1 G0～G4 验收与复验（历史证据）

需要 Node.js 和 pnpm。

```bash
pnpm install --frozen-lockfile
pnpm dev
```

根命令会初始化或原子升级本地合成数据，依次启动后端和前端，确认代理链路就绪后输出手机入口、后台、大屏和二维码文件位置。手机与电脑需位于同一可信网络；启动器不会修改 Windows 防火墙。当前生成的合成入口、合成姓名与学号、后台口令和数据库只保存在 `backend/.data/`，不得提交或截图传播。升级会保留既有个性邀请令牌和身份 ID，不会把学号明文写入数据库。

先验证 G2 功能基线：

```bash
pnpm verify:g2
pnpm db:verify
```

G3 自动化从仓库根目录运行：

```bash
pnpm verify:g3
```

作为 D-019 的历史独立 G3 基线，2026-08-10 的 `pnpm verify:g3` 曾以退出码 0、总时长 278.9 秒收口：Vitest 16 个测试文件、102 条测试全部通过；Playwright 6 个场景在 `chromium-ci`、系统 Chrome 和系统 Edge 三个项目各执行一次，合计 18/18；共享契约、后端和前端生产构建全部通过，退出后 E2E 临时目录为 0。D-021 的提交前工作树总验收重新执行扩展全门：`pnpm verify:g4` 退出码 0、观察总时长约 463.2 秒，Vitest 20 个测试文件/119 条全部通过，共享契约、后端与前端生产构建通过；Playwright 9 个场景 × 3 个项目 = 27/27，各项目 9/9，报告状态 `passed`、失败检查点 0、浏览器报告窗口 295388 ms。新增的 `session-races.spec.ts` 覆盖参与者退出晚到快照、后台快速重登和外部重置后的私密 DOM 清理，`recovery` 与 `three-surfaces` 已记录静态失败检查点。

D-021 的运行环境为 Windows 10.0.26100、Node.js 24.16.0、pnpm 11.16.0、Playwright 1.62.1；浏览器分别为 Chromium 151.0.7922.34、Chrome 151.0.7922.76、Edge 151.0.4129.72。机器可读浏览器报告是被 Git 忽略的本地脱敏证据，不是提交产物。CI 已配置，但尚无远端执行证据，不能算作 D-021 通过依据。

G4 专项负载与包含既有门禁的最终全门分别运行：

```bash
pnpm test:load
pnpm verify:g4
```

D-021 对应的 `pnpm verify:g4` G4 报告状态为 `passed`，总时长 123452.51 ms：300 个固定合成参与者、301 条 WebSocket 连接，阶段 20 次、礼物 1200 次、弹幕 300 次、协同点亮 300 次均成功；阶段、礼物、弹幕、协同点亮的传播延迟 p95 分别为 46.12、1426.08、1438.55、939.64 ms，均低于 2 秒，四档礼物各自 p95 也均低于 2 秒。协议错误、业务失败、重复账本/奖励/扣减/幂等记录、负余额和旧 epoch 污染均为 0；服务重启时 301 条连接全部断开并恢复，重置、旧会话失效、旧 epoch 重同步和固定凭据再激活均通过。主要 HTTP 写入并发上限是 24，不能扩张为 300 次写请求同一瞬间并发。`tests/reports/g4-load.json` 是被 Git 忽略的本地脱敏证据，不是提交产物。

上述 G4 结果来自 Windows 单机 loopback 上约 123 秒的完整混合旅程，不是 30 分钟 soak、分布式局域网或场馆验收，也不表示正式上线。根级 `pnpm verify:g4` 已实际先重跑 G3 全门再执行 G4 负载，并以退出码 0 完成；该结果不能扩张为尚未执行的正式环境结论。

启动后可用共用后台账号进入 `/admin`，先取得需要的 Demo 角色，再启动排练或现场流程；参与者从启动器生成的带合成令牌入口进入 `/welcome`，大屏使用 `/screen`。刷新、重复提交、短暂断线和服务重启均以服务端权威状态恢复；确定性重置会恢复固定合成目录并让旧会话失效。

G2 完成的是 v1“真实可操作”的功能基线，不等于协议 v2、最终视觉或正式现场验收。D-021 记录 G3 的桌面浏览器自动化与 vivo X300 默认浏览器人工核心旅程均通过，且未发现或复现未关闭 S0/S1；真机浏览器版本和实际 CSS 视口未单独采集的限制已在验收记录中接受。D-021 记录 G4 的 300 人本机协议负载专项通过，但不替代 v2 的新协议负载、30 分钟真实渲染 soak、分布式局域网、场馆或正式上线验收。D-023/D-024 的视觉预览、星色、身份字段迁移与时光胶囊候选池只作为 v1/G5 历史实现保留。

## 动画同学快捷入口

Windows 下可直接双击仓库根目录的 `打开手机端预览.cmd`。它会自动进入正确目录，优先使用系统或 Codex 自带的 `pnpm`，并在首次缺少依赖时完成准备。启动后会打开 390×844 的真实 v2 `/welcome` 页面和手机动线检查器；控制上下文中的 `/admin` 以合成管理员会话驱动 READY、三场景、暂停/恢复、节目选择与终章。检查器可依次进入星色选择、锁色入公共星系、胶囊提交/跳过、等待启程、三个现场场景、权威终章与刷新恢复，正常页面不包含调试入口。

预览会在 OS 临时目录建立固定 300 人合成目录，执行受控 v2 切换后运行真实 Fastify HTTP/WebSocket 与 Vue 页面；它不会读取或修改 `backend/.data/`。这套预览可证明当前桌面浏览器中的 v2 手机动线与恢复投影，不等同于 V2-09 已完成的 300 人协议负载。V2-10 的 30 分钟自动渲染 soak 已通过，但该预览与自动长跑都不替代 vivo X300、三端局域网与 OBS 实际合成人工验收。

D-027 已把 normal-motion 正常首次寻星修订为稳定 `visible` 后约 2.8 秒不可跳过镜头，并通过几何、时长、可见性、恢复和业务隔离自动门；项目负责人随后在真机否决了首版视觉连续性，因此该历史 `PASS` 不等于视觉通过。D-028 当前版本改为电影与选色共用同一颗恒星、`50% / 42vh` 统一视觉轴、四阶段全屏径向穿越和白核—中性晕—默认 `5800K` 色温晕连续交接；选色控件预渲染但在交接前不可操作，主动画结束只解除隔离，不换星、不跳位。手机端仍不迁移 GSAP。D-028 自动连续性子门已通过；vivo X300 必须用当前版本的新鲜邀请重新完成人工视觉签核，三端局域网与 OBS 现场复验也仍为 `PENDING`。

D-029 已把确认后的完整个人旅程接入正式 `/welcome`：锁色后同一颗星先闪烁并进入寄语，提交或持久化跳过后再拉远到 Canvas 本人轨道；低亮轨道微尘负责低人数时的环绕层次，不冒充真实参与者。操作坞使用高透玻璃，正常动态完整旅程的聚焦 Chrome smoke 已通过；减少动态、恢复和中断仍直接静态。vivo X300 当前版本人工复验仍为 `PENDING`。

只调试 `/welcome` 手机端星色与入场动画时，在仓库根目录执行：

```bash
pnpm preview:mobile
```

命令会自动打开手机窗口与检查器，后台控制页在独立控制标签中运行。前端 Vue、CSS 与 JS 保存后可热更新；星色锁定、胶囊决定、场景动作、节目选择、暂停/恢复和完成均调用隔离服务的真实 v2 接口。重看 normal-motion 首次动线必须关闭并重新启动预览、使用新鲜邀请和新会话；同一身份刷新/返回只验证“不重播”的恢复路径。关闭手机窗口或按 `Ctrl+C` 后，临时数据库、备份、会话和端口都会被清理。命令不会打印邀请令牌、合成姓名、合成学号或后台密码。

## V2-09 自动化与协议负载

```bash
pnpm test:v2:e2e
pnpm test:v2:load
pnpm verify:v2-09
```

2026-08-13 的最终本地证据为：全量 Vitest 32 个文件/292 条全部通过；全量 Playwright 12 个场景在 `chromium-ci`、系统 Chrome、系统 Edge 三个项目各执行一次，共 36/36 通过，其中 v2 三端闭环为 3/3；共享契约、后端、前端构建与相关类型检查均通过。v2 负载在独立 OS 临时合成库中完成 300 人、301 条 WebSocket 连接和 601 个逻辑流，300 次激活/锁色/胶囊决定/星星启动/弹幕/协同点亮与 1200 次礼物均成功；所有操作 p95 小于 2 秒，协议错误、重复帧、重复奖励/幂等事实、负余额、旧 epoch 污染和公开事件隐私泄漏均为 0。后端重启后 301/301 连接恢复，重置使 301 条旧连接收到合法 epoch 失效通知，旧会话/旧 epoch 被拒绝且固定凭据可在新 epoch 重新激活。脱敏报告位于被 Git 忽略的 `tests/reports/v2-load.json`。

这些是 Windows 单机 loopback 的协议与桌面浏览器证据，不是 300 个真实浏览器同时渲染，也不能单独证明 30 分钟运行、vivo X300、OBS 或现场局域网验收。V2-10 的 30 分钟自动渲染 soak 已另行通过；vivo X300、三端局域网与 OBS 实际合成人工验收仍为 `PENDING`。

## V2-10 自动渲染门与现场预览

```bash
pnpm test:v2:soak:smoke
pnpm test:v2:soak
```

2026-08-13 的正式渲染长跑在 Google Chrome 151.0.7922.137、1920×1080、普通动态与独立 reduced-motion 浏览器进程中完成，计划受测时间 1,800,000 ms、实际 1,800,056.8 ms。六段依次覆盖 0 星 READY、24 星典型 READY、300 星集结、300 星节目透明层、300 星协同点亮与 300 星终章；动态星系约 27.73～28.60fps，节目层持续绘制为 0，reduced-motion 六段持续绘制均为 0。最大 JS 堆约 34.1MB，DOM 峰值 59，最长任务 215ms；两页控制台错误、页面错误、外部请求和 HTTP 异常均为 0。后端重启后继续完成点亮与终章，终章刷新从权威快照恢复。节目层背景透明、中心 Canvas 最大 alpha 为 0，且没有 `video`、`audio` 或 `iframe`。脱敏报告与截图分别写入被 Git 忽略的 `tests/reports/v2-render-soak.json` 与 `output/playwright/v2-10-render-soak/`。

现场人工验收使用隔离的临时 v2 栈，不触碰 `backend/.data/`：

```powershell
$env:DEMO_HOST = '本机可信局域网 IPv4'
pnpm preview:v2:field
```

命令只把统一的 Vite 入口绑定到明确私网 IPv4；Fastify 后端仍只监听本机回环。它会打开已登录的后台、大屏和专用“手机验收二维码”浏览器页；终端不再是看见二维码的前提。二维码页只显示一次性合成手机入口与扫码说明，不显示邀请令牌文本、姓名、学号或后台密码。按 `Ctrl+C` 或关闭预览浏览器后删除临时数据库。正式签核步骤见 [`docs/V2_10_FIELD_ACCEPTANCE.md`](./docs/V2_10_FIELD_ACCEPTANCE.md)；自动长跑不替代 OBS、局域网和 vivo X300 人工结果。

## 当前任务清单

本清单是协议 v2 的当前执行顺序；详细状态与验收边界见 [`docs/PROTOCOL_V2.md`](./docs/PROTOCOL_V2.md) 和 [`docs/TEST_PLAN.md`](./docs/TEST_PLAN.md)。完成状态必须以实际文件和验证证据为准。

- [x] **V2-00 协议/文档冻结**：记录 D-025，建立 v2 单一权威并同步当前文档；不包含代码实现。
- [x] **V2-01 契约与协议握手**：已建立 v2 共享契约、版本中立能力发现、严格握手与混用拒绝；业务运行时仍为 v1，V2-05 前不发送 v2 业务快照或实时事件。
- [x] **V2-02 数据库迁移与显式合成重置门**：已追加 `0008` 数据基础、一次性切换/验证/v2 重置 CLI、精确合成数据分类、不可覆盖备份、失败回滚，以及“真实监听—同代干净关闭”服务维护回执；实际 `.data` 未执行切换，当前业务运行时仍为 v1。
- [x] **V2-03 参与者入场命令/状态/账本**：已实现隔离的 v2 激活、锁色入公共星图、胶囊提交/持久跳过、晚到补填、只读恢复、幂等/revision 门与一次性奖励；正式 HTTP/实时启用仍随 V2-04～V2-08 原子发布。
- [x] **V2-04 三场景运行时与原子结束**：已实现三场景顺序/排练切换、参与者场景写入门、就绪警告与 override 审计、LIVE 一次确认原子结束及 REHEARSAL 仅演示结尾；正式 HTTP/实时启用仍随 V2-05～V2-08 原子发布。
- [x] **V2-05 v2 快照/实时/恒星 upsert/重同步**：已实现 Screen/Participant/Admin 权威快照、ACTIVE WebSocket、按受众分流游标、幂等恒星与聚合事件、快照后续订、局部缺口重同步和 epoch 全量收敛；当前 v1 页面仍待 V2-06～V2-08 接入。
- [x] **V2-06 后台控制/就绪检查/胶囊插入**：已实现 v2 后台登录/退出、三场景与终章控制、匿名入场漏斗、就绪警告及可审计 override、人工选择/插播/清除/带原因撤下、终态安全减法与合成 Demo 重置；v1 数据库仍显示原控制台。
- [x] **V2-07 大屏星系/插入/结尾**：已实现最多 300 颗真实恒星的 Canvas 星系、三场景投影、OBS 透明节目叠加层、匿名弹幕/礼物、胶囊独占插入、后台事后处置与权威终章；GSAP core 仅按需服务少量大屏覆层，业务事实仍完全由服务端决定。
- [x] **V2-08 手机入场/星系/操作坞/动效/提示/退出/草稿**：已完成 `/welcome` 能力切换、双流实时恢复、独立入场、真实星系、三场景操作、节目选择、3.5 秒成功反馈、持久异常、退出确认、内存草稿、短视口/键盘和 reduced-motion；检查器已迁移到隔离的真实 v2 临时栈。
- [x] **V2-09 聚焦自动化回归与 v2 300 人协议负载**：已完成全量类型检查/构建、32 文件/292 条 Vitest、三浏览器 36/36 Playwright、真实 v2 三端闭环与 300 人/301 WebSocket/601 逻辑流协议负载；恢复、幂等、账本、余额、epoch 与隐私不变量通过。
- [x] **D-027 手机首次寻星修订与自动子门**：约 2.8 秒 Vue+CSS 穿越镜头、稳定可见后开播、开播后后台中断静态、normal-motion 首次不可跳过及 reduced/刷新/恢复静态均已落地；新鲜邀请的聚焦浏览器用例与相关自动门已真实通过。该勾选只关闭自动子门，不代签 vivo X300、三端局域网或 OBS 人工门。
- [x] **D-028 手机首次寻星视觉连续性自动子门**：项目负责人真机否决 D-027 首版后，已实现同一持久恒星、统一视觉轴、四阶段全屏径向穿越、同心光晕、`animationend` 收束与预渲染控件无跳变交接。Chrome artifact 正常首次 `2783ms`、隐藏→可见 `2717ms`、F6→F7 中心/尺寸差 `0px`、CLS `0`，六帧目检、37 文件/341 条测试、两项 typecheck、71 模块构建、三项目 v2 E2E 与现场预览 smoke 均通过；该勾选仍不代签 vivo X300 人工视觉。
- [x] **D-029 手机个人旅程连续镜头聚焦子门**：正式 `/welcome` 已实现持久星“寻星 → 选色 → 闪烁 → 寄语 → 拉远入轨”，高透操作坞、装饰轨道微尘和真实星轻量环绕均已接入；Chrome 聚焦旅程观测寄语后入轨 `3298ms`，持久星未替换、最终单 Canvas 与本人标签可见。该勾选不代签真机或现场验收。
- [ ] **V2-10 真实浏览器 30 分钟渲染 soak 与 vivo X300/三端人工验收**：自动 30 分钟 Chrome 渲染与恢复门已通过，局域网临时预览入口与浏览器二维码页已就绪；待 OBS 实际合成、三端局域网和 vivo X300 用当前版本新鲜邀请完成 D-029 完整个人旅程视觉签核后关闭。

任务执行原则：一次只关闭一个可验证门禁；只把已有代码和测试证据的部分写成已完成。动画协作边界见 [`docs/ANIMATION_HANDOFF.md`](./docs/ANIMATION_HANDOFF.md)。

## 维护方式

当前项目由负责人本人长期单人维护。产品、前端、后端、视觉和测试仍按职责拆分，以便规划、审查和验收，但不假设固定有五名成员，也不把 AI 产出视为人类验收。

当前项目边界：

- 同一局域网内运行 `/welcome`、`/admin` 和 `/screen`；
- v1 六阶段只作为历史实现保留；v2 目标是参与者独立入场时钟、三场景运行时与 `COMPLETED` 终态；
- 不进行公网部署，不接真实名单，不写实体 NFC，不接 AI 审核服务；
- 正式活动、硬件、印刷、域名和真实数据方案后续单独确认。

实体物料采购、付款、供应商下单、样品测试和到货验收均属于正式版延期事项，不阻塞 Demo v0。

## 协作规则

- GitHub Issue、分支和文档是开发任务与验收的正式记录；即时通信仅作可选通知渠道。
- 原则上一个 Issue 对应一个功能分支和一个 Pull Request；Demo v0 的跨模块基线以 `docs/DECISIONS.md` 中的已确认例外为准。
- 不直接向 `main` 推送；AI 也只能在独立分支工作。
- AI 生成的代码由对应人类负责人审查，不能自行验收。
- 真实姓名、学号、手机号、名单、密钥、数据库和现场导出文件不得进入仓库。

详细规则见 [CONTRIBUTING.md](CONTRIBUTING.md)。
