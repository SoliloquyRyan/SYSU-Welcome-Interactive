# 贡献指南

感谢参与 SYSU Welcome Interactive。请先通过 Issue 明确需求、负责人、优先级、所属模块和验收标准，再开始实现。

协议 v2 的当前权威是 [`docs/DECISIONS.md`](./docs/DECISIONS.md) 的 D-025 与 [`docs/PROTOCOL_V2.md`](./docs/PROTOCOL_V2.md)。实施必须按 [README](./README.md) 的 V2-00～V2-10 顺序，在项目负责人明确授权的下一张 V2 卡片范围内进行；不得越过当前卡片预做后续契约、迁移、三端页面或上线动作。

## 开发环境

- Node.js 22.18.0 或兼容的更新版本
- pnpm 11.16.0

仓库使用根级 pnpm workspace 和唯一的根锁文件。请使用以下命令：

```bash
pnpm install --frozen-lockfile
pnpm dev
```

提交前从仓库根目录验证共享契约、后端、自动化测试与前端构建：

```bash
pnpm verify:g2
```

V2-09 的三浏览器与 300 人协议负载门已经实际通过，V2-10 的 30 分钟自动渲染 soak 也已通过；vivo X300、三端局域网与 OBS 实际合成人工验收仍为 `PENDING`。下述 `verify:g2`、`verify:g3`、`verify:g4` 与既有 G0～G4 浏览器/负载结果只证明 v1 历史基线或回归，不单独证明协议 v2；每张 V2 卡片还必须执行该卡片新增或更新的聚焦测试，并保留准确的命令、环境和结果。

涉及三端页面、浏览器恢复或 G3 验收的变更，还应在 Windows Chrome、Edge 和隔离 Chromium 环境执行：

```bash
pnpm exec playwright install chromium
pnpm verify:g3
```

浏览器报告只保留脱敏的环境、用例名称、状态与耗时；不要开启或提交含邀请令牌、Demo 码或学号、Cookie、密码、时光胶囊正文的 trace、截图、录屏和网络附件。

涉及实时协议、快照扇出、并发写入、恢复或 G4 性能边界的变更，先运行独立专项；准备收口 G4 时再运行包含 G3 全门的根命令：

```bash
pnpm test:load
pnpm verify:g4
```

`pnpm test:load` 使用 300 个固定合成参与者和独立临时数据库。生成的运行报告只允许保留脱敏汇总，并继续由 Git 忽略；不得提交凭据、姓名、完整学号、时光胶囊正文、完整弹幕正文、数据库或逐请求载荷。G4 的本机 loopback 结果不得扩张为分布式局域网、场馆、30 分钟 soak 或正式上线结论。

D-021 的提交前工作树验收见 [`docs/ACCEPTANCE_G0_G4.md`](./docs/ACCEPTANCE_G0_G4.md)：根级 `pnpm verify:g4` 退出码为 0，Vitest 20 个文件/119 条、共享契约/后端/前端构建，以及 9 场景 × 3 浏览器项目的 Playwright 27/27 均通过。该历史结果不绑定任何提交。G0～G4 冻结候选提交形成后，必须在该提交的干净工作树重跑适用门禁，并由 D-022 记录被验收提交的完整 SHA、命令结果与复验前后工作树状态；D-022 落地前只能称为冻结候选，不能称为可复现冻结版本。任何代码改动仍须重新执行适用门禁，不能长期借用 D-021 结果。

如果依赖发生变化，请更新对应 workspace 的 `package.json` 与根 `pnpm-lock.yaml`。不要提交 `node_modules/`、`dist/`、`backend/.data/`、测试报告、本地环境文件或编辑器临时文件。

协议 v2 还遵守以下硬边界：

- 不建立 v1/v2 双写、静默状态翻译或失败后回退六阶段的兼容旁路；
- 数据库变更只能新增下一编号迁移，不修改已冻结的 `0001`～`0007`；
- 破坏性切换只允许在明确确认全部数据均为可丢弃合成/Demo 数据后执行；发现真实、不可重建或有保留价值的数据必须停止，不能自动清空；
- 不因本机已安装技能而增加 GSAP、Three.js 或其他运行时依赖；任何新依赖都需要独立授权、范围、性能和降级审查。

## Issue 流程

新功能和缺陷分别使用对应 Issue 模板。每个 Issue 必须包含：

- 明确的负责人或“待认领”；
- P0–P3 优先级；
- 所属模块；
- 背景、复现信息或建议方案；
- 可检查的验收标准。

P0 表示阻断交付或现场核心流程，需优先同步项目负责人；P1 为高影响事项；P2 为常规需求；P3 为低优先级改进。

## 分支与提交

通常从最新的 `main` 创建短生命周期分支，建议使用：

- `feature/<简短名称>`
- `fix/<简短名称>`
- `docs/<简短名称>`

已记录的例外：`feature/demo-v0` 根据 `docs/DECISIONS.md` 的 D-006，从 `design/invitation-ui-guide` 创建，以继承尚未合并的三端视觉基线；不得为此切换到 `main`、新建仓库或额外工作区。该例外不改变其他功能分支的默认规则。

提交应聚焦单一目的，说明“做了什么”和“为什么”。不要把格式化、依赖升级和功能修改无关地混在同一个提交中。

## Pull Request

创建 PR 时请完整填写模板，至少包括：

- 关联 Issue；
- 变更说明；
- 界面截图或录屏（可见改动）；
- 已执行的测试与构建命令；
- 数据、接口、本地存储及兼容性影响；
- 可执行的回退方案；
- 无敏感信息确认。

PR 必须通过 CI 的冻结依赖安装、类型检查、自动化测试和生产构建。评审意见处理完成后再合并；未解决的高优先级问题不得以备注代替修复。

## 数据与安全

- 仅采集实现功能所必需的数据，并避免在页面、日志、截图或大屏中暴露姓名、学号等个人信息。
- 不得提交访问令牌、API 密钥、密码、私有链接或真实个人数据。
- 示例、测试和截图必须使用虚构或脱敏数据。
- 发现凭据泄露时，不要在公开 Issue 中粘贴内容；应立即通知负责人并轮换凭据。
