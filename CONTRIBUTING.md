# 贡献指南

感谢参与 SYSU Welcome Interactive。请先通过 Issue 明确需求、负责人、优先级、所属模块和验收标准，再开始实现。

## 开发环境

- Node.js 22.18.0 或兼容的更新版本
- pnpm 11.16.0

仓库根目录没有根级 `package.json`，前端项目与锁文件均位于 `frontend/`。请使用以下命令：

```bash
cd frontend
pnpm install --frozen-lockfile
pnpm dev
```

提交前从仓库根目录验证生产构建：

```bash
pnpm --dir frontend build
```

如果依赖发生变化，请在 `frontend/` 中更新并提交 `package.json` 与 `pnpm-lock.yaml`。不要提交 `node_modules/`、`dist/`、本地环境文件或编辑器临时文件。

## Issue 流程

新功能和缺陷分别使用对应 Issue 模板。每个 Issue 必须包含：

- 明确的负责人或“待认领”；
- P0–P3 优先级；
- 所属模块；
- 背景、复现信息或建议方案；
- 可检查的验收标准。

P0 表示阻断交付或现场核心流程，需优先同步项目负责人；P1 为高影响事项；P2 为常规需求；P3 为低优先级改进。

## 分支与提交

从最新的 `main` 创建短生命周期分支，建议使用：

- `feature/<简短名称>`
- `fix/<简短名称>`
- `docs/<简短名称>`

提交应聚焦单一目的，说明“做了什么”和“为什么”。不要把格式化、依赖升级和功能修改无关地混在同一个提交中。

## Pull Request

创建 PR 时请完整填写模板，至少包括：

- 关联 Issue；
- 变更说明；
- 界面截图或录屏（可见改动）；
- 已执行的测试与构建命令；
- 数据、接口、本地存储及兼容性影响；
- 可执行的回退方案；
- 无敏感信息确认。

PR 必须通过 CI 的冻结依赖安装和前端生产构建。评审意见处理完成后再合并；未解决的高优先级问题不得以备注代替修复。

## 数据与安全

- 仅采集实现功能所必需的数据，并避免在页面、日志、截图或大屏中暴露姓名、学号等个人信息。
- 不得提交访问令牌、API 密钥、密码、私有链接或真实个人数据。
- 示例、测试和截图必须使用虚构或脱敏数据。
- 发现凭据泄露时，不要在公开 Issue 中粘贴内容；应立即通知负责人并轮换凭据。

