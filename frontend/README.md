# 前端

Vue 3 + Vite 初始项目，当前只提供三个不会收集真实数据的占位入口：

- `/welcome`：NFC、二维码和短网址进入的手机端统一入口。
- `/screen`：现场大屏。
- `/admin`：内容审核与现场控制后台。

## 本地运行

```bash
pnpm install
pnpm dev
```

构建检查：

```bash
pnpm build
```

正式功能开发前，请先阅读仓库根目录的 `README.md`、`CONTRIBUTING.md` 和 `docs/`。
