# D-067 夜间改版验收

当前状态：2026-09-07 已部署并完成下列范围的验证。保留已有工作区改动，未提交或推送。

## 范围和操作

- 保留 C 字体。大屏降低银河观察倾角、拉近镜头，加强核心、尘带和旋转视差；手机增强星核与层次。
- 手机节目单显示名称与原始节目单提供的表演者，当前节目蓝色。串场内容不是表演者，不误标为人名。竞拍继续延期。
- 默认大屏节目阶段有低对比星海底图。OBS 媒体叠加源使用 `/welcomeparty/screen?media=overlay`，报幕背景源使用 `/welcomeparty/screen`。在 OBS 场景间切换这两种浏览器源；后台切换当前节目触发 8.5 秒标题/表演者淡出，不自动播放演出视频。
- 手机显示实时匿名消息（D-068 已改为可上下滚动的最近 100 条列表，不再定时移除），不追播刷新前历史；撤回、清屏、换场和离开页面清理。付费配色为服务端枚举，前端不接收任意 CSS。
- 五种单色免费；极光、日落、星云各 10 动力首次成功发送时解锁。以本场代际和参与者为范围，重复请求不扣第二次。审核/限流/余额不足回滚交易。
- 100 初始动力保留。每个表演首份礼物减免最多 10 动力，5 动力礼物减 5，未用满不累计；按节目持久记录，排练来回跳转不会重复补贴。18 个节目可各送一个基础礼物，再自由分配余额。热度仍按礼物面值记录。
- 手机音乐按钮点按后播放原创合成钢琴《星海序曲》；进入节目阶段渐弱。未包含《星际穿越》原曲，替换原曲仍需要主办方提供可用音源。网页隐藏自动静音，回到页面不自行响起。
- 终章增加原始节目单的演出鸣谢，抽奖统一为星蓝银白；星号仍为姓氏首字母与学号后四位。

## 数据和发布

新增 schema 16：弹幕样式列和按代际/参与者唯一的解锁、礼物减免记录。schema 15 升级入口为 `backend/src/cli/interaction-upgrade.ts`，必须停止服务、指定全新备份并传入 `--confirm V2_SERVICES_STOPPED`；CLI 验证来源、备份与目标库，事务失败回滚。不重置账号、轮次、星星或节目进度。

## 验证记录

- 单元/API/集成全量 **453/453 通过**，报告 `output/overnight-20260907/all-tests-final.json`。覆盖配色首次付费、幂等重试、非法样式、余额不足、服务端扣费及 schema 15→16 备份升级。
- 三浏览器主流程首轮 11/12；Chrome 一次提前结束暴露时钟差异，已用 `performance.now()` 防止提前清空镜头。修复后 Chrome 主流程与手机布局 **6/6 通过**，Chromium 音乐及渐变弹幕补充 **2/2 通过**。MS Edge 首轮 4/4 通过；最终时钟保护未另外重跑完整 Edge 套件。原始日志分别为 `browser-final.log`、`browser-verified.log`、`gradient-music.log`。
- 前后端构建、e2e/soak 类型检查、文档链接检查和 `git diff --check` 通过。未运行整夜 soak 或实际投影设备亮度验收。
- 线上网页加载 `index-BIUjO4lB.js`，默认完整动效；报幕底图 2.4 秒前后 transform 改变，`media=overlay` 底图元素为 0。
- 本机 OBS 32.2.1 浏览器源已刷新。恢复其最小化窗口后采集实际节目画面两帧，间隔至少 3 秒，预览区 47.9% 像素有变化；这是缓慢报幕背景的真实运动证据，不代表本次在 OBS 重演了银河/抽奖/全部转场。检查后恢复原最小化状态，未开推流或录制。
- 截图：`output/overnight-20260907/screen-galaxy.png`、`mobile-program.png`、`mobile-gradient.png`、`screen-finale.png`、`live-screen.png`。截图用于本轮观察，不替代手机真机或现场审美确认。

## 线上版本与回滚

- 当前 release：`/opt/sysu-welcome-internal/releases/20260907-d067-f17932086823`；发布包 SHA-256：`f179320868236979d2deec654aab0c13ffe80af52adc8dac10f3850e4229d1c3`，合并清单 402 项。
- 备份：`/var/lib/sysu-welcome-internal/backups/d067-before-20260907.sqlite`，SHA-256：`c1507b7409782490b766314d83fa106f9230fde7c1da3aeac5758baa23342174`。
- 前后均为 epoch 8、REHEARSAL/RUNNING/PROGRAM_SUPPORT、runRevision 6、1 颗星；节目、聚合、抽奖快照相等。`party-screen-site` 和 Nginx PID 保持，根站与晚会子路径均可用。
- 现在不能只切回 D-066 的旧后端。回滚需停止本项目服务，先保存当前 schema 16 数据，再恢复上述 schema 15 一致性备份、切回 D-066 release 并验证启动；发布后发生的新增互动需要另行处理，不能无声丢弃。

## 明早建议验收顺序

1. 刷新手机和后台，排练切回集结，看大屏与手机银河、点开音乐。
2. 推进节目，确认音乐淡出；选择节目，看名称和表演者出现后淡出。
3. 发送免费单色弹幕，再发一种渐变，确认大小屏同时显示、只在首次扣 10 动力；后台撤下确认消失。
4. 每个新节目首礼使用减免，切回旧节目确认不会再次领取。检查抽奖与终章鸣谢。
5. OBS 演出时切到透明浏览器源并在下方放媒体源；主手机平台的音色/音量、投影亮度和现场视频搭配仍由现场人员确认。

