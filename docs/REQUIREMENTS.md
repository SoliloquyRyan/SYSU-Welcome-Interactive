# 产品与系统需求（v2 现行）

> D-099（2026-09-13，本地完成，未发布）：精修手机/主控/大屏共用面板与操作反馈，修复登录错误、弹幕确认焦点、谢幕全量回顾和撤下内容重现；独立合成场次已执行完整流程，495项测试及三浏览器各10项流程共30项全部通过；44张截图与关键录屏已整理。schema 21保持。见 [D-099](./D099_UI_AND_FULL_FLOW.md)。

> D-098（2026-09-13，本地完成，未发布）：手机节目超新星改为热核、云气喷流与碎光余辉；节目/主持/颁奖舞台加入学院标志与层叠银蓝光带。16份媒体已审查，建议 OBS 本地播放并叠加系统互动，节目对应差异待确认。schema 21及现有业务保持。见 [D-098](./D098_STAGE_CRAFT_AND_MEDIA.md)。

> D-097（2026-09-12，本地完成，未发布）：节目颁奖自动取有效动力值前三名，后台可调整节目动力值并保留原始礼物与调整记录；收紧独立环节间距，手机与大屏采用统一银蓝节目舞台，手机实时开场增加一次可跳过的超新星转场。代码目标 schema 21，线上仍以 D-095 / schema 19 为准。见 [D-097](./D097_PROGRAM_RANKING_AND_STAGE.md)。

> D-096（2026-09-12，本地完成）：新增节目颁奖、负责人讲话与校园图鉴颁奖；目录共 25 项，互动/颁奖/讲话使用无字母编号的独立分隔行，校园图鉴在手机节目单只显示标题；《光年之外》关闭礼物，主控支持黑金主持背景、草稿名单、确认揭晓和分页。代码目标 schema 20，线上仍为 D-095 / schema 19；未部署或复位线上场次。验证和操作见 [D-096](./D096_AWARDS_AND_STAGE.md)。

> D-095（2026-09-11，已部署）：D-088～D-094 前后端已发布，数据库保留升级至 schema 19；其他项目与 Nginx 不变。471 项测试、公网三端和 50 个旧测试身份核验通过。当前仍保留旧测试轮次与 21 项目录，复位并载入新 22 项需主控确认。见 [发布记录](./D095_SERVER_RELEASE.md)。


> D-093（2026-09-11，本地实现，未发布）：半透明玻璃弹层、星舰串行播放后间隔 2 秒、大屏独立礼物卡片弹跳并显示数量与星舰礼物值。见 [本轮验证](./D093_GLASS_GIFT_RHYTHM.md)。

> D-092（2026-09-11，本地实现并验证，未发布）：开场后大屏改为深色舞台，星舰重绘为暗色细线舰体且每节目最多两次完整特效；取消星光入口与新增积分，抢答采用统一三秒倒计时，互动面板移到输入框上方，礼物选择数量后发送，采用八色色板及精简确认。469 项测试通过，三端浏览器截图与星舰录屏见 [本轮验收](./D092_INTERFACE_REFINEMENT.md)。

> D-089（2026-09-11，本地实现，未发布）：正式节目独立编号，互动环节使用 A/B/C 且不计入节目；A/C 为服务器首位抢答，B 为上台观众抽取与一人一票；礼物支持数量并按 1/5/10/20 完整扣费，弹幕支持服务端个人星色和高级款首次确认；大屏与谢幕读取完整礼物、节目和弹幕账目。见 [D-089](./D089_LIVE_INTERACTION_ACCEPTANCE.md)。

> D-088（2026-09-10，本地实现，未发布）：表演者按节目 ID 纳入后台目录，同步手机、报幕和片尾；片尾沿用 D-086 银河，字幕只播一轮后定格，静态名单可翻页。维护为 schema 17→18，保留场次和互动记录。实现与验收见 [演职员目录与银河片尾](./D088_CREDITS_ACCEPTANCE.md)。线上仍以 D-087 发布记录为准。

> D-087（2026-09-09）：负责人授权发布已认可 D-086 并审查全流程；已完成前端更新，业务规则保持。后续待改包括演职员目录统一、片尾与抽奖细化，具体建议见 [流程审查](./D087_DEPLOYMENT_FLOW_REVIEW.md)，不构成新增业务规则批准。当前操作见 [主控单](./LIVE_OPERATOR_GUIDE.md)。

> D-086（2026-09-09，本地轻微尘动）：按负责人要求，四周薄尘以低强度缓慢游移散聚，保留 D-085 配色、层次和中央银河。两端复用原时钟，暂停及静态模式保持；未发布服务器。验证见 [D-086 记录](./D086_SUBTLE_DUST_MOTION.md)。

> D-085（2026-09-09，本地远景层次）：按负责人要求，在 D-084 柔和背景上补充细碎远星尘、小片星团及被近处暗尘遮挡的薄雾，维持既有配色与白边强度。无参与者或业务变化；未发布服务器。验证见 [D-085 记录](./D085_BACKGROUND_DEPTH.md)。

> D-084（2026-09-09，本地四周尘云）：负责人要求背景更深、增加银白轮廓，并延伸到四周。以气体云、暗尘埃和局部受光边缘呈现；按后续“喧宾夺主”反馈降低雾团与白边对比。横竖屏分别取景，中央动态银河保持。未发布服务器；验证见 [D-084 记录](./D084_SURROUNDING_DUST_CLOUDS.md)。

> D-083（2026-09-09，本地背景柔化）：按负责人要求，将 D-082 突兀的纯色色斑淡化，改为疏密不一、边缘不规则的薄雾远景。维持黑色主调及背景与银灰动态星云的分层。未发布服务器；验证见 [D-083 记录](./D083_SOFT_BACKGROUND_ART.md)。

> D-082（2026-09-09，本地纠正）：负责人澄清只要求背景显色，流动星云仍是独立主体。纠正 D-081 彩色远景与银河旋臂重叠的观感，将颜色移至外围深空并柔化纹理，主星云保持银灰、暖白与原有运动。未发布服务器；验证见 [D-082 记录](./D082_BACKGROUND_SEPARATION.md)。

> D-081（2026-09-09，本地显色调整）：负责人反馈 D-080 仍太浅，将局部紫色、冷青和暖金增强至清楚可见。保留 D-079 黑色底调与既有色彩分布；仅调整三处材质颜色，无业务变更。未发布服务器；验证见 [D-081 记录](./D081_VISIBLE_NEBULA_COLOR.md)。

> D-080（2026-09-09，本地微调）：按负责人批注，在 D-079 黑色底调上稍增强局部暗紫、冷青和暖金。仅校准共享背景材质三处色彩强度，保持原有分布、银河与星体。未发布服务器；验证见 [D-080 记录](./D080_ACCENT_COLOR_ACCEPTANCE.md)。

> D-079（本地黑色深空配色 / 审片）：负责人明确要求黑色为主体，局部点缀深空颜色。背景改为中性炭黑、墨黑与灰银云气，暗紫/冷青/暖金仅局部出现；主星云适度去蓝。覆盖 D-078 的大面积墨蓝配色，星色、尺度、人数与散聚动效保持。未发布服务器；实现与验证见 [D-079 记录](./D079_BLACK_SPACE_ACCEPTANCE.md)。

> D-078（2026-09-08，本地背景精修 / 审片）：保留 D-077 已认可银河、星色、手机尺度与云层聚散，增加低对比远景云气、细碎星尘和银蓝/暖金层次。两端与静态回退共用缓存程序材质，参与者星体和人数语义保持。本轮未发布服务器；实现与验证见 [D-078 记录](./D078_REFINED_SKY_ACCEPTANCE.md)。

> D-077（2026-09-08，本地精修 / 审片）：延续 D-076 已认可构图，强化大屏各人已选星色，手机轨道个人星缩小约 29%，星云沿原旋臂局部消散后重新凝聚，两端背景暗部抬亮。身份、人数、静态与 OBS 透明交接规则保持。本轮未发布服务器；实现与验证见 [D-077 记录](./D077_CLOUD_AND_STAR_ACCEPTANCE.md)。

> D-076（2026-09-08，本地实现 / 审片）：以原版倾斜盘面和留白为基础，统一手机与大屏的深墨蓝、银蓝及暖白光色。背景、五层流动星云与稳定星体使用共享路径和取景模型；大屏恢复左下标题，保留真实入场人数。替代本地 D-075 柔光图片美术，业务与 12 秒开场等现行时长保持。本轮未发布服务器；最近既有发布记录仍见 D-075。具体实现、审片与验证见 [D-076 记录](./D076_FLOWING_GALAXY_ACCEPTANCE.md)。

> D-075（既有发布记录；本地美术由 D-076 替代）：采用柔光星河，中央宽幅银蓝/珍珠暖光、边缘暗，背景降低细节反差以衬托动态星体。手机与大屏共享有界运镜、前后景视差和取景，集结标题位于左上暗部；覆盖 D-073 / D-074 具体美术，人数、身份、业务、12 秒开场、静态与 OBS 透明契约保持。实现与最终验证见 [D-075 验收](./D075_SOFT_STAR_RIVER_ACCEPTANCE.md)。

> D-072（2026-09-07）：当前美术以 [电影感修订与验收](./D072_CINEMATIC_ACCEPTANCE.md) 为准，覆盖下文旧 8.4 秒开场和 D-071 星舰美术。开场现为 12 秒；星舰手机 5.8 秒、公屏 7.2 秒；普通节目背景使用持续流动的正式银河，只有 `media=overlay` 保持透明并停止绘制。手机内容页去整面模糊，档案收紧布局。修复同一连接中快照先于礼物消息导致的漏播，状态与计数不回退。schema 17、四档礼物、身份、审核和抽奖规则保持。

> D-071（2026-09-07，已部署）：礼物每次按 1/5/10/20 完整扣除，不再有节目首礼减免；本人档案保存按节目汇总的礼物和本场弹幕，当前节目显示已收礼物数量。20 动力星舰只在实时成功事件中让在线手机和公屏播放银河掠过动画。手机聊天改为可上翻的半透明独立气泡，银河背板保持可见。schema 17 保留历史交易与旧减免审计，详情见 [验收记录](./D071_GIFT_EXPERIENCE_ACCEPTANCE.md)。
>
> D-070：手机常用页面改为内容优先布局，取消导航标题重复逐字，保留首次入场 5.4s/1.0s/4.2s 与 C 字体。节目名称/表演者为主标题、列表与档案使用扩展正文；键盘态隐藏导航与选色，输入独立保留。礼物四宫格、44px 色块点击区、抽奖提示、协同进度和个人纪念卡已接入；业务与数据库不变。
> D-069（2026-09-07）：手机发送不再要求匿名勾选；手机聊天列表和后台弹幕列表展示服务器提供的发送者 publicStarId（星号）及消息正文。覆盖此前匿名弹幕约定；新公开事件携带星号，后台旧消息关联星位补全，完整姓名/学号/身份 ID 不公开。
> D-068：手机节目互动区改为上下滚动聊天列表，保留当前页面最近 100 条已收到的匿名消息，不再横向飘过或定时消失。上翻时保持阅读位置，提供新消息提示与回到最新。大屏显示方式不变。

> D-067（2026-09-07，已部署；453/453，浏览器及 OBS 验证范围见记录）：本次用户要求覆盖早期“无手机音乐、手机不显示公共弹幕、节目态只能透明”的限制。新版默认节目星海底图，OBS 表演视频用 `/screen?media=overlay`；节目切换显示标题及节目单表演者后 8.5 秒淡出。手机匿名实时弹幕与大屏共用服务端审核，五种单色免费、三种渐变各 10 动力本场解锁一次。余额仍为 100；每个表演首礼减免最多 10 动力，不可重复领取、不跨节目积攒减免。开场音乐是原创 Web Audio 轻钢琴音色，点按开启、切节目淡出；未打包或下载《星际穿越》原声。字体保留已认可方案 C。数据库新增 schema 16，升级保留业务状态，必须先停本项目服务并建立验证备份。最终验收见 [本轮记录](./D067_OVERNIGHT_ACCEPTANCE.md)。

> D-058 手机实现稿：选色滑杆沿用现有色温映射，始终保留 Kelvin、键盘操作、不可更改说明与异常提示；完成页只以本人已确认事实生成私有纪念片段，未完成的动作不补写、不奖励、不公开。具体构图见 [`VISUAL_GUIDE.md`](./VISUAL_GUIDE.md) §2.4；此次视觉稿仍待负责人评审，不代签真机或大屏片尾。

> D-057 补充：抽奖按持久顺序完整揭晓且不提前泄露可见结果；协同聚合驱动大屏可读进度与集体光场，静态恢复可用；已知结束警告合并为一次确认；验收记录拒绝空白默认通过。具体规则见 [`PROTOCOL_V2.md`](./PROTOCOL_V2.md) §3.4–3.5 和 [`RUNBOOK.md`](./RUNBOOK.md) §4–5。该轮仅修缺陷；手机当前改稿见 D-058，银河镜头和大屏终章回忆录仍未重设计。

> 状态：协议 v2 现行。D-056 建立正式单机 HTTPS 部署、仓库外私有持久化、生产启动前验证、SQLite 一致性备份/全新目录恢复与固定合成排练要求。D-055 将 220 人设为正式大屏视觉满场参考，并启用低倾角盘状银河、420 颗无身份底星、未解析盘面光的有界密度补偿、环境层克制呼吸及逐人流星轨道捕获；300 人只保留为技术压力上限。D-054 已授权并导入 220 人受保护名单，正式库与合成测试库隔离，NFC 使用匿名随机令牌，人工恢复支持 8 位真实学号；D-037 的中场抽奖、D-051 的超新星接管、D-052 的通用实体手机门和 D-053 的 Orbital Signal 三端视觉体系继续有效。协议细节与验收口径以 [`PROTOCOL_V2.md`](./PROTOCOL_V2.md) 为权威；视觉细节以 [`VISUAL_GUIDE.md`](./VISUAL_GUIDE.md) 为准。v1 六阶段历史需求已归档。

## 1. 角色

- **Demo 参与者**：固定合成邀请令牌进入 H5；二维码/短链接携带同一令牌；人工协助使用虚构姓名 + 合成学号定位同一身份，不得建立第二账号。
- **共用后台操作会话**：登录后可自提 `REVIEWER`、`STAGE_CONTROLLER`、`DEMO_ADMIN` 或 `ALL`；角色只是能力分组（接受无个人归因风险，D-010）。
- **大屏会话**：只读聚合数据、公开星号/星色、公开弹幕与抽奖公开结果；不读取姓名。
- **项目负责人**：单人维护，负责产品取舍、运行与最终验收。

## 2. 入口与身份

- **REQ-ENTRY-001**：NFC 为主入口、同令牌二维码/短链接为兼容备用；私密写卡映射已生成，正式 HTTPS origin、实体写卡与印刷仍为 `FORMAL-PENDING`。
- **REQ-ENTRY-002**：所有入口进入同一 H5 并恢复同一参与者状态，不得重复发放或生成两个档案。
- **REQ-ENTRY-003**：入口 URL 与 NFC 数据不得包含姓名、学号等个人信息。
- **REQ-ENTRY-004**：页面提供轻触、扫码与人工协助说明。
- **REQ-ENTRY-005**：后台可识别有效/无效/已作废合成入口；令牌操作需 `DEMO_ADMIN`/`ALL` + 确认。
- **REQ-ENTRY-006**：一人一卡一码；同卡 NFC 与二维码编码同一不可猜测随机令牌。
- **REQ-ENTRY-007**：入口 URL 可承载令牌；读取后立即把地址栏与历史项替换为不含令牌的 `/welcome`，不得外传或写日志。
- **REQ-ID-001**：个性 NFC 令牌是主要核验方式；二维码/短链接携带同一 256 位随机令牌。人工协助使用姓名与 8 位学号恢复同一身份，不得生成第二账号；失败信息不得分别泄露姓名或学号是否存在。合成 Demo 通过服务端兼容层接受其 8 位测试学号，客户端不得再补写固定前缀。
- **REQ-ID-002**：D-065 统一正式和合成星号为“姓氏拼音首字母大写 + `-` + 学号后四位”，保留数字前导零，例如 `Z-0123`。复姓取首个字母，英文登记名默认取开头字母；新导入可用 `surnameInitial` 明确校正。完整目录须先检查重号，冲突只报告行号并停止，不自动追加字符或覆盖；数据库继续保证唯一。已有正式库只能在未激活、无运行记录的 READY 离线维护窗口同步更新目录与映射。
- **REQ-ID-003**：同一参与者多设备登录共享同一服务端状态。
- **REQ-ID-004**：真实名单来源与核验授权已确认；本次正式目录共 220 人，只导入姓名与 8 位学号，性别/班级不得进入运行库。访客模式与现场碰撞/补卡应急仍为 `FORMAL-PENDING`。
- **REQ-ID-005**：正式库必须是独立 `PROTECTED` 数据库；测试账号只能来自独立合成库，不得进入正式星系、计数、抽奖或后台身份目录。
- **REQ-ID-006**：D-065 明确允许公开星号展示姓氏首字母及学号后四位，因此不再把它描述为与身份无关的随机匿名标识；该编号不可用于身份认证。完整学号、姓名及凭据仍不得新增到公共事件、快照或错误中，NFC URL 继续只携带独立随机令牌。“完整学号→NFC URL”只保存在 Git 忽略、最小权限的私密映射中。

## 3. 协议 v2 要求（V2-MUST，详见 PROTOCOL_V2 对应章节）

### 3.1 版本与权威状态（PROTOCOL_V2 §1/§4.2）

- **REQ-V2-PROTOCOL-001**：命令、快照、响应、事件携带 `protocolVersion=2` 与 `resetEpoch`；v1/v2 不得同实例混用。revision 家族：`runRevision`、`presentationRevision`、`participantRevision`、`aggregateRevision`、`starRevision`、`interactionRevision`；`public|admin|participant:<id>` 三流各自连续 `streamSeq`。
- **REQ-V2-PROTOCOL-002**：合法运行 tuple 只有 `READY+null`、`RUNNING/PAUSED+三场景之一`、`LIVE+COMPLETED+COOPERATIVE_LIGHT`。
- **REQ-V2-PROTOCOL-003**：LIVE 终章由最后场景一次确认、服务端原子写入 `COMPLETED`；REHEARSAL 只能显式预演结尾。
- **REQ-V2-PROTOCOL-004**：`PAUSED` 拒绝参与者写入、允许只读恢复；`COMPLETED` 硬关闭写入；安全处置不含数值调整。
- **REQ-V2-PROTOCOL-005**：管理命令迁移（`SET_MODE` 仅 READY、`START`、REHEARSAL 可跳场景、LIVE 只向前、`PAUSE`/`RESUME`、`COMPLETE`）非法组合稳定拒绝。
- **REQ-V2-PROTOCOL-006**：错误码为 PROTOCOL_V2 §9 的闭合集合，不继承 v1 码。
- **REQ-V2-PROTOCOL-007**：参与者快照含完整个人事实与服务端计算的 `allowedActions`；事件后先刷新快照再启用写入。

### 3.2 参与者入场与晚到者（PROTOCOL_V2 §2）

- **REQ-V2-ONBOARD-001**：激活先原子预留 300 slot 之一，再建会话并发 100 动力 + 20 星光一次；满员以 `STAR_CAPACITY_REACHED` 整笔拒绝。
- **REQ-V2-ONBOARD-002**：`READY`/`RUNNING` 接受新到与晚到者；晚到者完成后直接进入当前场景，不补播。
- **REQ-V2-ONBOARD-003**：锁色即把预留恒星以 `publicStarId` 原子加入公开星系、发 `star.node.upserted` 并直接准入；本人端突出标注，他人无标签。
- **REQ-V2-ONBOARD-004**：手机端、后台和大屏均不提供寄语/时光胶囊入口；旧字段只作迁移兼容，旧写命令稳定拒绝。
- **REQ-V2-ONBOARD-005**：重复、刷新、多设备、断线、并发不重复激活/锁色/入星系/奖励。
- **REQ-V2-ONBOARD-006**：`PAUSED` 不接受入场写入；`COMPLETED` 后本 epoch 已激活身份只读重认证，未完成者不能继续入场。

### 3.3 星系与三场景（PROTOCOL_V2 §3/§4）

- **REQ-V2-GALAXY-001**：协议与渲染技术上限继续为 300 颗真实恒星，正式受保护目录及正式视觉满场参考为 220 人；不得生成携带身份或参与者外观的假星补位，也不得静默截断真实星，slot 从确定性目录预留。无身份中性底星和未解析盘面光只属于装饰环境，不进入快照、计数、聚合或抽奖。
- **REQ-V2-GALAXY-002**：客户端先取权威快照再幂等消费事件；刷新/重连/乱序/重放不制造重复恒星或伪缺口。
- **REQ-V2-SCENE-001**：`ASSEMBLY` 仅一次 `START_STAR`（+40 星光）；`PROGRAM_SUPPORT` 允许礼物/弹幕/抽奖；`COOPERATIVE_LIGHT` 仅一次点亮（+20 星光）；晚到者不补旧场景奖励。
- **REQ-V2-SCENE-002**：presentation 为服务端权威互斥状态 `NONE|RAFFLE|FINALE_PREVIEW`，独立 `presentationRevision`；抽奖只在 `RUNNING + PROGRAM_SUPPORT` 合法。
- **REQ-V2-SCENE-003**：抽奖由服务端在已准入参与者中无放回随机抽取；后台显示姓名+星号，大屏只显示星号；场景变化/暂停/完成自动关闭投影并保留记录，排练模式可清空。
- **REQ-V2-SCENE-004**：大屏恒星含 `started`；聚合至少区分激活/公开恒星/已准入/已启动/点亮/星光总量；后台匿名漏斗不以在线数作分母。
- **REQ-V2-SCENE-005**：`ADVANCE`/`COMPLETE` 的就绪警告合并进一次确认，可明确 override 并审计；权限/旧 revision/非法 tuple 不可 override。
- **REQ-V2-SCENE-006**：抽奖结果持久化并随快照、刷新和重连恢复；同一轮同一身份不得重复中奖。

### 3.4 大屏、OBS 与公共互动（PROTOCOL_V2 §5.2）

- **REQ-V2-SCREEN-001**：大屏技术上最多 300 颗真实恒星，正式视觉以 220 人为满场参考；保持单 Canvas 与稳定槽位，真实星位置不得因当前人数变化而缩放或重排。已公开抵达的恒星在正常可见的 `ASSEMBLY` 从出现首帧起即以逆时针恒星物质流持续运行，且与较慢盘面密度波保持可见相对运动。集结稳态及其离场不得显示当前星数、目标数、`x / 220`、`x / 300`、满员率或到场进度，也不显示编号/星号/排行/头像/可点击节点。
- **REQ-V2-SCREEN-002**：`ASSEMBLY` 使用本地无点源暗星云与程序化尘埃密度纹理。公共真实星严格一人一星，以盘面离散星为主体的低倾角恒星盘呈现并逐颗保留服务端 `displayColor`；中央核球、短棒与两组宽而弱的密度波只作低权重层次，不得读作发光 S 形旋臂、粗线或霓虹轨道。D-055 允许 420 颗固定、确定性、更小更暗的中性色装饰底星及连续未解析盘面光，让 0 人首帧也有可见逆时针流动并增强场馆结构；它们无公开星号、身份、事件或业务语义，不得进入任何人数、快照或聚合。盘面光和底星可按当前真实星数量做最大有界、连续平滑的环境曝光补偿，220 人及以上回到基准，不得生成额外参与者外观；正常模式只允许环境层以多周期低振幅曝光和极小尺度变化形成非同步呼吸，真实星不得同相脉冲，reduced-motion、页面隐藏和静态恢复不保留呼吸。每个页面会话只在实时首次公开一颗此前不存在的真实星时播放一次画外流星，末段沿目标真实星位的逆时针轨道切线被盘面捕获并自然沉入稳定星流；已有星更新、快照、刷新、重连、隐藏恢复和 reduced-motion 不补播。真实星继续使用近白亚像素核心、极薄保色光晕、重尾尺寸/亮度与极少数短衍射芒，禁止统一漂白、硬边彩珠、同半径呼吸或奶油光球。D-048 要求正常等待星流使用稳定 30Hz；到场流星及正常可见的 `ASSEMBLY → PROGRAM_SUPPORT` 高速段以垂直同步 60Hz 为目标，不忙等。WebGL2 与最长边 1280px 的有界后处理缓冲须在首帧显示前预热，静态星云须一次性缓存；页面仍只有一个可见 Canvas。WebGL2 不可用或上下文丢失时回退 Canvas2D，reduced-motion 直接到透明静态终态。`PROGRAM_SUPPORT` 稳态为 OBS 完全透明源，默认不显示网页节目板、背景、标题、星点、礼物或常驻弹幕面板；网页不碰媒体。预渲染视频仅为尚未启用的备选，接入前必须另行通过素材权利、透明/遮罩编码、预载、浏览器/OBS 解码和恢复验收。
- **REQ-V2-SCREEN-002A**：`/welcome` 与 `/screen` 共用午夜蓝 `#01030a`、深空蓝 `#07101f`、信号蓝 `#427eee`、柔蓝 `#7eb0ff`、克制青 `#4adbe9`、星白 `#eef4ff`、暖白 `#ffd79a` 的 Orbital Signal 色谱；大屏只以低权重使用，禁止高饱和霓虹化，参与者逐星 `displayColor` 不得被覆盖。D-049 的黑洞尾段与出口白光具体实现已被 D-051 替换，不再作为当前验收对象。
- **REQ-V2-SCREEN-002B**：D-051 的正常可见集结→节目镜头保持约 8.4 秒并继承触发瞬间的星流位置和速度：约 0.0～1.4 秒由当前星海连续出现中心引力预兆；约 0.8～4.8 秒让真实星与装饰底星保留角动量、以不同曲率和到达时序螺旋坍缩；约 4.0～5.6 秒压缩为极小高温核心；约 5.2～7.1 秒只形成一次非对称超新星；约 6.5～8.4 秒让冲击折射、丝状喷流、电影曝光与整幅 Canvas 透明化连续重叠，从白场自然显露外部 OBS 第一个节目。真实星在深度压缩前保留各自 `displayColor`，进入高温核心后才向星白收敛。稀疏人数只能以无身份尘埃/等离子体维持爆发尺度，不得添加假参与者或缩小爆发。禁止同速直线吸附、规整圆环、烟花粒子球、彩虹冲击波、霓虹描边、现成镜头光斑、重复闪白、纯白矩形硬盖、阶段停顿或末帧清空。服务端切场即时生效；动画不推进或延迟业务状态。
- **REQ-V2-SCREEN-003**：弹幕 ≤40 字符、单人 3 条/10s、全场 12 条/s，规则拒绝敏感词/链接/联系方式/暂停/屏蔽；Screen 只把实时新的合规正文从右向左短时飘屏，节点数量有界且离场销毁，不从快照、刷新、重连或高优先级覆层期间补播；Screen 不含发送者，手机以 `publicStarId` 展示逐条半透明上移气泡并可浏览，Admin 同时取得 `publicStarId` 和仅用于审核的匿名 `sourceId`。
- **REQ-V2-SCREEN-004**：后台可暂停、带原因撤下、屏蔽来源、紧急清屏；`COMPLETED` 后只允许减少公开内容。
- **REQ-V2-SCREEN-005**：普通礼物只更新账本、节目热度、当前节目礼物数量和本人档案；20 动力星舰在实时成功事件中让每个在线手机与公屏各播放一次长拖尾掠过银河的有界动画，事件不含赠送者，刷新/重连/恢复/快照不补播，高优先级覆层立即清理视觉但不撤销事实。
- **REQ-V2-SCREEN-006**：视觉优先级 `COMPLETED > FINALE_PREVIEW > RAFFLE > 故障提示 > 当前场景 > 实时互动`；抽奖期间不排队补播，减少动态时直接显示结果。
- **REQ-V2-SCREEN-007**：终章只在实时观察到权威完成时播 5～6 秒；快照已完成/刷新/重连/reduced-motion 直接静态终态。
- **REQ-V2-SCREEN-008**：`/screen` 可按需加载 GSAP core/timeline，只做少量覆层与有界实时弹幕节点的 `transform`/`opacity`；300 星点必须由单 Canvas 绘制；不新增插件、逐星 GSAP 或重型 3D，手机端不迁移 GSAP。隐藏/中断/刷新/重连/reduced-motion 直接进入透明静态终态，减少动态不执行弹幕位移。

### 3.5 手机信息架构与动效（PROTOCOL_V2 §6/§7，视觉细节见 VISUAL_GUIDE）

- **REQ-V2-MOBILE-001**：沉浸式流动星系 + 唯一底部液态玻璃操作坞；无六阶段线、"实时同步"、裸 `RUNNING`、玻璃卡片墙；节目聊天只以独立半透明气泡上移，不使用遮住银河的大面积背板；不支持模糊时按单个表面做可读降级。
- **REQ-V2-MOBILE-005**：本人档案从服务端权威快照展示当前 epoch 按节目/礼物汇总的数量与动力，以及本人弹幕正文、星色、时间和撤下状态；当前节目页与礼物面板展示本节目各礼物已送数量，客户端不得从本地操作历史推算。
- **REQ-V2-MOBILE-002**：成功提示约 3.5 秒自动收回并由持久权威状态接替；错误/离线/暂停/完成提示持续可见。
- **REQ-V2-MOBILE-003**：D-050 要求 `/welcome` 的寻星、选色、入轨和主场景共用 Orbital Signal 午夜蓝/深空蓝底色。现有轻量本地星云只作为低饱和亮度结构与尘埃细节，低透明叠加信号蓝/克制青气氛；不得继续以偏紫高饱和雾团为主色，不得复用约 1.85 MB 的大屏尘埃图或加载运行时网络素材。本人星与其他真实星继续逐颗保留 `displayColor`，文字、控件、焦点、短视口和软键盘对比度不得因背景重置下降。
- **REQ-V2-MOTION-001**：正常首次核验成功且服务端返回新建身份 + `NEEDS_COLOR` 后，先等稳定 `visible`，再播约 5.4 秒寻星（D-030 金标参考）；选色不计时；权威锁色后约 1.0 秒闪烁；权威准入后约 4.2 秒拉远入轨。**D-028/D-029 的 2.8s/1.2s/3.2s 是历史自动证据数字，不再作为设计目标。**
- **REQ-V2-MOTION-002**：正常首次不提供跳过按钮；开播前隐藏则等待；开播后后台中断立即静态且不重播；reduced-motion/返回/刷新/失败/超时直接静态可操作终态；不再使用怀士堂或建筑轮廓。
- **REQ-V2-MOTION-003**：叙事镜头用 Vue/CSS 低成本 `transform`/`opacity`，Canvas 只承担权威星系；动效不得承载或延迟业务提交。
- **REQ-V2-MOTION-004**：D-027～D-032 的自动与真机结果是 D-037 之前的历史基线；D-037 取消寄语页后，锁色确认直接衔接约 4.2 秒入轨，当前路径须重新执行自动门，并由项目负责人在至少一台代表实际上线访问方式的实体智能手机上完成人工复验；品牌、型号与操作系统不作阻塞限制，桌面移动端模拟不代签真机。

### 3.6 三端视觉系统与上线文案（D-053，视觉细节见 VISUAL_GUIDE）

- **REQ-V2-UI-001**：`/welcome`、`/screen` 与 `/admin` 共用 Orbital Signal 基础色谱、语义表面、文字层级、边界、焦点、连接、成功、警告、危险和禁用状态令牌；参与者 `displayColor` 与页面专属 Canvas/WebGL 美术参数不得被共享主题覆盖。
- **REQ-V2-UI-002**：三端共用本机 CJK、display、signal 与 data 字体角色，不下载网络字体；现行共享圆角为 `2/4/8/12px`，手机操作坞与控件继续使用非对称微角。手机关键状态、标签与操作说明不得低于 12px；禁用控件必须有独立文字、边框和表面状态，不能只降低整体透明度。
- **REQ-V2-UI-003**：`/admin` 使用克制的深空运营控制台，首屏优先显示连接、运行 tuple 和主要控制；不得加入持续星流、粒子、强模糊或竞争操作判断的氛围动画。连接、权限、危险操作、抽奖和内容处置必须同时以文字和状态色表达；`NONE` 等内部枚举不得直接展示给操作者。
- **REQ-V2-UI-004**：开发导航只在开发模式出现；站点版本、数据范围和部署范围可由显式 Vite 构建变量覆盖。未配置变量或未完成正式上线门时必须继续显示安全的 Demo/合成数据/非公网默认文案，不得因生产构建自动伪称正式上线。

### 3.7 数值与重置（PROTOCOL_V2 §8/§10）

- **REQ-V2-VALUE-001**：星光 = 激活 20、首次启动星星 40、首次送礼 10、首次合规弹幕 10、协同点亮 20；上限 100。寄语/胶囊不再产生奖励。
- **REQ-V2-VALUE-002**：奖励规则服务端版本化（`rewardRuleVersion`），每行账本保存实际增量与版本。D-037 的 `0013` 合成库迁移删除旧胶囊奖励、把既有 `STAR_STARTED` 调整为 40 并校正累计值；此后不得在运行中热切换规则。
- **REQ-V2-VALUE-003**：后台无扣减/补发入口；安全处置仅删除、屏蔽、清屏或撤下。
- **REQ-V2-VALUE-004**：激活初始动力为 100；活动礼物固定 1/5/10/20，每次完整扣费，不存在免费档或节目首礼减免。首次有效送礼的 10 星光奖励独立计算。schema 16 旧减免只保留审计，历史交易按原实际面值保留。
- **REQ-V2-RESET-001**：`RESET_DEMO` 需 `DEMO_ADMIN`/`ALL` + 确认 + 合成数据硬门；永不在启动时自动执行。
- **REQ-V2-RESET-002**：普通重置原子进入新 epoch 的 `REHEARSAL+READY+null`；清参与者可变状态，保留固定合成身份、令牌映射、`publicStarId` 与 slot 目录。
- **REQ-V2-RESET-003**：v1→v2 只允许一次性合成数据切换（v2 基础迁移）；失败整体恢复 v1 备份，不混跑。
- **REQ-V2-RESET-004**：已激活的 schema 12 合成库升级至当前 schema 17 时必须停服、显式确认、先写入不覆盖的独立 SQLite 备份，并在单事务内依次应用 `0013`～`0017` 和完整复验；失败回滚并保留备份。启动器不得自动升级或退回 v1 初始化。
- **REQ-V2-RESET-005**：`RESET_DEMO` 与合成升级门只接受 `SYNTHETIC_DEMO`；任何 `PROTECTED` 正式名单库必须硬拒绝，且正式启动器缺库或凭据时不得自动造数。
- **REQ-V2-RESET-006**：精确 schema 16 的 SYNTHETIC_DEMO/PROTECTED 可经 D-071 专用入口保留升级至 17；必须停服、显式配置与确认、备份优先、事务迁移和完整复验。跨 schema 回退必须恢复备份及旧代码，不能只切 release。

- **REQ-V2-CATALOG-001（D-061）**：本场 21 项目录支持开演前预览、确认应用、改名与调序；稳定 ID 和独立目录不得重建或改写名单种子。既有礼物归属不可通过目录替换更改。
- **REQ-V2-CATALOG-002（D-061）**：串场/延期占位应成为当前项；服务端关闭送礼、不扣动力/发奖励或增加上一节目热度，弹幕沿用既有规则。第 20 项竞拍不实现交易或线上玩法。
- **REQ-V2-CATALOG-003（D-061）**：精确 schema 14 的 SYNTHETIC_DEMO/PROTECTED 可经保留数据的 14→15 专用维护入口升级；须停服、显式配置与确认、备份优先、全事务验证/回滚，不更改身份/epoch/权益。该入口与只允许合成的重置及旧 schema-12 转换门分开。

### 3.8 正式部署、持久化与协作排练（D-056）

- **REQ-DEPLOY-001**：正式基线为单台支持 systemd 的 Linux 主机、单个 Node/Fastify 后端实例和 Caddy HTTPS/WSS 入口；后端只监听 loopback，只信任 loopback 代理，SQLite 不得由多实例、网络共享盘或两个 release 同时写入。
- **REQ-DEPLOY-002**：正式数据库、运行凭据和 NFC 映射必须在 Git/release 目录之外的持久化私有目录中，目录仅服务账号可访问（POSIX 建议 `0700`），文件仅服务账号可读写（建议 `0600`）。代码更新、release 切换或重新克隆不得覆盖该目录。
- **REQ-DEPLOY-003**：正式生产启动必须声明唯一无路径 HTTPS origin，强制 `Secure + HttpOnly + SameSite=Lax` 会话 Cookie，并在监听前验证构建、三项私密资产、`V2_ACTIVE + PROTECTED` 分类、当前 schema/迁移、人数/凭据一致性、仓库外路径和权限；任何失败均停止，不得自动生成、迁移、降级或放宽安全开关。
- **REQ-DEPLOY-004**：正式备份必须使用 SQLite 一致性 backup 机制生成独立快照，和同代运行凭据/NFC 映射组成全新仓库外 bundle，记录逐文件 SHA-256/长度并再次验证数据库。恢复必须先验 checksum 与协议，再物化到全新仓库外目录；禁止覆盖活动目录、直接热复制主库或手工拼接 WAL sidecar。
- **REQ-DEPLOY-005**：备份包仍是明文受保护数据，必须最小权限、使用获批准的加密传输与离机保存、记录保管/恢复/删除责任，并随 T+30 天数据治理范围清理；不得进入 GitHub、Issue、PR、聊天、普通协作盘或测试报告。
- **REQ-DEPLOY-006**：GitHub 只共享代码、迁移、模板与合成证据。协作者使用 `pnpm dev:rehearsal` 创建隔离固定 300 人 `SYNTHETIC_DEMO` 技术目录，界面明确标示合成排练且视觉仍以 220 人为正式参考；该入口不得读取/复制 `.private` 或伪称正式上线。
- **REQ-DEPLOY-007**：systemd 必须以专用非登录账号、`UMask=0077`、只读代码/仅状态目录可写和失败重启守护；上线前另行验证 Caddy 配置、证书、Host/Origin 拒绝、3000 端口不外露、服务重启恢复、正式 NFC、实体手机、OBS/大屏及数据责任。仓库 smoke 不代签实际服务器。

## 4. 待确认汇总

1. 正式到场规模、节目单、场地、时间与舞台 Cue。
2. 访客模式、同名/补卡现场处置与人工核验责任人。
3. 抽奖奖项、每轮人数、领奖/核销（如未来需要）、记录保留期，以及录像/直播提示。
4. 实际 Linux 主机、公网 DNS/HTTPS 域名、防火墙、加密私密传输、备份保管与数据负责人；仓库内单机部署方案已经确定。
5. 实体 NFC 写入、二维码、短网址、邀请函印刷与补卡流程。
6. 学院 Logo/VI 授权、审批、预算与最终验收人。
7. AI 内容审核供应商、隐私、成本与降级方案（Demo v0 不接入）。
