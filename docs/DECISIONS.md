# 决策记录

用于记录已经由项目负责人确认、会影响实现或验收的决定。聊天中的建议只有写入本文件后才视为开发基线；被新决策替代的历史项保留记录，但不再作为当前实现依据。

**怎么读**：表按时间倒序排列，编号越大越新、位置越靠上；"已确认"且未被替代的条目才是现行规则，被替代条目会注明替代它的决策编号。文档导航见 [`README.md`](./README.md)。

| 编号 | 状态 | 决策 | 日期 | 负责人 |
|---|---|---|---|---|
| D-100 | 已发布 | 负责人明确授权将目前全部已完成更新上传服务器与 GitHub。提交并推送现有功能分支；隔离暂存、备份和演练后将 D-096～D-099 前后端发布至既有晚会路径，schema 19→21，保留当前测试轮次、节目目录及历史数据，不影响其他项目。见 [D-100](./D100_SERVER_AND_GITHUB_RELEASE.md)。 | 2026-09-13 | 项目负责人 |
| D-099 | 本地完成，未发布 | 负责人要求继续雕琢全系统 UI 并确保完整流程可跑。三端逐页审查排版、弹层、输入与状态反馈，修复实际流程问题；沿用 D-098 学院银蓝舞台与动效规则，保留未提交工作、节目编排、原始素材和现有数据。使用独立合成场次执行全流程、恢复及跨浏览器验证，不把局部测试或旧验收等同本轮全流程完成。见 [D-099](./D099_UI_AND_FULL_FLOW.md)。 | 2026-09-13 | 项目负责人 |
| D-098 | 本地完成，未发布 | 按负责人要求精修手机节目开场超新星及大屏舞台背景，加入既有学院标志；保持 D-097 的一次性 2.4 秒转场、跳过、静态与后台权威状态。完成16份节目媒体审查，建议 OBS 本地播放与系统透明互动层配合；待确认的节目对应不擅自绑定，不修改现场 OBS 配置。见 [D-098](./D098_STAGE_CRAFT_AND_MEDIA.md)。 | 2026-09-13 | 项目负责人 |
| D-097 | 本地完成，未发布 | 节目颁奖按节目有效动力值前三名自动生成，主控可调整各节目动力值，保留原始礼物及调整记录；影响前三名的并列须先调整再揭晓。收紧互动与节目间距；节目、主持与颁奖统一深黑银蓝舞台视觉，手机在实时开场执行一次可跳过的超新星转场并落入节目背景。覆盖 D-096 的手填节目奖与黑金舞台表现，校园奖仍按确认名单揭晓。见 [D-097](./D097_PROGRAM_RANKING_AND_STAGE.md)。 | 2026-09-12 | 项目负责人 |
| D-096 | 本地完成，未发布 | 新增节目颁奖与校园图鉴三类奖项、负责人讲话、光年之外禁礼物，三端统一目录与主持舞台背景。互动、颁奖与讲话采用无编号的独立分隔行，覆盖 D-089 的字母展示约定；校园图鉴在手机节目单只显示标题。名单待 9 月 16 日截止后录入确认；控制台揭晓与翻页，草稿不公开。见 [D-096](./D096_AWARDS_AND_STAGE.md)。 | 2026-09-12 | 项目负责人 |
| D-095 | 已发布 | 负责人授权服务器发布、保留其他项目，并授权本地加密保存 SSH 凭据。新版前后端与 schema 19 已上线，历史表、场次和旧目录保留；50 个旧测试身份已核验交付。见 [D-095](./D095_SERVER_RELEASE.md)。 | 2026-09-11 | 项目负责人 |
| D-094 | 本地实现并验证，未发布 | 大屏礼物卡只显示数量，不显示动力或礼物值；统一圆角，收到礼物按对应颜色短暂亮起；手机弹层进一步提高透明度。覆盖 D-093 对应表现，账目与星舰队列保持。构建与 13 项相关测试通过，浏览器验证四档色彩、数量、透明度和减少动态；截图见 output/playwright/d094-polish。 | 2026-09-11 | 项目负责人 |
| D-093 | 本地实现并验证，未发布 | 弹出面板改为半透明玻璃；双端星舰串行播放，结束后间隔 2 秒；大屏独立礼物卡片显示数量，收到礼物弹跳，星舰另保留礼物值。效果增强保持减少动态和及时操作。覆盖 D-092 对应表现，不改账目。见 [D-093](./D093_GLASS_GIFT_RHYTHM.md)。 | 2026-09-11 | 项目负责人 |
| D-092 | 本地实现并验证，未发布 | 负责人要求大屏开场后切换舞台背景、暗色星舰且每节目最多两次、取消星光新增积分与入口、抢答三秒服务端倒计时、紧凑互动及弹幕、礼物选择数量后发送、八色与精简确认。覆盖 D-090 和 D-089 对应细节；保留历史账目。见 [D-092](./D092_INTERFACE_REFINEMENT.md)。 | 2026-09-11 | 项目负责人 |
| D-090 | 本地实现 / 已验证，未发布 | 负责人否决原星舰建模并要求重新设计贴图与动画：以原创银白金属双舷星舰材质替代平面舰体，独立呈现双引擎、近景展示与加速跃迁；覆盖 D-089 星舰具体美术。沿用手机 5.8 秒、公屏 7.2 秒、减少动态静态及既有礼物账目和数量，不增加运行时依赖。构建、21 项相关单元测试、3 项 Chromium 流程与双端视觉检查通过。见 D090_STARSHIP_REDESIGN.md。 | 2026-09-11 | 项目负责人明确执行 |
| D-089 | 本地实现 / 验证中，未发布 | 负责人提供新节目单并要求一次性完成现场精化：19 个正式节目独立编号，互动环节使用 A/B/C 且不计入节目；A/C 建立服务器抢答，B 建立上台观众抽取与投票；礼物支持 1/5/10/20 与数量选择，个人星色弹幕和高级弹幕明确确认；大屏增加前三档礼物计数与高级星舰；谢幕汇总节目礼物值、全部弹幕及礼物总量。schema 19 保留既有活动事实。见 D089_LIVE_INTERACTION_ACCEPTANCE.md。 | 2026-09-11 | 项目负责人明确执行 |
| D-088 | 本地完成 / 验收通过，未发布 | 负责人同意开始修改 D-087 的优先项：表演者按稳定节目 ID 纳入后台目录并同步手机、报幕、片尾；片尾沿用 D-086 银河，字幕只播一轮后定格，静态完整名单可手动翻页。schema 18 为保留记录的 metadata 维护，不重置场次；抽奖业务另行细化。见 D088_CREDITS_ACCEPTANCE.md。 | 2026-09-10 | 项目负责人明确执行 |
| D-087 | 已部署 / 流程审查完成 | 负责人在认可 D-086 后要求更新服务器并审查整个流程。只发布既定前端，保留后端、数据库、现场状态和其他项目；修正过时验收预期，整理主控操作单及片尾/抽奖/目录后续问题。新功能和业务规则仅列建议，本轮不实施。发布、验证及保留的失败记录见 D087_DEPLOYMENT_FLOW_REVIEW.md。 | 2026-09-09 | 项目负责人明确授权 |
| D-086 | 已确认 / 已随 D-087 部署 | 负责人认可 D-085 并要求四周增加不明显的尘埃涌动。通过外围低强度遮光薄尘的缓慢流动和散聚补充氛围；既有配色、背景星位、中央银河及业务保持。复用两端原时钟，Canvas 使用有界小遮罩，减少动态仍静止。替代 D-085 正常模式背景静止约束，其他层次保留。负责人审看预览后明确“这版可以了”，作为后续银河及背景调整的视觉基线；D-087 已发布服务器，现场设备验收仍另计。见 D086_SUBTLE_DUST_MOTION.md。 | 2026-09-09 | 项目负责人明确认可 |
| D-085 | 本地实现 / 审片，未发布服务器 | 负责人要求四周再增加一点有层次的内容。在 D-084 已收淡背景上增加不均匀细星尘、小片远景星团和更远的薄雾，并让近处暗尘遮挡远光。保持现有气体层和白边强度，中央动态银河及参与者星体仍为主体。装饰均为缓存背景材质，无身份、标签、实时数量语义或新动画循环。见 D085_BACKGROUND_DEPTH.md。 | 2026-09-09 | 项目负责人要求完善 |
| D-084 | 本地实现 / 审片，未发布服务器 | 负责人要求背景颜色更深、加入少量白色轮廓、赋予宇宙实物形态，并在实施中补充四周采用类似处理。将外围表现为气体云、暗尘埃带和局部银白受光边缘，中央留空；横竖屏分别生成有界缓存纹理，避免侧边被裁掉或拉长。随后负责人指出过深、喧宾夺主，最终降低背景气体浓度与白边对比。银河运动、星体、人数和业务保持。见 D084_SURROUNDING_DUST_CLOUDS.md。 | 2026-09-09 | 项目负责人明确调整 |
| D-083 | 本地实现 / 审片，未发布服务器 | 负责人反馈 D-082 纯色背景太突兀，要求艺术化并浅化。将外围背景改为淡彩薄雾，以域扭曲和多尺度缓存纹理打散规则色块，增加透黑间隙并降低色彩浓度。保持 D-082 背景与动态主星云分层；银河配色、运动、星体及业务不变。见 D083_SOFT_BACKGROUND_ART.md。 | 2026-09-09 | 项目负责人明确调整 |
| D-082 | 本地实现 / 审片，未发布服务器 | 负责人澄清原要求针对背景，未要求将流动星云变紫。纠正助手在 D-081 中将背景色贴合银河盘面的呈现；色彩移至外围深空，远景使用柔和低频纹理，动态主体继续保持银灰、暖白和散聚。覆盖 D-080 / D-081 的叠层美术，黑色主题与业务保持。见 D082_BACKGROUND_SEPARATION.md。 | 2026-09-09 | 项目负责人明确纠正 |
| D-081 | 呈现被负责人否定，D-082 纠正 | 负责人反馈 D-080 仍太浅，将局部紫色、冷青和暖金增强到清楚可见，覆盖 D-080 的保守强度。黑色基调、色彩分布、主银河、星体和业务保持；仅校准三个共享材质颜色。验证见 D081_VISIBLE_NEBULA_COLOR.md。 | 2026-09-09 | 项目负责人明确调整 |
| D-080 | 本地实现 / 审片，未发布服务器 | 按负责人批注，稍增强 D-079 局部暗紫、冷青和暖金的可见度，保留黑色主调；仅调整共享背景材质三处颜色强度，分布、星体、云层散聚与业务保持。本条细化 D-079 的局部点缀强度，不替代其黑色基调。验证见 D080_ACCENT_COLOR_ACCEPTANCE.md。 | 2026-09-09 | 项目负责人明确微调 |
| D-079 | 本地实现 / 审片，未发布服务器 | 负责人指出 D-078 整体过蓝，明确以黑色深空为主体，仅局部点缀深空颜色。将背景底色收回中性炭黑，远景云气降为灰银，暗紫、冷青与暖金仅在银河附近局部出现；主星云适度去蓝，真实星色、手机尺度与散聚动效保持。覆盖 D-078 的全局墨蓝配色，不改变业务、依赖或发布状态。实现与验证见 D079_BLACK_SPACE_ACCEPTANCE.md。 | 2026-09-08 | 项目负责人明确调整 |
| D-078 | 本地实现 / 审片，未发布服务器 | 负责人要求进一步精修背景。在 D-077 亮度、倾斜银河、已选星色、手机尺度与云层聚散基础上，补充低对比远景云气、细碎星尘和银蓝/暖金光色层次。两端共用程序材质，维持星体优先级、静态模式与 OBS 透明交接；不改变业务或增加依赖。本轮仅本地实现与审片，范围与验证见 D078_REFINED_SKY_ACCEPTANCE.md。 | 2026-09-08 | 项目负责人提出调整 |
| D-077 | 本地实现 / 审片，未发布服务器 | 在 D-076 已认可构图上精修：大屏真实星保留并清楚呈现各自已选星色，手机轨道个人星缩小，星云沿原旋臂流动时局部消散、重新凝聚，抬亮两端背景暗部。保留星位、真实人数、手机静态模式、OBS 透明交接和全部业务语义；不增加运行时依赖。本轮限本地修改与审片，尚未发布服务器。实现与验证见 D077_CLOUD_AND_STAR_ACCEPTANCE.md。 | 2026-09-08 | 项目负责人提出并确认调整 |
| D-076 | 本地实现 / 审片，未发布服务器 | 负责人回看 D-071 / D-062 原版后，确认以倾斜盘面、留白和聚集感为基础精修：先确定背景，再增加有厚度的流动星云，并按同一流向布置星体；统一手机与大屏光色，改善原版中心模糊。采用共享稳定星位、世界空间旋臂与时钟，两端单独取景；原生 WebGL2 与 Canvas2D 回退，不增加依赖。提供两端完整构图、分层查看和 30 秒动态审片，保留人数、业务、字体、12 秒开场与现行星舰时长。替代本地 D-075 图片式星河，既有线上版本本轮不变。实现与验证见 D076_FLOWING_GALAXY_ACCEPTANCE.md。 | 2026-09-08 | 项目负责人确认执行 |
| D-075 | 既有发布；本地美术由 D-076 替代 | 负责人否决 D-073 / D-074 构图并明确“ok开始执行”：星河以延伸出画面的巨大斜向光带、非规则暗尘带与细密星团建立空间尺度，采用石墨蓝、银白和少量暖金，取消居中完整盘面、规则 S 形细丝及整面蓝色提亮。先检查手机和大屏完整关键帧，再接入有界缓慢运镜和前后景视差；手机保持节目标题与表演者、透明聊天和个人星体，公屏保持真实入场人数。允许生成原创环境素材并与代码星体按相同光向融合。制作中负责人进一步要求适合晚会的柔和背景：中央较亮、四周暗，减少硬云脊与碎亮点，让实时星体成为主角；最终采用柔光修订，强纹理首稿留档。覆盖 D-073 / D-074 具体美术，不改变已认可字体、身份、账本、人数语义、12 秒开场、星舰时长及 OBS 透明/减少动态/隐藏恢复契约；通过本地验证后更新同一内测路径。 | 2026-09-07 | 项目负责人 |
| D-074 | 美术已由 D-075 替代（历史验证留档） | 负责人认为 D-073 背景虚假、与星体不融合且过黑，要求尝试艺术化。将手机照片式主盘面替换为代码绘制的极光、流光细丝与星尘，背景和星点共享投影与光色；大小屏抬高深靛蓝暗部，增强环境色层次并保留文字区域对比。D-073 旧图留档，人数含义与真实星位保持；不改变活动业务、字体、开场/星舰时长及 OBS 透明叠加、静态/隐藏/恢复规则。 | 2026-09-07 | 项目负责人 |
| D-073 | 已部署（33 项、Chromium 6 项通过；真机另计） | 负责人要求大屏真实星更明显，入场时显示人数，手机银河更精致并重新评估底图。增强真实恒星亮核、色彩光晕和光学星芒，与装饰小星区分；集结页显示服务器 aggregate.admittedCount（已入场人数），覆盖 D-055 等旧“不显示人数”视觉限制，不使用装饰星数或连接数。手机退役旧紫蓝云框主背景，采用新生成原创精细星系素材，作为缓慢运镜层配合独立动态星点；生成素材仅是无身份环境，不承载人数。保留 C 字体、12 秒开场、星舰时长、静态/中断规则和全部 schema 17 业务。 | 2026-09-07 | 项目负责人 |
| D-072 | 已部署（457 项、三浏览器三端闭环和线上只读验证通过；真机另计） | 负责人要求重新审查 D-071 的视觉，不合格处直接重做，并把手机/电脑银河与整体动效提升为更有电影沉浸感的连续镜头，允许合理延长。重做扁平逆向星舰为有体积、正确朝向、远近变化与长曝光尾迹的掠过镜头；星舰手机约 5.8 秒、公屏约 7.2 秒。银河以暖白核心、冷色外盘、不均匀暗尘带和前景视差构成，程序化纹理同源、手机缓存 Canvas、电脑沿用原生 WebGL2，节目底图延续同一星系；OBS media=overlay 仍透明。去除内容页整面模糊并收紧档案布局；修复快照先于礼物消息的同连接漏播且不回退状态；开场交接延长到 12 秒；保留已认可字体和即时操作，减少动态/隐藏/中断仍直接可用。覆盖 D-071 星舰造型/时长及旧银河具体美术，不更改 schema 17、身份、礼物价格、奖励、抽奖结果和审核，不新增运行时依赖。实现、浏览器中间帧、性能与部署证据见 [本轮验收](./D072_CINEMATIC_ACCEPTANCE.md)。 | 2026-09-07 | 项目负责人 |
| D-071 | 已部署（456 项、Chromium 三端流程与线上升级验证通过；真机/OBS 复验另计） | 负责人要求个人档案展示本人给哪些节目送过哪些礼物及数量、发过哪些弹幕；互动页显示当前节目收到的礼物数量；礼物取消免费/首礼减免，档位改为 1/5/10/20。最大档星舰在每个在线手机和公屏以长拖尾掠过银河，礼物公开事件不得暴露赠送者。手机节目聊天改为类似竖屏直播的逐条半透明气泡向上加入，取消遮挡银河的整体不透明背板。实现以 schema 17 保留旧交易、余额、热度、奖励和旧减免审计，但新操作完整扣费且不读取/新增减免；历史 schema 16 事件的 50 面值只保留解析兼容，活动目录严格为新档位。刷新/重连不补播星舰，手机遵守系统减少动态，公屏沿用舞台动效策略。 | 2026-09-07 | 项目负责人要求，Codex 实施并部署 |
| D-070 | 已部署（455 项与浏览器 7 项通过，真机另计） | 负责人确认先处理手机审查问题：修复键盘遮挡、礼物排版与小触摸目标；重整节目聊天、节目单、档案、抽奖状态与个人结束页，统一常用页面和礼物弹层短过渡。保留 C 字体、星海背景和既定首次入场时长；不更改身份、奖励、抽奖结果、节目业务或数据库。覆盖旧手机通用底部面板与每次导航逐字标题的呈现约定。 | 2026-09-07 | 项目负责人 |
| D-069 | 已部署（专项通过，真机另计） | 负责人要求取消手机匿名展示文案与勾选，手机聊天和后台消息同时展示发送者星号与正文。星号由服务器按已登录身份派生，客户端不可指定；沿用现有审核、限流、扣费和撤回规则。覆盖 D-068 及更早匿名弹幕呈现约定，不公开姓名、完整学号或身份内部 ID。协议新增可选 publicStarId 兼容已持久化旧事件，新消息必带，后台既有消息按关联星位查询。 | 2026-09-07 | 项目负责人 |
| D-068 | 已部署（浏览器专项通过，真机另计） | 负责人澄清手机弹幕应为直播间上下滚动消息列表。替代 D-067 的手机横向飘屏和 9 秒移除；当前页面保留最近 100 条匿名实时消息，底部跟随、上翻停留、新消息提示及回到底部。撤回/清屏仍生效，页面隐藏、退出或场景切换清理；不新增服务端历史接口，大屏保持飘屏。 | 2026-09-07 | 项目负责人 |
| D-067 | 已部署（453/453；浏览器与 OBS 范围见记录） | 负责人授权夜间优化大小屏银河、节目表演者、报幕底图与标题淡出、手机实时弹幕及星色解锁、礼物可持续额度、开场音乐与终章。具体规则与替代范围见 [本轮记录](./D067_OVERNIGHT_ACCEPTANCE.md)。 | 2026-09-07 | 项目负责人 |
| D-066 | 已实施（已部署，三浏览器 12/12；OBS 复验待补） | 负责人反馈大屏转场和弹幕缺失，本机浏览器确认 Windows 关闭动画可触发问题，随后要求各状态保持完整动效。专用 v2 `/screen` 默认舞台完整动效，统一作用于银河、转场、弹幕、抽奖与终章，包括排练终章预览；`motion=system` 显式跟随系统，`motion=reduced` 显式静态，`settings=1` 打开仅供主控使用的设置面板。覆盖 D-063 等旧条目中大屏必须无条件跟随系统减少动态的规则；手机和后台继续尊重系统偏好。静态模式的新合规弹幕改为有界、定时移除的静态正文，不能整层消失；仍只在节目应援、运行中、无活动覆层时呈现，不从快照补播。隐藏、断网、刷新、重连、渲染失败和结束保持正确权威状态，不承诺休眠或故障时完整播放，不补播过期转场，不让动画决定奖励、中奖或场景。保留旧稿备份，不变更数据库、中奖规则和节目媒体控制。最终 23 项相关单元、三浏览器 12 项通过；前端补丁已无停服部署，后台进程、epoch 和现场终态保持，详见 TEST_PLAN §3.28。 | 2026-09-06 | 项目负责人要求完整动效，Codex 实施 |
| D-065 | 已实施（本机正式 220 人已更新，专项通过） | 负责人为统一管理要求所有星号采用“姓氏首字母 + 学号后四位”。保留现有六字符形态，字母大写、中间 `-`、四位数字保留前导零，例如 `Z-0123`；正式导入与合成账号使用同一规则，覆盖 D-054 关于正式星号随机生成且不派生身份的约定。中文按姓氏拼音识别，复姓只取首个字母；英文登记名默认取开头字母，新导入可通过 `surnameInitial` 明确校正。重复结果在写入前报行号并停止，不静默加序号、覆盖或随机改号。星号是公开且可关联的展示编号，不作登录凭据；NFC 随机令牌、完整学号 HMAC、身份/星位和正式/合成隔离继续有效。为已有未激活正式名单提供默认只读预览与显式应用/回退入口，保留无凭据改号日志，数据库与私密映射同步校验；已有参与或运行记录时拒绝改号。本机 220 人已于 2026-09-06 按此更新，schema 14 保持原样；59 项相关回归及类型检查通过。服务器合成账号本来已按此生成，本次不重建身份或干扰今晚测试。 | 2026-09-06 | 项目负责人明确新规则，Codex 实施 |
| D-064 | 已实施（公网三端通过，内测待开始） | 负责人明确今晚为服务器内测，提供目标服务器及内测路径，并更正最终域名为 `sysuzgxytj.top/welcomeparty/`，要求不得影响其他项目。按独立目录、独立 3216 loopback 端口、非 root 服务账号和全新合成数据库部署，沿用现有 Nginx/TLS；页面、API、WebSocket 与 Cookie 均按 `/welcomeparty/` 隔离，不改根站和其他域名路由，不升级真实运行库。检查发现该路径已有旧迎新服务，因此先保留旧服务和数据，负责人要求继续并更正域名后，已恢复 `hiwebsun.top` 原路径，仅切换 `sysuzgxytj.top` 的该入口；旧服务与数据保留。代码按精确文件 SHA-256 打包并注明包含未提交改动，不擅自提交或推送。 | 2026-09-06 | 项目负责人要求服务器内测，Codex 已部署并复验 |
| D-063 | 已实施（本机与服务器专项通过，现场另计） | 负责人认可 D-062 银河样片，要求先使用并尽快准备今晚测试。将同一镜头核心接入正式 `/screen`，接收真实公开星位与锁定星色，保留一次到场流星、抽奖中断、协同聚合反馈、隐藏/减少动态/恢复直接静态以及节目透明稳态；相机环境运动改为有界路径，不能按 19 秒样片时钟无限向前穿过星系。不新增业务状态或依赖，不修改私有运行库；本轮只在独立合成环境验证三端流程，提供晚会操作与测试顺序。抽奖美术、电影回忆录片尾和竞拍继续后续细化；本机通过不等于真机/OBS/场馆签核。 | 2026-09-06 | 项目负责人认可样片并要求今晚测试，Codex 已接入并复验 |
| D-062 | 样片已实现（本机专项通过，视觉待评审） | 负责人在 D-061 后要求继续已提出的电影银河连续镜头样片。本轮制作独立、可重播/暂停/逐时刻查看的本地美术预演：以确定性三维盘面、深度视差、连续相机推进及按运动速度形成的短拖影改善平面感；延续群星汇聚、高温压缩、一次非对称超新星及白场透明接管，过渡仍为约 8.4 秒。样片只用固定合成公开星位，220 为视觉参考、300 为技术上限，420 颗无身份底星不计入人数；不读取后端或现有数据库，不引入运行时依赖/网络素材，不修改生产路由、手机字体、节目目录或业务状态。样片可展示单独标明的节目底图以观察透明接管，该底图不进入生产大屏。减少动态、页面隐藏与渲染失败直接静态终态；正式 `/screen` 暂沿用 D-051/D-055，样片通过不等于已接入或已通过 OBS/场馆验收。Chrome 专项 16 项及连续性/初始 GPU 失败 2 项通过，原银河 21 条单测和前端构建通过；1920×1080 无录屏转场短测为 220 星约 54.86 FPS、300 星约 59.04 FPS。可观看的 19 秒镜头、录屏及验收范围见 GALAXY_SHOT_PREVIEW 与 TEST_PLAN §3.25，不沿用历史绿色结论。 | 2026-09-06 | 项目负责人要求下一步，Codex 制作样片 |
| D-061 | 已实现（临时环境自动门通过，运行库应用另计） | 负责人在 D-060 字体落地后要求继续下一步，按 D-059 实施顺序接入本场节目目录。新增独立的 v2 节目目录及维护入口，保留稳定节目 ID；采用导入/编辑预览后明确应用，只允许 READY 且无当前场景时修改目录，使用既有主控权限、epoch/revision、幂等和事务机制。旧名单种子、身份凭据和历史礼物归属不得因节目维护被重建或改名覆盖；schema 15 将现有 v2 节目与礼物外键迁入独立目录，并提供停服、备份优先的既有库升级入口，本轮只操作临时合成环境。21 项按 18 表演、2 游戏串场、1 竞拍占位接入，保留原文标题与时长；串场/延期占位可设为当前项，服务端不接收礼物且不扣动力，弹幕遵循原审核/暂停规则。后台明确区分下一项与下一场景，显示当前/下一项和协同不可回退的提醒；不按节目时长自动切场，不替用户安排未明确的抽奖轮次。竞拍和线上游戏仍不实现；电影银河、抽奖美术、电影片尾另行推进。自动验收覆盖目录持久化/修订冲突/权限/失败回滚/重启、串场防误计热度、三端更新和名单种子独立性，发现并修复旧受保护身份指纹包含可变热度的问题，schema 15 将热度独立与礼物账本核对。完整 G3 退出 0：429 条 Vitest、三浏览器 49 通过/2 既定跳过；最终呈现整理后专项 9/9、本场 21 项目录的 300 人协议负载和恢复通过，证据见 TEST_PLAN §3.24。正式库升级及现场签核另计。 | 2026-09-06 | 项目负责人要求下一步，Codex 实现 |
| D-060 | 已实现（方向已确认，最终视觉待评审） | 负责人选择 D-059 对照稿 C，并允许继续优化具体字体选择。手机采用清晰方正黑体、中等字重标题与常规正文的统一组合，覆盖 D-059 的像素大标题方向；具体选型为 Noto Sans SC 2.004 可变字体的本地 UI 子集，按固定公开界面文字及本场公开节目标题生成并重命名为 Welcome Sans SC，保留 OFL 与来源记录。字体仅用于 `/welcome` 的 v2 页面，标题、正文、按钮与数字使用同族不同字重/字号/字距，未知或子集外字形仍回退系统字体；字体失败、加载或换字形不得阻止入场、操作或推进业务，正常首次连续镜头与 reduced-motion 静态规则保留。产物 150224 bytes、509 码位、400–700 字重；无新增 npm 运行时依赖，不修改全局跨端字体令牌、不导入正式节目目录、不实现竞拍、不访问正式数据。更新两条过时视觉断言后完整 G3 退出 0，421 条单元测试与三浏览器 46 通过/2 既定跳过；实际中文渲染、同源加载与断字库回退、小屏/模拟键盘/网页字号放大及首次连续旅程证据见 TEST_PLAN.md §3.23。方向选择与自动检查不等于负责人已签核最终字体或实体手机效果。 | 2026-09-06 | 项目负责人选 C，Codex 实现 |
| D-059 | 目录接入由 D-061 完成，字体探索由 D-060 收口 | 负责人提供本场《节目单.docx》，确认名称与顺序基本确定；21 个编号项按原文转录为 18 个表演与 3 个串场，开场白/结束语另列，目录与阶段建议见 EVENT_PROGRAM_2026.md。第 20 项竞拍按负责人“暂时不管，后期沟通加上”保留占位，不转换成抽奖、不实现竞价或扣费。D-059 时后台只有已有节目的选择能力，转录稿不等于已导入运行库；当时待补目录维护、串场时应援归属与导入校验，再在排练环境验证。历史字体探索：负责人先选定游戏界面式方正像素标题，替代 D-032/D-053 对手机大标题“不采用像素游戏字体”的限制；首稿使用 Fusion Pixel 简体中文 12px proportional。负责人随后认为首稿一般，并要求评估其余文字；当时补充 A 首版像素、B 像素加统一正文/数字、C 清晰黑体加统一正文/数字三组页面对照，尚未定案；后续 C 的选择与应用实现以 D-060 为准。D-059 当轮只有文档与隔离浏览器 CSS 样稿，未修改应用字体、运行目录或正式数据。银河镜头可独立推进，电影片尾与协同时间点须跟随本场节目编排收口。 | 2026-09-06 | 项目负责人确认方向并反馈，Codex 整理 |
| D-058 | 实现稿（视觉待评审） | 在 D-057 明确缺陷修复后，按负责人“下一步”推进手机视觉收敛：沿用本地字体、低饱和深空、唯一顶部标题和一个微角操作坞，统一内容对齐与文字层级；选色使用服务端同源色温映射生成光谱，保留 Kelvin、键盘和锁色不可更改说明，正常连接静默，异常提示可见。节目应援突出当前节目与输入任务；完成后以本人已确认的入场/启动/首次礼物/首次弹幕/协同事实组成私有纪念页，未参与项不补写或虚构，详细数值仍在档案。保留约 5.4s/1.0s/4.2s 连续镜头、静态恢复、服务端权限与奖励；恒星材质只作轻量可读性调整。未改大屏银河、抽奖美术或电影片尾；此实现稿不等于负责人已认可审美，也不代签真机。 | 2026-09-06 | Codex 实现，负责人视觉评审待补 |
| D-057 | 已确认（明确缺陷修复） | 项目负责人要求先修抽奖、协同反馈和验收记录等明确问题。本轮保留服务端无放回抽奖、三场景、奖励与身份边界：大屏按持久化顺序依次揭晓新中奖结果，未揭晓结果不进入可见历史，连续抽取不打断前一位；刷新、重连、隐藏及 reduced-motion 直接恢复持久结果。协同点亮以公开 aggregate 的完成数/准入数驱动可读进度及集体亮度，实时新增可产生一次有界反馈，不能把 ASSEMBLY 的 started 当成协同完成、猜测单星参与者或自动 COMPLETE。终端验收仅接受显式 P/F/B/S，空白、非法输入、EOF 和中断保留 PENDING；记录绑定 Git HEAD/工作树指纹、执行人、设备、证据和起止时间，缺项不宣告通过，人工签核仍独立。推进/结束将已知就绪警告合并到一次确认，服务端新出现的警告须重新明确确认，revision/权限错误不能绕过；正式手机恢复说明和公共星号文档按 D-054 区分正式与合成目录。本轮不改变银河镜头路线、不重设计手机/抽奖/终章，不新增依赖或操作正式数据 | 2026-09-06 | 项目负责人 |
| D-056 | 已确认（正式单机部署、私有持久化与可共享合成排练） | 项目负责人要求把正式数据库部署和多人协作缺口一并收口后重新上传 GitHub。正式版本采用单台 Linux 主机、单个 Node/Fastify 后端实例、Caddy HTTPS/静态文件/反向代理和服务器代码目录之外的 SQLite 私有持久目录；后端只监听 loopback，只信任 loopback 代理，生产入口必须声明唯一无路径 HTTPS origin，会话 Cookie 强制 `Secure + HttpOnly + SameSite=Lax`。正式启动必须在监听前验证 `V2_ACTIVE + PROTECTED` 数据库、运行凭据、NFC 映射、构建产物、文件边界和最小权限，任何缺失、错配、宽权限、HTTP origin、非 loopback 监听或错误覆盖均安全停止。systemd 以专用非登录账号、`UMask=0077`、只读代码、仅状态目录可写和失败重启守护单实例；当前 SQLite 方案禁止多实例并发、共享网络盘或把数据库放进 release 目录。正式备份使用 SQLite 在线 backup API 生成独立单文件快照，连同运行凭据和 NFC 映射写入全新仓库外目录，逐文件记录 SHA-256/字节数并再次做协议、分类和完整性验证；恢复只可校验后物化到全新目录，再人工切换 `FORMAL_RUNTIME_DIR`，不得覆盖活动目录或临时拼接 WAL。备份包仍含受保护数据，必须最小权限、加密离机保存并按 30 天治理期限清理，绝不进入 GitHub、聊天或普通协作盘。GitHub 只共享代码、迁移、部署模板和固定合成排练：`pnpm dev:rehearsal` 在隔离目录首次创建固定 300 人技术目录并自动完成 v2 初始化，UI/视觉口径仍以 220 人为正式满场参考；它不得访问正式 `.private` 或伪称正式数据。`deploy/Caddyfile`、systemd/env 模板、部署静态检查、正式启动 smoke、备份/恢复测试和服务器手册成为仓库交接基线；实际域名/DNS、服务器凭据、私密数据传输、Caddy 实机校验、systemd 启动、HTTPS/NFC/手机/OBS/场馆验收仍须在目标服务器和现场另行签核。本决定解决 D-004 的仓库内单机部署方案部分，但不把尚未提供的实际主机、域名或上线证据视为已完成 | 2026-09-05 | 项目负责人 |
| D-055 | 已确认（220 人盘状银河、底星呼吸与轨道入场） | 项目负责人确认正式活动名单最多约 220 人且实际到场可能不足，要求不再把 300 人画面当作正式美术满场基准：220 人是正式视觉参考，300 人只保留为协议安全余量、合成 Demo 与压力测试上限，页面继续不显示人数、目标数或满员率。`ASSEMBLY` 从强调恒星棒和清晰旋臂的构图收束为更接近真实银河的低倾角恒星盘：盘面离散星与连续、无身份的未解析星光构成主体，中央核球/短棒和两组密度波只作低权重结构，不得读作发光 S 形旋臂、霓虹轨道或几条粗线。固定装饰底星从 320 调整为 420 颗更小、更暗、确定性的中性微星，仍无公开星号、身份、事件或业务语义，不进入快照、聚合或人数；盘面星雾与底星允许按当前真实星数量进行有界、连续的曝光补偿，使 0～220 人均保持丰满但不伪造参与者，真实星位置不得随人数缩放或重排。正常动态模式只让未解析盘面光以多周期、低振幅、非同步的曝光与极小尺度变化形成高级呼吸感，禁止所有真实星同相放大、闪烁或整片机械脉冲；`prefers-reduced-motion: reduce`、页面隐藏和静态恢复路径不保留呼吸或位移。每名新人实时首次公开时继续只播放一次画外流星，并让末段速度沿其真实星位的逆时针轨道切线被盘面捕获后自然沉入稳定星流；快照、刷新、重连、已有星更新、隐藏恢复和 reduced-motion 不补播。D-051 的约 8.4 秒错峰汇聚、高温核心、一次非对称超新星、人数无关爆发尺度与白场透明节目接管保持不变；实现继续使用现有单可见 Canvas、原生 WebGL2 后处理与 Canvas2D 回退，不新增依赖、媒体或运行时网络资源。本决定替换 D-046/D-047 中强调清晰棒旋臂的集结几何、固定 320 底星和以 300 人作为正式视觉稠密端点的验收口径；一人一颗真实星、逐星 `displayColor`、逆时针星流、逐人单次流星、30Hz/60Hz、节目稳态透明及仅实时弹幕边界继续有效 | 2026-09-04 | 项目负责人 |
| D-054 | 已确认（受保护名单、匿名 NFC 与隔离测试账号）；星号规则由 D-065 覆盖 | 项目负责人确认有权使用 `2026级名单(2).xlsx` 并要求录入、完成三端适配及准备 NFC/测试账号。正式导入只取姓名与 8 位学号，拒绝导入性别、专业班级及其他非必要列；本次源文件 SHA-256 为 `d4332ce10b9cff2cb2d9c3780dc774cdec0708ec21a2e35faf5c8603f5f5f74b`，校验后共 220 条、学号无缺失/重复。正式身份库必须使用全新 `backend/.private/` SQLite 与独立运行凭据，标记 `V2_ACTIVE + PROTECTED`，保存源摘要和导入时间，不得经过或伪装成可丢弃的 Demo 切换；`RESET_DEMO` 对该分类硬拒绝，正式启动器缺库/缺凭据时停止而不代填合成种子。每张 NFC/二维码继续使用同一 URL 模板，但唯一变化项必须是 256 位随机匿名邀请令牌，不得把明文学号、姓名或可推导的学号后四位放入 URL/公开星号；本地私密映射把学号对应到完整 NFC 地址，供批量写卡，公开星号改用与身份字段无关的随机 `A-0000` 形态。人工重新认证直接提交姓名+8 位真实学号；旧合成 Demo 仍兼容 8 位测试学号。正式库只含 220 名真实成员；测试账号不得混入正式库，继续由独立 `SYNTHETIC_DEMO` 库承载并只导出 5 个便捷账号。本轮新增迁移 `0014_protected_roster_runtime.sql`，正式库 schema 14 完整验证通过；原合成 Demo 已备份优先从 schema 12 升至 14 并验证通过，成功备份 SHA-256 为 `5c34e67e2ef173412d113557cee391130a58e627b2c21c940ce74700f447305a`。私密数据库、运行凭据、NFC 映射和测试账号清单均被 Git 忽略，不得提交或在日志/快照/事件中输出；正式 HTTPS 域名、实体写卡、数据告知/保管/删除责任和现场复验仍是独立上线门 | 2026-09-04 | 项目负责人 |
| D-053 | 已确认（三端 Orbital Signal 视觉收口） | 项目负责人确认按当前三端 UI 审计建议依次收口视觉体系：`/welcome`、`/screen` 与 `/admin` 共用 Orbital Signal 基础色谱、语义表面、正文/辅助文字、边框、焦点、连接、成功、警告、危险和禁用状态令牌，但继续保持手机个人触控、大屏电影叙事、后台高密度运营三种不同强度。`/admin` 从旧纸张/油墨 Editorial Signal 外观切换为克制的深空控制台：使用静态午夜蓝画布、分层深空面板、低权重网格/信号线和清晰状态色，不加入持续星流、粒子、强模糊或会竞争操作判断的氛围动画；首屏优先呈现权威连接、运行 tuple 和主要控制，窄屏减少标题与导航占高。共享组件改为消费语义令牌，页面专属星云、Canvas 后处理和参与者 `displayColor` 仍留在各自美术层，不被强行抽象或覆盖。三端统一使用本机 CJK、display、signal 与 data 字体角色，不联网下载字体；现行小圆角 `2/4/8/12px` 与手机非对称微角作为代码权威，旧视觉指南中的 `10/14/18/24px` 表格同步纠正。后台不得直接向操作者暴露 `NONE` 等内部枚举；手机关键状态、标签与操作说明不得低于 12px，并为禁用态提供独立文字、边框和表面，不只降低透明度。开发导航继续只在开发模式出现；`Demo v0`、数据范围与部署范围文案改为显式构建环境可覆盖，未配置或未通过正式上线门时继续安全显示 Demo/合成数据/非公网默认值。此变更只涉及视觉、文案映射和可访问性，不改变协议、权限、命令、持久化、实时事实、大屏动画、OBS 透明节目稳态、隐私或依赖；自动门至少覆盖共享视觉契约、前端构建、三端 v2 E2E、390×844/1366×768/1920×1080 浏览器画面、键盘焦点、禁用/危险状态与 reduced-motion，实体手机、OBS 和场馆仍须另行人工签核 | 2026-09-04 | 项目负责人 |
| D-052 | 已确认（取消 vivo X300 指定机型门） | 项目负责人确认正式上线前的当前与后续手机人工验收不再绑定 vivo X300，也不预设品牌、型号或操作系统；最低签核门改为由项目负责人选择至少一台能够代表实际上线访问方式的实体智能手机，使用其实际浏览器完成当前新鲜邀请、触控与软键盘、跨端色彩和动画观感、页面隐藏/恢复、断线重连、退出/重新认证以及连续运行/温升检查，并记录手机型号、系统版本、浏览器及版本、CSS 视口、DPR 与 `prefers-reduced-motion` 实际值。桌面移动端模拟、Playwright、截图和 AI 观察继续只构成自动证据，不能代替实体手机签核；ADB/CDP 脚手架只对支持该能力的 Android 手机提供可选预检，iOS 或不支持远程调试的手机可直接按人工表验收。额外机型和浏览器兼容测试可作为建议性扩展，不作为当前阻塞门，除非后续决策重新指定目标设备矩阵。D-021/D-036 及旧文档中已经发生的 vivo X300 验收记录继续作为历史事实保留，不被改写，也不能代签 D-037/D-051 当前版本。本决定只替换现行与未来人工手机验收中的指定机型要求，不改变三端局域网、OBS、隐私、动画、降级和正式上线其他门禁 | 2026-09-04 | 项目负责人 |
| D-051 | 已确认（恒星坍缩与超新星节目接管） | 项目负责人否决继续修饰黑洞路线，确认以“所有星星向画面中心汇聚、超新星爆发、白场后显露第一个节目”整体替换 `ASSEMBLY → PROGRAM_SUPPORT` 的黑洞/事件视界/隧道叙事。正常可见镜头继续约 8.4 秒并继承切场瞬间的银河位置和速度：约 0.0～1.4 秒由当前星流连续产生中心引力预兆，约 0.8～4.8 秒让真实星与无业务语义的装饰底星保留角动量、以不同曲率和到达时间螺旋坍缩，约 4.0～5.6 秒压缩成极小高温星核，约 5.2～7.1 秒形成一次非对称超新星爆发，约 6.5～8.4 秒让爆发曝光、冲击前沿和网页 Canvas 透明化连续重叠，从白场自然显露 OBS 下方的第一个节目；这些窗口必须交叠而不是分段停顿。真实星在深度压缩前逐颗保留服务端 `displayColor`，进入高温核心后才向星白收敛。汇聚路径禁止同速直线吸附；爆发禁止规整圆环、烟花粒子球、彩虹冲击波、霓虹描边、现成镜头光斑、重复闪白或纯白矩形硬盖。冲击前沿使用低饱和非轴对称折射、丝状喷流、体积密度和有界电影曝光；稀疏到场时由无身份的尘埃/等离子体提供画面尺度，爆发强度不得按人数缩水，也不得伪造更多参与者或显示人数。实现沿用一个可见 Canvas、DOM 外 WebGL2 有界后处理、Canvas2D 回退、首帧前预热、60Hz 目标高速段、30Hz 等待态及不新增依赖/媒体/运行时网络资源；原黑洞后处理应退役而非与超新星叠加。服务端切场继续即时生效，动画不成为业务门；页面隐藏、中断、刷新、重连和 reduced-motion 直接进入透明静态节目终态。`PROGRAM_SUPPORT` 网页稳态仍完全透明、无节目板/背景/标题/星点/礼物/常驻弹幕面板，仅实时新弹幕短时飘过。本决定替换 D-042～D-049 中关于黑洞、事件视界、引力隧道和出口白光的视觉实现；D-046/D-047 的等待态棒旋银河、真实星材质、零人底星和逐人流星，D-048 的帧率/预热/有界缓冲，以及 D-049/D-050 的 Orbital Signal 色谱与手机背景规则继续有效 | 2026-09-04 | 项目负责人 |
| D-050 | 已确认（手机背景同源重置） | 项目负责人在 D-049 跨端色谱基础上进一步确认：手机背景可以重置，以便与大屏形成统一而非拼接式的深空视觉。`/welcome` 的寻星、选色、入轨与主场景统一以 Orbital Signal 午夜蓝/深空蓝为底，原轻量本地星云只作为低饱和亮度结构和尘埃细节，再叠加低透明信号蓝与克制青气氛，不继续保留偏紫、高饱和或塑料雾团作为主色。不得为统一风格把约 1.85 MB 的大屏尘埃图下发到手机，也不得新增运行时网络素材；手机继续使用现有约 32 KB 本地纹理、生产 Canvas 渲染器和 D-030 的 5.4s/1.0s/4.2s 视觉时长。参与者本人恒星及其他真实星仍逐颗保留服务端 `displayColor`，操作坞、文字、焦点、短视口、软键盘、页面隐藏和 reduced-motion 规则不变。手机根节点须暴露可自动检查的同源色谱与背景系统标记。本决定细化 D-049 在手机端的背景实现，D-049 的大屏柔性边界和其他协议继续有效 | 2026-09-04 | 项目负责人 |
| D-049 | 已确认（柔性边界与三端轨道信号色谱） | 项目负责人认可 D-048 的整体方向，但要求动画边界更丝滑、出现与退场不突兀，并让大屏背景更有科技感且与手机版视觉更协调。`ASSEMBLY → PROGRAM_SUPPORT` 仍保持约 8.4 秒、同一持续增速摄影机与服务端即时切场；不新增镜头段落，而是在现有连续镜头内把隧道退场、空间白光和透明节目源显影扩展为约 0.8～1.3 秒的重叠溶解区，尾段所有光学权重必须以连续曲线降到精确零，禁止约百毫秒骤降、末帧硬清空或硬边横线。白光保留“一道光”的叙事，但采用更薄的近白核心、更宽的低透明体积光页和较低全屏曝光；黑洞前段由当前星海先产生弱曲率再逐步显出事件视界。手机 `/welcome` 与大屏 `/screen` 共用 Orbital Signal 色谱：午夜蓝 `#01030a`、深空蓝 `#07101f`、信号蓝 `#427eee`、柔蓝 `#7eb0ff`、克制青 `#4adbe9`、星白 `#eef4ff`、暖白 `#ffd79a`；大屏暗星云、尘埃、透镜焦散与隧道只以低权重使用该色谱，禁止整屏高饱和霓虹化。每名参与者的服务端 `displayColor` 继续逐星保留，不被共享色谱覆盖。D-048 的 30Hz/60Hz 节拍、GPU 预热、有界缓冲、单可见 Canvas、WebGL2/Canvas2D 回退、无人数进度、节目稳态完全透明且仅实时弹幕、reduced-motion 静态终态及不新增依赖/媒体/业务状态等边界不变。本决定替换 D-048 的尾段光学包络、白光锐度和大屏背景配色细节，其余 D-048 继续有效 | 2026-09-03 | 项目负责人 |
| D-048 | 已确认（60Hz 惯性光流与连续快门） | 项目负责人复核 D-047 后仍认为黑洞隧道镜头僵硬，要求得到真正流畅且具有高级感的连续动画，而不是继续加长时长或叠加特效。检查确认 D-047 的高速转场仍被实现层硬限制为 30 FPS，现场证据仅约 27.6～27.8 FPS；同时星轨使用固定归一化进度回看、WebGL 隧道混入独立墙钟相位，白光仍带明显几何直线，导致抽帧、速度错配和分段换镜头感。正常可见的高速到场流星及 `ASSEMBLY → PROGRAM_SUPPORT` 改为与浏览器垂直同步对齐、以 60Hz 为目标且不忙等的有界渲染节拍；低速等待星流可继续使用稳定 30Hz 以保护长时间运行。摄影机位置改用持续加速、加速度在两端连续归零的惯性坠落曲线，镜头速度必须单调且直接驱动星群快门拖影、引力采样、隧道前向缩放、弯曲深度丝束和出口曝光，禁止另起独立主运动时钟。事件视界、隧道与白光仍是同一约 8.4 秒镜头，但隧道更早以低权重从当前星海中生成，黑体遮蔽随纵深流连续退让；出口保留“一道白光”的语义，但改为先有消失点预辉、再展开柔和的非对称空间光页和低权重 anamorphic flare，禁止硬边横线、突然全屏灰白或末段速度回落。1920×1080 自动性能门应分别验证等待态约 20～35 次有效绘制/秒、高速转场约 50～70 次有效绘制/秒且无长任务回归，节目透明稳态与 reduced-motion 仍停止持续绘制。单可见 Canvas、DOM 外原生 WebGL2 与 Canvas2D 回退、零人底星、逐人单次流星、真实星色、不显示人数、服务端场景即时生效、节目稳态完全透明且仅实时弹幕等边界不变；不新增 npm 运行时依赖、媒体或业务状态。本决定替换 D-047 的高速转场节拍、摄影机速度曲线、星轨快门和出口白光具体形态，其余 D-047 继续有效 | 2026-09-03 | 项目负责人 |
| D-047 | 已确认（零人底星、逐人流星与连续黑洞隧道） | 项目负责人现场复核 D-046 后明确指出：初次进入 `/screen` 时等待星海仍像静止、银河结构在场馆画面中过淡，且进入黑洞隧道的镜头依然僵硬。集结态从页面首帧起增加一层数量固定、确定性生成、低亮中性色且持续差速流动的装饰底星，用于显出棒旋银河的核球、恒星棒和拖曳旋臂；该层不携带星号、身份、事件或业务语义，不计入公开星数，也不能冒充同学入场。每个会话中实时首次收到一名新同学的 `star.node.upserted` 时，只在唯一可见 Canvas 内播放一次从画外沿银河切线掠入并收束到其真实星位的短流星；快照、刷新、重连、页面恢复、已有星更新、页面隐藏和 reduced-motion 均不得补播或重放。场馆可见度允许在保留摄影式点扩散、重尾亮度和暗部层次的前提下提高尘埃结构与真实星核心对比，但仍禁止彩色塑料珠、霓虹轨道和人数比值。`ASSEMBLY → PROGRAM_SUPPORT` 正常可见转场延长至约 8.4 秒，并由一条始终单调前进的摄影机轨迹统一驱动曲率、事件视界、星流、隧道纵深与空间白光前沿；黑洞尺度变化改为有界渐进推近，隧道在穿越前提前以不规则纵深流交叠且不断速，禁止固定辐条、分段重新起步和白光硬切。服务端场景仍即时生效，节目稳态继续完全透明且仅实时弹幕正文飘过；单可见 Canvas、DOM 外 WebGL2 后处理与 Canvas2D 回退、不新增运行时依赖以及 reduced-motion 静态终态等边界不变。本决定替换 D-046 的“不得添加装饰底星”、D-045 的约 7.2 秒包络以及 D-044 的隧道纹理细节，其余一人一颗真实星、逐人 `displayColor`、隐私、协议和节目态规则继续有效 | 2026-09-03 | 项目负责人 |
| D-046 | 已确认（银河系棒旋结构、逆时针星流与光学星点） | 项目负责人指出当前 `/screen` 初入等待态虽有逐帧位移但肉眼仍像静止、旋转方向与旋臂手性不符、整体近似稀疏椭圆环且星点仍有塑料珠感。集结态改为银河系启发的低倾角棒旋结构：真实星分布为中央核球与有厚度的恒星棒、从棒两端延伸的两条拖曳主臂、两条更弱更短的支臂以及少量盘面离散星；仍严格一人一星、最多 300，不添加装饰星或背景伪星。正常动态模式从首批公开星进入画面的第一帧起持续运行逆时针恒星流，恒星物质角速度与较慢的旋臂密度波分离，并叠加有界的逐星差速和径向漂移；旋臂向外缠绕方向必须与逆时针物质流形成拖曳关系，禁止整幅星图与引导层同速锁死成机械唱片。恒星材质改为摄影式点扩散：绝大多数只保留亚像素高亮核心和极薄低透明光晕，核心向近白曝光收敛但光晕继续保留各自服务端 `displayColor` 的冷暖差异；亮度/尺寸继续重尾，只有极少数最亮真实星允许短而克制的光学衍射芒，禁止硬边圆片、彩色实心珠、同半径呼吸和大面积奶油状光球。旋臂引导只可用无星点的低亮尘埃密度纹理作为慢速参照，不得读作霓虹轨道。`prefers-reduced-motion: reduce` 继续使用同一棒旋构图的静态帧；D-045 的约 7.2 秒连续黑洞坠落、首帧位置/速度连续、单可见 Canvas、WebGL2/Canvas2D 回退、节目透明稳态与仅实时弹幕规则不变。本决定替换 D-045 继承自 D-042/D-041/D-040 的集结星系几何、稳态流向和星点材质细节 | 2026-09-03 | 项目负责人 |
| D-045 | 已确认（连续坠落动势与空间光前沿） | 项目负责人验收 D-044 后明确指出整段动画仍然僵硬。问题按镜头连续性修正而不是继续叠加特效：正常可见的 `ASSEMBLY → PROGRAM_SUPPORT` 保持约 7.2 秒，但从曲率预演开始即由同一个单调前进的 `cameraTravel` 驱动星群卷入、背景推镜、事件视界放大和隧道纵深，禁止各阶段分别起步、缓停或在相邻段落之间出现速度归零；事件视界尚未铺满画面时，取样自当前真实星海的隧道必须提前交叠显影并沿单调 `tunnelTravel` 持续向前流动，以空间交叉溶解掩蔽穿越边界，不得把静态隧道仅作透明度淡入。中心预辉、横向白光束和最终曝光仍保持单次无频闪，但白光接管改为从消失点向外扩张的椭圆光前沿，只有光前沿已接近画面边缘时才允许低权重全幅曝光，禁止一开始铺设全屏白色蒙层。总时长、单个可见 Canvas、WebGL2 后处理与 Canvas2D 回退、最多 300 颗逐人 `displayColor` 真实星、服务端即时切场、`PROGRAM_SUPPORT` 完全透明稳态、仅实时新弹幕飘屏以及隐藏/中断/刷新/重连/reduced-motion 直接静态终态等边界不变。本决定替换 D-044 的相位时间包络、推镜曲线、隧道运动和曝光空间形态，其余 D-044/D-043/D-042/D-041 边界继续有效 | 2026-09-03 | 项目负责人 |
| D-044 | 已确认（曲率预演、内部隧道与缓动白光束） | 项目负责人认可 D-043 实时引力透镜方向，但指出黑洞本体突然出现、穿过事件视界后缺少内部空间纵深，以及白光接管过硬。正常可见的 `ASSEMBLY → PROGRAM_SUPPORT` 仍为约 7.2 秒单镜头，但重排为连续因果链：先在黑色事件视界成形前让当帧暗星云和真实星像出现微弱的空间弯曲、远侧复映和方向性焦散，事件视界再从这片曲率中逐步凝结，禁止独立黑球或完整亮环突然贴入；摄像机穿过事件视界后进入短暂的时空内部隧道，以受当前星海颜色驱动的破碎径向光流、螺旋剪切、近远视差和中心消失点表现高速纵深，不使用重复同心环、霓虹管道或独立视频；消失点先出现极短中心预辉，再让一束白光从中心快速但带缓动包络地向两侧伸展，随后转为一次无频闪曝光并渐隐到完全透明的外部 OBS 节目画面，不得全幅白线突现、硬切或重复闪烁。实现继续使用 D-043 的 DOM 外原生 WebGL2 后处理与 Canvas2D 回退，不新增运行时依赖、媒体或可见 Canvas；最多 300 颗真实星、逐星 `displayColor`、首帧位置/速度连续、服务端即时切场、`PROGRAM_SUPPORT` 透明稳态、仅实时新弹幕飘屏、隐藏/中断/刷新/重连/reduced-motion 直接静态终态等边界不变。本决定替换 D-043 的转场时间包络、穿越后镜头和白光形态，其余 D-043/D-042/D-041 边界继续有效 | 2026-09-03 | 项目负责人 |
| D-043 | 已确认（实时引力透镜重制，视频仅作备选） | 项目负责人再次否决 D-042 黑洞、星点与背景的塑料感，要求黑洞不再依赖黑圆叠层或静态发光贴图塑形，而应以具有空间透明感的引力透镜成为镜头主体：正常可见的 `ASSEMBLY → PROGRAM_SUPPORT` 继续从当前流动的双主臂星系无跳位进入约 7.2 秒单镜头，实时折射同一幅暗星云和最多 300 颗逐人保留 `displayColor` 的真实星点，在事件视界外形成非对称像素弯折、远侧星像复映、克制色散和断续焦散；事件视界只负责遮蔽，不得读作贴在画面上的实体黑球，吸积纹理只能作为被透镜共同扭曲的低权重细节。实现允许以一个不进入 DOM 的原生 WebGL2 后处理 Canvas 扭曲现有 2D 场景，再回写唯一可见的 `.v2-galaxy` Canvas；不新增 Three.js 或其他 npm 运行时依赖，WebGL2 不可用、上下文丢失或初始化失败时回退到现有 Canvas2D 路径，reduced-motion 仍直接进入透明静态节目终态。预渲染视频只保留为真实设备/GPU 仍不能达到主观质感或稳定帧率时的第二方案，启用前必须另行确认原创/授权来源、分辨率、透明/遮罩编码、浏览器与 OBS 解码、首帧预载、断线恢复和实时星图交接；当前不以视频替代实时到场星海。`PROGRAM_SUPPORT` 网页稳态、仅实时弹幕飘屏、服务端即时切场、隐私和覆层优先级不变。本决定替换 D-042 的黑洞渲染技术与“仅离屏 2D 纹理”限制，其他 D-042/D-041 边界继续有效；本项目的当前用户指令和本决策高于旧动效技能中的默认建议 | 2026-09-03 | 项目负责人 |
| D-042 | 已确认（可见黑洞穿越与单次白光接管） | 项目负责人明确否决 D-041 的薄裂隙/抽象力场观感与星系形态，要求黑洞本体成为转场中持续可辨认的镜头主体：优化后的倾斜盘核与双主臂星系先保持流动，随后同一批真实星连续卷入具有非对称吸积盘、引力透镜与纯黑事件视界的黑洞，镜头加速穿越并由一次平滑、无频闪的白光曝光把外部 OBS 节目画面接管出来；不得再用眼睑状黑缝、塑料椭圆、传送门开孔或末帧硬切冒充黑洞穿越。每颗公共恒星仍严格一人一星，并必须直接保留该参与者服务端 `displayColor` 的冷暖和色相差异，不能统一漂白成灰白，也不能用额外彩色装饰星补密度；小尺寸、重尾亮度和克制光晕继续避免彩珠感。正常可见转场仍约 7.2 秒，触发首帧继承实时位置与速度；白光只能是进入事件视界后的单次连续曝光，不得反复闪烁，页面隐藏/中断/刷新/重连/reduced-motion 均跳过穿越和白光并直接得到权威静态终态。这里的“节目板”指 Canvas 透明后显露的外部 OBS 节目源；`PROGRAM_SUPPORT` 网页稳态仍无背景、标题、节目板或常驻弹幕面板，只有实时新合规弹幕正文飘过。单可见 Canvas、现有按需 GSAP、最多 300 颗真实星、服务端即时切场、覆层优先级及隐私边界不变。本决定替换 D-041 的黑洞表现、星系几何与星色材质，并仅在该转场内覆盖 D-039/D-040 的“禁止白闪”条款；其他 D-041/D-040 边界继续有效 | 2026-09-03 | 项目负责人 |
| D-041 | 已由 D-042 部分替代（持续星流、隐藏人数与连续性继续有效） | 项目负责人明确要求同学入场期间，已经公开抵达的真实恒星从出现起就在星系中持续流动；人数可能不会到齐，因此 `/screen` 的集结稳态及其离场文案不得显示当前星数、目标数、`x / 300`、满员率或到场进度。数据层仍保持一人一星、最多 300 且不得用伪星补密度。正常可见的 `ASSEMBLY → PROGRAM_SUPPORT` 继续约 7.2 秒，并以黑洞概念作为克制的物理隐喻：触发前的实时星流位置与切向速度必须逐帧延续，随后同一轨道连续弯折为吸积流，引力透镜、偏心事件视界、星群收束与噪声颗粒透明显影相互交叠；不得出现星流先停住、黑球突然生成、分段蓄力、门体开启或末帧硬切。黑洞不是独立的科普物件或实体圆盘，亮度只保留为低亮非对称焦散。`PROGRAM_SUPPORT` 透明稳态、仅实时新弹幕飘屏、单可见 Canvas、现有按需 GSAP、服务端即时切场以及隐藏/中断/刷新/重连/reduced-motion 直接静态终态等边界不变。本决定替换 D-040 的集结环境运动、人数文案和转场时间包络，D-040 的暗星云、真实星点材质、噪声消隐及其他边界继续有效 | 2026-09-03 | 项目负责人 |
| D-040 | 已由 D-041 部分替代（材质与噪声消隐继续有效） | 项目负责人连续否决 D-039 的塑料感，并明确指出背景与星点也缺少高级质感。正常可见的 `ASSEMBLY → PROGRAM_SUPPORT` 保持约 7.2 秒，但视觉重制为“摄影质感暗星云锁定 → 300 颗真实恒星沿三臂发生同向引力旋聚 → 星群压入偏心薄引力裂隙 → 星云由低分辨率确定性噪声场驱动颗粒侵蚀 → OBS 节目源自然接管”。`ASSEMBLY` 背景允许使用一张经审阅、无星点/文字/天体的本地暗星云纹理，并叠加确定性程序化尘埃；不得联网取图。公共恒星仍严格一人一星、上限 300，不得用背景星或装饰粒子补密度；绝大多数星点必须是低饱和冷白亚像素微光，采用重尾亮度/尺寸分布，只有极少数真实星点出现克制色温、光晕或衍射。禁止彩珠粒子、平滑渐变塑料底、完整椭圆/圆形传送门、同心环、对称门框、规则分层开孔、白闪和霓虹面板。节目显影只能使用不对称烟尘边界与断续焦散，不能形成实体圆盘或门体。`PROGRAM_SUPPORT` 稳态及实时弹幕规则继续沿用 D-039：网页层完全透明且默认无可见 DOM，只有实时新合规弹幕正文无底条短时飘过并离场销毁。实现保持单个可见 Canvas，离屏纹理只用于缓存背景与低分辨率溶解蒙版；继续复用现有按需 GSAP core/timeline，不新增 Three.js 或 GSAP 插件。页面隐藏、中断、刷新、重连和 reduced-motion 直接收敛到透明静态节目终态；动画不得延迟或驱动服务端事实。本决定替换 D-039 的背景、星点材质与转场镜头编排，其余协议、隐私、OBS、弹幕和优先级边界不变 | 2026-09-03 | 项目负责人 |
| D-039 | 已由 D-040 替代（视觉编排；实时飘屏继续有效） | 项目负责人明确认为 D-038 首版大屏动画质感不足，并授权在不改变约 7.2 秒总时长、节目透明稳态和协议事实的前提下进行电影化精修；同时纠正节目态设计为“没有常驻弹幕面板，只在新弹幕发送时让正文飘过”。正常可见的 `ASSEMBLY → PROGRAM_SUPPORT` 固定为“深空锁定 → 三臂星流同向旋聚 → 冷暖能量狭缝压缩 → 双侧空间门揭开 OBS 节目源 → 干净交给节目画面”的单向连续镜头；禁止全屏白闪、重复同心圆、无统一方向的随机星轨、常驻弹幕面板及转场期间弹幕抢镜。亮度峰值只局限在中央狭缝及门缘，画面始终保留深蓝黑层次；节目源只在空间门开启后从中央向两侧显露。`PROGRAM_SUPPORT` 稳态的网页层完全透明且默认无可见 DOM；仅实时收到新的合规 `barrage.published` 事实时，把不带底条、卡片或边框的匿名正文从右向左短时飘屏，离场即销毁，数量有界，不显示作者/来源，不从快照、刷新、重连或高优先级覆层期间补播。减少动态模式不执行位移动画。实现继续复用单 Canvas 的最多 300 颗真实恒星与现有按需 GSAP core/timeline，仅动画化少量覆层和有界弹幕节点的 `transform`/`opacity`，不新增 Three.js、GSAP 插件、媒体、图片或字体依赖；页面隐藏、中断、刷新、重连及 `prefers-reduced-motion: reduce` 仍直接收敛到完全透明静态节目终态。动效不得延迟或驱动服务端场景切换。本决定替换 D-038 的具体视觉编排与“常驻透明弹幕面板”规则，其余 D-038 边界继续有效 | 2026-09-02 | 项目负责人 |
| D-038 | 已确认（加长星海转场并收敛节目大屏） | 项目负责人要求把大屏从 `ASSEMBLY` 进入 `PROGRAM_SUPPORT` 的正常可见转场加长为约 7.2 秒，采用“星海蓄能 → 轨道加速与星轨迁移 → 舞台退场 → 透明弹幕面板落位”的连续镜头；运行状态仍由服务端命令即时切换，动画不得延迟、推进或反向触发业务事实。实现继续使用单 Canvas 绘制最多 300 颗真实恒星，并仅以现有按需 GSAP core/timeline 驱动少量 DOM 覆层的 `transform`/`opacity`，不新增 Three.js、GSAP 插件、逐星 DOM/GSAP、媒体或素材依赖。`PROGRAM_SUPPORT` 稳态改为完全透明 OBS 源：不渲染背景、大标题、节目标题、边缘星点或礼物视觉，只保留一块不含作者/来源的透明弹幕面板；礼物业务与账本仍正常生效但不在大屏呈现。抽奖、故障、终章等更高优先级必要覆层继续有效。刷新、重连、页面隐藏、中断及 `prefers-reduced-motion: reduce` 直接进入同一透明静态终态，不补播转场或互动。本决定只覆盖 D-026 中节目稳态边缘星/标题/礼物视觉及短转场细节，不改变三场景、服务端权威、隐私、弹幕治理、抽奖、终章、OBS alpha、无网页媒体/音频和单 Canvas 边界 | 2026-09-02 | 项目负责人 |
| D-037 | 已确认（取消寄语并启用中场个人抽奖） | 项目负责人明确取消手机端、后台和大屏的时光胶囊/寄语产品流程，并要求在节目中场增加“具体到人”的抽奖。协议 v2 当前流程改为“激活 → 锁定星色并直接准入”；既有胶囊表和列只保留为前向迁移兼容层，正文与终章 recap 清除，旧命令拒绝，所有页面不再提供入口。原胶囊 20 星光并入首次启动恒星，使总星光上限仍为 100。抽奖仅允许在 `RUNNING + PROGRAM_SUPPORT` 开启，由服务端在当前代已准入参与者中无放回随机抽取；后台显示合成姓名和公开星号，大屏只显示公开星号。抽奖记录持久化并随快照/重连恢复；暂停、换场、完成或手动关闭会自动收屏但保留结果，只有排练模式的 Demo 管理员可以清空。该决定覆盖 D-009、D-011、D-023～D-026、D-029～D-032 中所有胶囊/寄语及“抽奖延期”边界，不改变三场景、身份隐私、弹幕安全、终章或 reduced-motion 原则 | 2026-08-30 | 项目负责人 |
| D-036 | 已确认（V2-10 人工签核关闭，Demo v0 收口） | 项目负责人在 2026-08-15 现场预览栈（临时 `V2_ACTIVE` 合成库，`DEMO_HOST=192.168.3.6`）上完成 V2-10 人工签核并报告"全部通过"：vivo X300 新鲜邀请 normal-motion 金标首镜、三端局域网旅程 20 项与 OBS 实际合成 5 项全部 `PASS`，无 S0/S1、无真实数据泄漏、无写入越权；[`V2_10_FIELD_ACCEPTANCE.md`](./V2_10_FIELD_ACCEPTANCE.md) 已按负责人报告填表并关闭。证据限制：Windows/Chrome/OBS/vivo 版本号与真实 CSS 视口未单独记录，与 D-021 对 vivo 版本的证据限制处理一致，不影响负责人签核结论。至此协议 v2 的 V2-00～V2-10 全部关闭，Demo v0 完成；剩余事项仅为正式版延期项（NFC 写卡、印刷、公网、真实名单、正式 VI 等，见 PROJECT_CONTEXT §5）。经项目负责人授权，全部工作树改动已于 2026-08-15 提交并推送（`5f69311` feat、`ed4bd99` docs、`c709913` docs）至 `origin/release/demo-v0`（分支由 `feature/mobile-interaction-layout` 更名而来） | 2026-08-15 | 项目负责人 |
| D-035 | 已确认（接手优化批次） | 项目负责人授权执行优化清单 A/B 全部任务：A1 后台关键命令权威反馈（成功提示回显 epoch/run/presentation/interaction revision，并与刷新后的权威快照对比，revision 未变化时明示警告，响应 D-021 OBS-01/02 教训）；A2 视觉回归门 `tests/e2e/v2-journey-visual.spec.ts`（normal-motion 金标旅程：时长窗口/同星交接/CLS<0.05/无横向溢出断言 + 阶段截图到被忽略的 `output/playwright/v2-journey-visual/`，实测 journey=6049ms、CLS=0.0147 通过）；A3 `scripts/check-docs.mjs` + `pnpm docs:check`（链接完整性 + DECISIONS 严格倒序，已接入 CI）；A4 回滚演练（恢复 D-034 v1 备份 → v1 验证 → 再次切换 v2 成功，演练备份 `demo-v1-drill-backup-2026-08-15.sqlite` SHA-256 `5ce7f1ad2577952fd4ae26b11f03a8f8b812e3ca456b2f4b263246df02a65076`；同时沉淀"回滚前删除残留 -wal/-shm"的 WAL 卫生规则写入 RUNBOOK）；A5 `.github/workflows/ci.yml` 重写为可执行全门（frozen install → 契约/类型/测试/构建/docs:check → chromium-ci 全量 E2E）；A6 `preview:v2:field` 终端检查单模式（`DEMO_FIELD_CHECKLIST=1`，P/F/B/S 逐项，写入被忽略的 `output/field-check/`，TTY 与非 TTY 双路径，实测 25 项全流程通过）；B7 `scripts/vivo-field-automation.mjs` + [`FIELD_AUTOMATION.md`](./FIELD_AUTOMATION.md)（adb+CDP 真机只读预检脚手架，不代签）；B8 前端 bundle 拆分（Vite 8/Rolldown 函数式 manualChunks，vendor-vue 88.8KB/34.4KB gzip 分离，主包 97→63KB gzip）；B9 后端延迟 metrics（`DEMO_METRICS=1` 时每 60s 输出路由级 p50/p95/max 直方图，默认关闭，`pnpm dev` 已开启）。`output/` 整目录加入 .gitignore。验证：Vitest 39/360、三浏览器闭环、视觉门、现场 smoke、docs:check、三项构建全部通过 | 2026-08-15 | 项目负责人 |
| D-034 | 已确认（v1→v2 实际切换与启动器 v2 化） | 项目负责人授权并执行仓库实际 `backend/.data/` 的 v1→v2 一次性切换。前置序列：`v2-switch` 首次运行应用 0008～0012 后中止 → 当前代 v1 服务真实 listen 并干净关闭（同代回执）→ 因存在 v1 演示残留（账本 2、会话 5 等）由确定性重置回到干净基线（epoch 7→8）→ 再次同代 listen/干净关闭 → 切换成功。结果：`resetEpoch 8→9`、`V2_ACTIVE + SYNTHETIC_DEMO`、schema=12、300 身份 slot 目录，`v2-verify` 通过；v1 备份 `backend/.data/demo-v1-backup-2026-08-15.sqlite`，SHA-256 `bbcede7ccfc329e082f369dc1eb4616d971c79dd5c6879839bd90a3132718ea0`。同时把 `scripts/dev.mjs` 升级为协议感知启动器：`V2_ACTIVE` 库跳过 v1 种子/迁移步骤，就绪探针改用 `/api/protocol-capabilities` 的 ACTIVE 判定；`pnpm dev` 冒烟通过（三端 200、能力发现 v2、`/api/v2/screen/snapshot` 200）。回退方式：停服务后整体恢复上述备份 + 同版 v1 代码，禁止手工拼表 | 2026-08-15 | 项目负责人 |
| D-033 | 已确认（2026-08-15 文档治理重组） | 项目负责人要求重组 `docs/`：新增 [`README.md`](./README.md) 索引；把 v1 历史内容归档到 [`docs/archive/`](./archive/README.md)（v1 需求、v1 测试证据、v1 运行手册、v1 API、G0～G4 验收原文、G5 基线、动画交接），`G5_MOBILE_BASELINE.md`、`ANIMATION_HANDOFF.md` 与两份 `ACCEPTANCE_G0_G4*.md` 改为指路桩；`REQUIREMENTS.md`、`TEST_PLAN.md`、`PROJECT_CONTEXT.md` 改为仅载 v2 现行内容；`RUNBOOK.md` 重写为 v2 手册；新增 [`GLOSSARY.md`](./GLOSSARY.md)；各文档状态头统一为一行，DECISIONS 表改为严格倒序。文档更新规则简化为"改哪写哪，不再要求多文档连锁同步；影响实现或验收的决定仍须写入本文件"。同时把 `PROTOCOL_V2.md` 第 7 节的手机动效时长同步为 D-030 金标（参考约 5.4s/1.0s/4.2s），D-027/D-028/D-029 的 2.8s/1.2s/3.2s 与历史自动数字降级为历史证据。安全红线不变：合成数据、不碰 `backend/.data/`、维护门、不代签真机验收、未经授权不提交推送 | 2026-08-15 | 项目负责人 |
| D-032 | 已确认并完成自动子门，vivo 真机字体与视觉待签核 | 项目负责人确认本轮 `/welcome` 手机端协调性与本人档案边界：页面级大标题采用中文代码终端气质的本机等宽回退栈，正文采用克制、易读的科幻无衬线回退栈；本轮明确不采用像素游戏字体，也不新增网络字体、未获准捆绑的中文字库或授权不明字体。进入每个新视图只保留一个顶部大标题并逐字显现，末尾具有可见光标，下方操作坞不得重复同名标题，不必要英文删除，减少动态、页面隐藏、刷新、重连及失败恢复直接显示完整静态标题且动画不得承载业务推进。`/welcome` 下方只保留一个能透出星系背景的高透操作坞，面板、控件和弹层改用接近直角的非对称微角，避免常见大圆角卡片墙；退出操作移除普通描边，改用克制的科技括角提示，同时维持不小于 44×44px 的触控目标和清晰焦点。已认证参与者的 `displayName` 与预分配 `personalStarCode` 字段只进入本人私有投影；`personalStarCode` 在锁色前可稳定存在，但不代表公共恒星已经建立，此时 `ownPublicStarId` 仍为空且公共星数不增加；锁色事务成功后，同一值才按既有 `publicStarId` 规则成为公共恒星主键，姓名仍不得进入公共、大屏或后台投影。退出确认后，人工重新认证界面只收"学生姓名 + 恰好 8 位学号后缀"，客户端仅在当前 `SYNTHETIC_DEMO` 中补固定前缀 `2026` 并复用既有人工协助激活接口以恢复同一合成身份，不得生成第二个参与者；失败提示保持通用，不得把该适配层表述为真实学生名册核验，`studentNumber` 永不进入任何响应、快照、事件或公开字段。入场页顶部以"找到属于你的星"的逐字标题为主，并显示本人姓名、星号和迎新晚会欢迎语；星际档案依次显示档案者姓名、星星编号、星色、动力、星光与时光胶囊，不再显示"已完成入场"一类冗余状态；动力和星光必须是可点击、可聚焦、可关闭的解释入口，分别说明"动力是赠送虚拟礼物的可消费额度"和"星光是由权威奖励累积、不可消费或兑换的个人成长记录"。本决定只确认表现、本人私有投影和当前合成 Demo 的重新认证适配，不改变服务端权威、公开恒星建立条件、奖励规则、退出撤销会话、恢复语义或既有动效安全边界，也不构成正式身份系统授权。2026-08-15 自动证据为聚焦 5 文件/73 项、根级 39 文件/360 项、contracts/backend/frontend 构建（前端 80 模块）、两项 typecheck、Google Chrome 正式个人旅程及三项目 v2 E2E 全部通过；这些桌面证据不代签 vivo X300 的实际字体回退、触控、软键盘与视觉观感 | 2026-08-15 | 项目负责人 |
| D-031 | 已确认并完成自动子门，vivo 真机视觉待签核（D-030 手机稳定星系与品牌入口补充） | 项目负责人要求 `/welcome` 的稳定 `ORBIT` 在普通动态且页面可见时持续缓慢流动：星系中心必须最亮并向外单调衰减，既有 `156` 个本机装饰轨道微粒使用固定种子从克制的多色恒星光谱中派生，保持跨帧稳定且不代表或依赖其他参与者；本人公开星号必须跟随显示在本人运动恒星下方，不再固定在页面左下。页面隐藏时停绘，`prefers-reduced-motion: reduce` 固定为同一静态轨道终态，动画继续不得承载业务事实。项目负责人同时明确授权把本轮提供的白色透明 PNG（SHA-256 `5E665D84994508A6A5BF106EF87A22E0CC443EA63634B5384A310EF3A498C008`）仅用于 Demo `/welcome` 左上学院标识，并链接中山大学智能工程学院官方站点 `https://ise.sysu.edu.cn/`；图片必须随前端本地打包，页面加载不得向官网或第三方拉取图片。该授权不等于正式矢量母版、其他页面、品牌色或线下物料的完整 VI 授权 | 2026-08-14 | 项目负责人 |
| D-030 | 已确认，视觉还原进行中（覆盖 D-027～D-029 的手机表现实现细节） | 项目负责人明确否决当前正式 `/welcome` 与已认可动态预演之间的视觉偏差，并要求停止继续微调现有扁平径向线方案；`motion-previsual-personal-star/personal-star-journey-animatic.webm`、同目录 `index.html` 与十张关键帧成为本轮唯一视觉金标。正式页必须与预演共用同一生产渲染器，允许使用经压缩验收的原创星云纹理、前/中/后分层 Canvas 或等价合成层、局部颗粒与确定性本机轨道尘；装饰星点不代表参与人数，手机端个人旅程与主星系的视觉完整性不依赖同步或突出其他参与者恒星。正常首次寻星参考约 `5.4s` 的沉入/接近/捕获，锁色确认参考约 `1.0s`，寄语停留由真实用户输入决定，提交或持久化跳过后的连续拉远入轨参考约 `4.2s`；同一颗本人恒星必须处于同一坐标系，不得以 DOM 星淡出后再出现 Canvas 选中圆环冒充连续交接。手工预览必须先把真实手机页置前再触发镜头，不得在检查器背后播放后只展示终态。D-029 既有自动结果仅保留为业务隔离、恢复、几何与旧实现回归证据，不再构成当前视觉通过。D-030 只覆盖 `/welcome` 的时长、低 DOM SVG、单 Canvas、低粒子密度及 D-013 中与本次金标冲突的表现限制；服务端权威、隐私、触控、软键盘、失败静态、后台中断和 `prefers-reduced-motion: reduce` 直接静态终态等边界继续有效。预演本身使用原生 CSS/Canvas/rAF，本轮不因还原新增运行依赖，也不需要删除 Codex skill 或治理文件 | 2026-08-14 | 项目负责人 |
| D-029 | 已确认并完成聚焦浏览器子门（D-025/D-028 手机个人旅程表现顺序修订） | 项目负责人确认独立动态预演后授权正式实现：`/welcome` 首次寻星仍沿用 D-028 的约 2.8 秒持久星交接；锁色权威成功后不再立刻把镜头拉回星系，而由同一持久星播放约 1.2 秒色温闪烁并平滑上移到寄语画面。提交或持久化跳过胶囊后，服务端先完成 `ADMITTED` 事实，表现层再以约 3.2 秒将同一星缩远到真实手机 Canvas 的本人轨道位置，同时逐步展开流动星系与收起高透玻璃操作坞。手机 Canvas 可绘制不代表参与人数的低亮轨道微尘以维持低人数时的环绕层次；真实恒星仍来自权威 `publicStars`，本人恒星仅轻微放大并保留本人标签。上述动画不得决定、延迟或伪造协议事实；隐藏、中断、刷新、恢复与 `prefers-reduced-motion: reduce` 直接进入对应权威静态终态。`/welcome` 继续使用 Vue/CSS/SVG/单 Canvas，不迁移 GSAP。聚焦 Google Chrome 真实邀请 normal-motion 动线已通过：首次寻星 `2765/2773ms`、F6→F7 `0px`、CLS `0`，寄语后入轨 `3298ms`，持久星节点未替换、最终单 Canvas 与本人标签可见；该自动子门不代签 vivo X300、三端局域网或 OBS 人工验收 | 2026-08-14 | 项目负责人 |
| D-028 | 已确认并完成自动连续性子门（D-027 手机首次寻星视觉连续性修订） | 项目负责人在真机上否决 D-027 首版寻星的视觉连续性；D-027 已通过的几何、时长、可见性、恢复和业务隔离自动断言不等于视觉体验通过，其历史数值与原证据范围保持不改。`/welcome` 当前权威改为：电影与选色静态态共用同一颗持久恒星节点，统一位于横向 `50%`、纵向 `42vh` 的视觉轴；约 2.8 秒以"建立/加速 → 连续穿越 → 减速/捕获 → 锁定/交接"四阶段编排全屏低 DOM SVG 径向速度线，并用同心白核、中性光晕与默认 `5800K` 色温光晕完成无跳变交接。选色文案与控件提前渲染但在交接前保持 `inert`、不可聚焦，末帧只解除交互隔离，不替换恒星、不改位置/尺寸/颜色；正常完成以主动画 `animationend` 为准，超时仅作兜底。实时连接和服务端权威事实不得等待表现层；隐藏、中断、刷新、恢复和 `prefers-reduced-motion: reduce` 直接进入同一静态可操作态。D-026 的 GSAP core 例外仍只适用于 `/screen`，不得迁移到 `/welcome`。新自动证据为 Chrome artifact 正常首次 `2783ms`、隐藏→可见 `2717ms`、F6→F7 恒星中心/尺寸差 `0px`、CLS `0` 与六帧序列目检；根级全量测试 37 文件/341 条、两项 typecheck、前端构建 71 模块、三项目 v2 E2E 及现场预览 smoke 均通过。D-028 自动连续性子门为 `PASS`；vivo X300 必须使用新鲜邀请对当前版本重新完成人工视觉签核，结果仍为 `PENDING`，自动证据不得代签 | 2026-08-14 | 项目负责人 |
| D-027 | 已确认并完成自动子门（D-025 手机首次寻星细则修订） | `/welcome` 正常首次成功激活后的星云寻星调整为约 2.8 秒，以 Vue 与 CSS 的 `transform`/`opacity` 编排更明确的穿越、聚焦和中性恒星显现，不给手机端迁移 GSAP。只有服务端返回首次新建身份、权威状态为 `NEEDS_COLOR`，且页面已处于稳定 `visible` 时才开始；开始前暂不可见应等待，真正开播后若页面进入后台或被中断则立即收束到同一权威静态终态，返回时不重播。正常首次仍不可跳过；`prefers-reduced-motion: reduce`、刷新、已激活返回/重认证、断线恢复、失败与超时均直接静态。动画不得推进或延迟任何业务事实。D-027 只替代 D-025 及当前文档中的 1.8～2.0/1.9 秒时长和开播可见性细则，D-024 的 0.9 秒历史记录保持不改。"真实邀请 + normal-motion + 生产 Vue 页面"的聚焦浏览器断言与全量自动门已真实通过；自动子门为 `PASS`，但仍不能替代 vivo X300、三端局域网与 OBS 的人工复验 | 2026-08-14 | 项目负责人 |
| D-026 | 已确认（V2-07 大屏表现与导播边界） | v2 大屏不显示任何参与者编号、星号、排行或可点击个人节点；最多 300 颗公共恒星只按稳定不透明槽位和已锁星色形成 Canvas 星系。`ASSEMBLY` 使用完整星海，`PROGRAM_SUPPORT` 切为 OBS 透明叠加层，中央画面与声音由 OBS/导播负责，网页仅在边缘保留约 15%～20% 的弱星系并展示匿名弹幕与礼物，`COOPERATIVE_LIGHT` 回到全屏协同点亮；网页不得上传、播放或控制节目媒体。弹幕在服务端内容/长度/频率/来源规则通过后匿名公开，后台可暂停、带原因撤下、屏蔽匿名来源并清屏；礼物只公开节目、礼物种类和时间，不公开发送者，1.5 秒内同类礼物合并，画面最多保留一个主礼物和一个次礼物，星舰可使用更突出但有限时的覆层。人工胶囊插入独占视觉层，期间到达的弹幕/礼物仍持久化但不排队补播；优先级固定为 `COMPLETED > FINALE_PREVIEW > CAPSULE_INSERT > 故障提示 > 当前场景 > 实时互动`。LIVE 终章只在首次实时观察完成事件时播放约 5～6 秒收束，刷新、重连和减少动态直接进入稳定终态。V2-07 允许 `/screen` 按需加载 GSAP core，仅驱动少量 DOM 覆层的 transform/opacity；300 星点仍由单 Canvas 绘制，禁止逐星 GSAP、GSAP 插件、媒体控制、动画业务回调及手机端顺带迁移 | 2026-08-13 | 项目负责人 |
| D-025 | 已确认（协议 v2 当前权威） | 采用不兼容的协议 v2：NFC 为主入口，二维码/短链接承载同一随机令牌，合成姓名/学号仅作 Demo 人工协助备用且必须恢复同一身份；参与者时钟改为"核验即激活并获得初始权益 → 锁定星色后恒星加入星系 → 提交或持久化跳过时光胶囊后准入"，与现场运行时钟解耦。全场仅保留集结、节目支持、协同点亮三个场景，完成为终态而非第六阶段，晚到者完成自身流程后直接进入现场当前场景。手机端以流动星系和唯一突出本人恒星为主舞台，下半页只设一个克制的液态玻璃操作坞；首次成功核验播放不可跳过的短星云寻星，锁色成功后播放缩放回星系，减少动态及恢复路径直接进入静态状态。胶囊为后台人工触发的插入投影，不改变运行场景；排练结尾是服务端权威、可撤销且持续标记的演示投影，不写现场完成状态。合法状态组合、奖励触发、版本化账本、快照/revision/事件、暂停与完成写入边界、普通 Demo 重置、合成数据破坏性切换及验收细则以 [`PROTOCOL_V2.md`](./PROTOCOL_V2.md) 为唯一权威。D-025 覆盖 D-008 的六阶段、D-023 的个人/全场六阶段与旧入场顺序、D-024 的 0.9 秒和首帧可跳过；D-023 的同身份备用核验、公开星号格式及其他不冲突的安全、隐私、视觉和延期边界继续有效 | 2026-08-13 | 项目负责人 |
| D-024 | 已部分替代（由 D-025） | 项目负责人批准深午夜蓝星海、发光圆点恒星和克制轨道作为 G5 手机端视觉根基，并明确取消怀士堂线稿转场；星色选择、约 0.9 秒可跳过入轨过场、时光胶囊公开候选确认与 390×844 固定布局第一版可进入代码和协作阶段。动画协作者只修改表现层并保留 reduced-motion；身份学号、`L-4821` 星号、后台人工选中和大屏胶囊展示仍是后续独立任务。当前代码允许提交并推送到 `feature/demo-v0` 供协作，旧 GitHub Issue #1～#5 全部删除，后续按新范围重建。D-025 保留深午夜蓝星海、发光圆点恒星、克制轨道、取消怀士堂线稿和 reduced-motion 原则；0.9 秒、首帧可跳过、旧入轨顺序及审批状态不再是 v2 验收依据 | 2026-08-11 | 项目负责人 |
| D-023 | 已部分替代（由 D-024、D-025） | `/welcome` 优先按 390×844 无整页纵向滚动重构：NFC 个性令牌为主要入口，姓名与学号为备用核验；首次进入显示编号星辰，公开星号采用姓氏拼音首字母加学号后四位（项目负责人接受其可关联风险并确认预期名单不重复），用户通过红—橙—黄—白—蓝白连续恒星温度条选色，显示 Kelvin 数值，点击"确认星色 · 进入星辰"后本场锁定；星辰进入中央并以原创信号动画勾勒怀士堂，随后进入可跳过的时光胶囊。提交时光胶囊必须确认内容可能经人工筛选后以星号和星色上屏，提交即进入候选池，不由 AI 自动公开，也不承诺保存四年。主导航为"星程 / 节目单 / 档案"，六阶段改为直线进度，节目单内部可滚动，节目开始后底部常驻弹幕与礼物入口。抽奖只保留星号扩展方向，本阶段不实现抽奖、兑奖或领奖核销；两张怀士堂图片只作结构参考，不复制现成线稿。D-025 保留其中与 v2 不冲突的隐私、公开候选、非 AI 自动公开、抽奖延期、合成姓名/学号人工协助备用核验与公开星号格式；人工协助核验必须映射到同一随机令牌身份和同一状态，不得建立第二账号。六阶段、旧入场顺序及旧移动端信息架构不再生效 | 2026-08-11 | 项目负责人 |
| D-022 | 已确认 | G0～G4 提交绑定冻结基线为提交 `63e3d5094c49a5635f4ebf7ccbd0c9da07b7d58a`（tree `1cf870688e5fc13231988b6aa3eec7afc70de5da`）。该提交形成前暂存范围审查通过，形成后在干净工作树实际执行根级 `pnpm verify:g4`，退出码 0、观察时长约 511.7 秒；Vitest 20 文件/119 条、共享契约/后端/前端构建及 Playwright 9 场景 × 3 项目 = 27/27 全部通过。G4 用 300 个固定合成参与者和 301 条 WebSocket 完成 135684.5 ms Windows loopback 旅程，阶段/礼物/弹幕/协同点亮 p95 为 50.57/1434.59/1664.35/1121.22 ms，目标操作失败、协议错误与核心不变量错误均为 0；清理后工作树干净、临时目录与 3000/5173 监听均为 0。两份被忽略的脱敏报告 SHA-256、环境、恢复、隐私与限制见 [`archive/acceptance-g0-g4-d022.md`](./archive/acceptance-g0-g4-d022.md)。D-022 只冻结本地 Demo v0 的 G0～G4 网页与服务基线，不覆盖 G5、30 分钟 soak、分布式局域网、场馆、公网、实体 NFC、真实数据或正式上线；本记录提交已获负责人授权，但不得推送或创建 PR | 2026-08-10 | 项目负责人 |
| D-021 | 已确认 | G0～G4 以 2026-08-10 当前未提交工作树完成总验收：项目负责人确认 G0 的合成数据、共用后台与延期边界，vivo X300 默认浏览器人工核心旅程通过，本轮未发现或复现未关闭 S0/S1；最新根级 `pnpm verify:g4` 退出码 0、观察总耗时约 463.2 秒，Vitest 20 文件/119 条、三项生产构建及 Playwright 9 场景 × 3 项目 = 27/27 通过，浏览器报告窗口 295388 ms。最新 G4 Windows loopback 报告用 300 个固定合成参与者和 301 条 WebSocket 完成 123452.51 ms 旅程，阶段/礼物/弹幕/协同点亮 p95 为 46.12/1426.08/1438.55/939.64 ms，目标失败、协议错误和核心不变量错误均为 0。两次人工漏操作分别通过补做紧急清屏与结束命令收敛，记为体验观察而非已复现缺陷；最终确定性重置回到 E7、排练、就绪、Stage 1 空白基线。真机浏览器版本与实际 CSS 视口未单独记录，作为本次证据限制接受；当前结论不是可复现提交、30 分钟 soak、分布式局域网、场馆或正式上线验收。G5 仍为下一阶段，在负责人另行授权前不得提交、推送或创建 PR | 2026-08-10 | 项目负责人 |
| D-020 | 已确认（历史 G4 运行；由 D-021 完成总验收） | G4 以 2026-08-10 最终根级 `pnpm verify:g4` 实际结果收口：退出码 0、总时长 460849.4 ms，Vitest 20 个文件/119 条、共享契约/后端/前端生产构建全部通过；Playwright 9 个场景在 `chromium-ci`、系统 Chrome、系统 Edge 各 9/9，合计 27/27，报告 `passed`、失败检查点 0、浏览器部分约 299.7 秒。最终负载报告在 Windows loopback 上用 300 个固定合成参与者、301 条 WebSocket 连接完成 126523.63 ms 混合旅程；阶段 20 次、礼物 1200 次、弹幕 300 次、协同点亮 300 次的 p95 分别为 48.78、1409.71、1417.94、1056.86 ms，四档礼物各自 p95 均小于 2 秒；业务失败、协议错误、重复账本/奖励/扣减/幂等记录、负余额和旧 epoch 污染均为 0，301 条连接在服务重启后全部恢复，重置、旧会话失效、旧 epoch 重同步和固定凭据再激活通过。脱敏报告均被 Git 忽略。该结论通过 G4 本机协议专项与当前全门回归，但不是 30 分钟 soak、分布式局域网、场馆或正式上线结论。G5 按 D-013 作为下一开发阶段，vivo X300 人工证据继续 `PENDING`，正式延期项不变 | 2026-08-10 | 项目负责人 |
| D-019 | 已确认 | G3 自动化门以 2026-08-10 的稳定实际结果收口：根级 `pnpm verify:g3` 退出码 0、总时长 278.9 秒，Vitest 16 个测试文件共 102 条全部通过，Playwright 6 个场景在 `chromium-ci`、系统 Chrome、系统 Edge 三个项目合计 18/18，共享契约、后端与前端生产构建通过，退出后 E2E 临时目录为 0。该结果只证明自动化门通过；G3 最终关闭仍须项目负责人完成 vivo X300 默认浏览器人工证据并确认无未关闭 S0/S1。浏览器报告是被忽略的本地脱敏证据，CI 仅已配置且尚无远端运行证据。300 人负载与 p95 仍严格归 G4，G5 视觉仍待后续门禁 | 2026-08-10 | 项目负责人 |
| D-018 | 已确认 | Demo v0 后续门禁连续冻结为：G3 只完成真实浏览器端到端与恢复验收，使用真实本地服务和固定合成数据，覆盖 Chrome、Edge 与由项目负责人在 vivo X300 默认浏览器执行的人工证据；G4 单独完成 300 个合成参与者的协议级负载、传播延迟与运行韧性证据；G5 继续按 D-013 统一"轨道信号场 Orbital Signal"视觉与动效。G3 不新增业务、不承担正式 300 人性能结论、不做视觉重设计；实体 NFC、印刷、公网、真实数据、外部 AI 与场馆硬件仍按 D-017 延期 | 2026-08-10 | 项目负责人 |
| D-017 | 已确认 | Demo v0 只交付网页、本地服务和验证工具；实体 NFC 写入、邀请函印刷、短网址、公网部署、真实数据、AI 审核、正式节目 CRUD、现场硬件和最终品牌授权均延期，不阻塞 Demo v0 | 2026-08-10 | 项目负责人 |
| D-016 | 已确认 | 所有业务写入由服务端持久化并保证幂等；刷新和服务重启后状态保留，断线时禁止写入，重连后获取权威快照；确定性重置恢复固定合成种子并保持邀请令牌有效 | 2026-08-10 | 项目负责人 |
| D-015 | 已确认 | Demo 身份只使用固定种子中的虚构姓名和六位 Demo 码；同一参与者可在多个设备或浏览器同时登录并共享服务端状态，不接真实名单或真实身份字段 | 2026-08-10 | 项目负责人 |
| D-014 | 已确认 | G0 及后续实现均只使用合成数据；在项目负责人明确授权前不得提交、推送或创建 PR | 2026-08-10 | 项目负责人 |
| D-013 | 已确认 | Demo v0 网页视觉方向为"轨道信号场 Orbital Signal"，项目标题为"智工星域｜协同感知 · 共同抵达"；使用深午夜蓝、精确网格、学院蓝轨道、琥珀节点和受控光晕，避免紫粉大渐变、通用星空、玻璃卡片墙、重型 3D 和大型动画库 | 2026-08-10 | 项目负责人 |
| D-012 | 已确认 | Demo v0 以 300 个合成参与者进行协议级负载验证；阶段、礼物、弹幕和协同点亮更新的传播延迟 p95 不超过 2 秒，并覆盖 vivo X300 默认浏览器、Windows Chrome、Windows Edge、1920×1080 与减少动态效果模式 | 2026-08-10 | 项目负责人 |
| D-011 | 已确认（奖励触发与版本语义由 D-025 澄清） | 初始动力值 100；虚拟礼物为微光 5、信标 10、星轨 20、星舰 50，可重复赠送且无现金价值；当前星光值上限 100，默认分配为首次成功激活 20、首次实际提交时光胶囊 20、首次有效启动星星 20、首次送礼 10、首次合规弹幕 10、首次协同点亮 20。跳过胶囊为 0，精确触发、幂等和版本规则以 D-025/协议 v2 为准 | 2026-08-10 | 项目负责人 |
| D-010 | 已确认（接受风险） | 后台使用共用账号，不记录真实操作者姓名；任何已登录会话均可自行提升为审核处理、阶段控制、Demo 管理或全部权限。角色只作为能力分组和会话日志标签，不构成可靠的最小权限或个人审计 | 2026-08-10 | 项目负责人 |
| D-009 | 已确认 | 未来寄语在 Demo v0 中保持私密并只进入本人档案；弹幕在首次公开告知后，经本地确定性规则校验即匿名实时上屏，后台负责事后删除、屏蔽来源和紧急清屏；AI 审核仅预留接口 | 2026-08-10 | 项目负责人 |
| D-008 | 已替代（由 D-025） | Demo v0 必须让六阶段均形成真实可操作闭环；排练模式允许跳阶段，现场模式只能向前推进，服务端状态为唯一权威。该六阶段编排仅作 v1 历史记录，不再是协议 v2 的当前实现或验收依据 | 2026-08-10 | 项目负责人 |
| D-007 | 已确认 | Demo v0 在同一局域网运行，采用同仓库 Node.js/TypeScript、Fastify 5、`better-sqlite3`/SQLite（WAL）、HTTP 命令接口、`@fastify/websocket` 事件流和共享 TypeScript/Zod 契约；正式部署方案仍由 D-004 延期决定 | 2026-08-10 | 项目负责人 |
| D-006 | 已确认 | 项目由负责人本人长期单人维护；Demo v0 继续使用同一仓库，从 `design/invitation-ui-guide` 创建 `feature/demo-v0`，不创建新仓库、文件夹或工作区 | 2026-08-10 | 项目负责人 |
| D-005 | 已确认 | 采用一人一卡一码的随机个性令牌；同卡 NFC 与二维码编码同一令牌 | 2026-08-10 | 项目负责人 |
| D-004 | 待确认 | 正式版后端平台、HTTPS 域名和公网部署环境；不阻塞本地 Demo v0 | — | 项目负责人 |
| D-003 | 已替代 | 原五人协作时使用微信沟通、GitHub Issue/PR 记录；当前单人维护方式由 D-006 替代，GitHub 与项目文档继续作为正式记录 | 2026-08-09 | 项目负责人 |
| D-002 | 已确认 | NFC 为主要体验入口，二维码和短网址为设备兼容备用，进入同一 H5；实体写卡与印刷不属于 Demo v0 | 2026-08-09 | 项目负责人 |
| D-001 | 已替代 | 使用个人 GitHub 仓库进行五人协作；当前维护方式由 D-006 替代 | 2026-08-09 | 项目负责人 |
