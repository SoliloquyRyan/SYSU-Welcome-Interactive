# D-109：半屏控台、一屏入场与游客应援

2026-09-17，负责人确认实施及配套发布。基线 `05adbdb937d2a0c11b64d92dbd84b5e0da0ff9af`，开始时工作区干净。

## 恢复与数据边界

- D-108 源码归档：项目外 `.welcome-recovery/d109/d108-05adbdb-source.zip`，SHA-256 `368bb43ffe8fffaa3212ed299df86440b63fb0d56d5487e027207d2e104e770e`。
- 本轮发布不自动重置正式场次。正式重置须主控在待开始、暂停或已结束时输入“重新开场”，先校验数据库归档，再开启新轮次。
- 固定名单 256 学生＋50 工作人员，游客每轮最多 94，总容量 400。游客昵称直接进入，100 动力，不参与正式计分、投票或抢答；同浏览器恢复，不声称匿名身份具有实名唯一性。
- 仅手机更换入场：一屏姓名、学号、选色，霓虹背景及个人星色流星；大屏星海、12 秒开场、165 秒片尾保留。
- 网页半屏固定控制、页内切换，OBS 由操作员手动配合；移除监视预览，保留重要功能。

## 工程约定

- schema 23 增加权威 `account_kind`（STUDENT / STAFF / GUEST）；旧 `account_type` 列保留为既有名单指纹的兼容信息。业务权限统一读新字段，旧 schema 读取原字段，避免重建身份父表破坏历史外键。
- 游客恢复凭据只保存摘要，Cookie 使用既有安全属性；正式名单指纹不包含游客。
- 归档只复制运行数据库与非秘密校验信息，不复制运行鉴权文件、原始表格或账号分发清单。
- 工程、三浏览器、OBS 与离线材料验证已完成；公网切换及保留数据结果见发布恢复索引。


## 已实施的行为

- 控台移除公共预览及专用订阅，顶部、舞台按钮、页签和底部固定；960×900 / 800×700 下使用分页和固定弹窗。功能位置见[主控操作单](./LIVE_OPERATOR_GUIDE.md)。
- 新增正式轮次归档重置；不复用合成清库入口。备份失败、并发写入、版本过期、事务回滚、响应丢失、重启重试均有覆盖。原执行主控可恢复回执，其余旧会话失效。
- 手机学生、工作人员一屏核验与锁色；游客昵称＋星色，首次成功短流星，重复登录与恢复不重播。手机完成后的纪念页保持只读连接，重置后自动回到入场表单，再次提交使用新轮次。
- 游客恢复凭据只在浏览器 Cookie 中持有，服务器只存摘要。游客转学生不合并账户、记录或动力。
- 最终节目单稳定 ID、顺序和 25 / 19 数量保持。夜航星改为王奇琦；三项典礼参考时长 5 / 8 / 15 分钟。查无此队完整成员及禁礼物保持。
- 舞台中文标题统一方线体，英文及重点数字为 Orbitron，姓名正文和手机 Noto。方线体核心预加载仅发生在大屏，21 个追加分片按字形需求加载；原始字体 metadata 保留，覆盖源字库 8497 个码位。

## 工程和业务验证

- 2026-09-18：60 个测试文件、540 项单元／集成／API 测试全部通过。新增核验与锁色原子性、94 个同网络游客、Cookie 恢复、创建响应丢失、重启恢复、306＋94 满容量、非学生不计分与越权拒绝。
- 正式重置覆盖数据库归档与校验、备份和事务失败、暂停要求、并发入场屏障、重复操作、丢失响应后的同请求重试、服务重启及旧轮次请求隔离。schema 22→23 测试保留 COMPLETED 状态和既有事实。
- 类型检查、正式构建、部署清单检查通过。生产启动 smoke 使用临时合成 PROTECTED 数据，确认正式启动、回环接口和 Secure / HttpOnly 会话行为。
- Chromium / Chrome / Edge 合计 85 个不同执行用例通过，2 个视觉基线按配置仅在 Chromium 执行；控台、入场与重置最后另复测 12 项。范围包括 25 项走通、半屏按钮、字体实际来源、四档礼物、透明叠层、手机中间帧与中断恢复。
- 重置 UI 遇到服务器明确拒绝的版本过期请求后，清除该次失败请求键；未知网络结果仍保留原键以安全恢复回执。三浏览器均验证拒绝后重新确认只归档一次。
- 手机重置衔接在首轮浏览器检查中发现遗漏：原片尾页停止实时连接。现改为继续只读订阅，已验证结束→归档重置→入场表单→新轮次重新核验。
- 浏览器实际字体检测返回 `FZZH-FangXianTiS Light` 和本地 `Welcome Sans SC`（Noto 字体）。手机小屏、模拟键盘收缩、减少动态效果和缺字兜底由浏览器检查覆盖；不将模拟视口等同微信真机。

## 素材体积

当前正式应用主体不含对稿媒体为 12,081,270 字节，D-108 为 12,337,157 字节。新增方线体分片后，已退出运行的局部宋体及旧展示字体不再进入正式构建；历史源素材继续保留。

方线体总计 996,624 字节，其中核心 48,636 字节；所有构建字体合计 10,259,736 字节，按字符范围加载。合成身份入场样本实际请求手机 Noto 字体 693,912 字节，未请求舞台字体；具体姓名和页面内容会改变追加分片数量，此数不是任意手机的固定首屏下载量。

## 现场边界

全部压力身份、获奖样片与录制实例为合成数据。实际学生名单、工作口令和后台口令不进入代码、公开网页或离线包。真实微信输入法、调音台接线、NFC 写卡与场馆 LED 的现场结果另行记录。

## OBS 与对稿材料实测

2026-09-18，使用独立 OBS 测试场景与生成的 256 学生＋50 工作人员＋94 游客，1080p / 60fps 连续采样 1801.61 秒。1140 笔礼物全部成功，覆盖四档、周期性集中送礼与音乐联动。采样期间未推流、未录屏，也未并行构建或运行浏览器回归。

| OBS 采样项 | D-108 基线 | D-109 |
| --- | ---: | ---: |
| 区间渲染帧数 | 108,100 | 108,096 |
| 区间新增渲染丢帧 | 0 | 0 |
| 平均帧渲染耗时 | 0.224 ms | 0.233 ms |
| P95 帧渲染耗时 | 0.333 ms | 0.321 ms |
| 采样最低帧率 | 60.00 | 60.00 |
| 页面脚本错误 | 0 | 0 |

两个版本在同机、相同分辨率、受控音源和送礼节奏下分时采样；数据表示 OBS 合成渲染表现，不代表微信或 LED 实机验收。使用独立测试配置，结束后恢复原晚会场景集合与配置。启动阶段曾发现本地根路径产物与部署前缀不符，正式采样前已按 `/welcomeparty/` 重新构建并确认实际大屏与音量响应。

主持动画全部重新录制为本版：35 段，包含 25 项目录对应画面、12 秒开场、165 秒原速片尾、手机一屏入场、个人流星、2.4 秒过渡和四档礼物。移除开头加载等待，未加速节目动效或片尾。音乐段在采样结束后另行录制，含真实受控 WAV 声音、强弱变化、透明层关键帧参照、音乐中送星轨及停止后的回落；不标为真实调音台实录。

发布包只收录索引引用的录像、压缩预览图和操作文档。原始 PNG 截图及原始录制仍保留在本机审阅目录，不重复放入离线包。

离线索引在 1440×1000 与 390×844 验证 35 个入口，音乐、完整片尾与手机转场均完成解码定位；页面无横向溢出。音乐实录时长约 38.65 秒，立体声音轨存在，检测平均音量 -20.9 dB、峰值 -12.0 dB。发布前文本隐私检查覆盖 309 个受保护值，未发现真实口令或学号进入变更和公开材料。
