# D-112 临场全流程测试记录

## 测试范围

- 测试时间：2026-09-18
- 公网只读检查：当前 D-112 release、正式测试轮次第 2 轮
- 业务写入检查：隔离的 300 个合成身份实例；不写入公网真实测试轮次
- OBS：本机只读输入检查，不播放、不暂停、不切场

## 通过项

- 手机 390×844：统一背景、入场表单、星色确认、会话恢复入口可见
- 大屏 1920×1080：晚会标题和副标题可见，旧“星海集结”公共文案不存在，WebSocket 保持连接
- 后台 960×900：主控登录、实时连接、节目／互动／颁奖／弹幕／管理页签和归档入口可达
- 隔离浏览器流程：入场 → 开始活动 → 报幕主题 → 互动二投票 → 互动三线下 → 送礼 → 直接结束 → “感谢你的参与”
- 互动二：2 位选手确认、投票、揭晓、收起结果
- 互动三：显示线下互动标题，不出现抢答按钮
- 互动一服务层：OBS `mix` 开始后 10 秒倒计时、抢答锁定、判错续播、同一人再次抢答、音频结束后手动收题
- OBS 三组 `mix` 输入：`b2-eason`、`r2-jj`、`r3-gem` 均可识别
- 数据：schema 24、resetEpoch 2、READY、25 项节目、306 个正式身份容量保持不变

## 自动化结果

- `pnpm test`：61 个测试文件、544 个测试全部通过
- `pnpm typecheck`：通过
- `pnpm build`：通过
- `pnpm deploy:check`：通过
- `pnpm docs:check`：通过，94 个 Markdown 文件、510 个链接
- `pnpm obs:audio --check`：通过
- 服务器服务：应用、Nginx、原站点均 active，应用 `NRestarts=0`

## 未替代的现场确认

- 微信真机、NFC 写卡、真实调音台音源、场馆 LED 和 1080p/60fps 长时间运行仍需明天现场确认。
- 历史 D-109 浏览器脚本仍有“进入节目”“星海集结”等旧选择器；它们不作为 D-112 失败依据。当前流程使用本记录中的 D-112 顺序复验。

## 数据边界

本次没有执行公网开始、入场、送礼、投票、结束或归档重置；公网轮次、余额、名单和历史记录保持原状。
