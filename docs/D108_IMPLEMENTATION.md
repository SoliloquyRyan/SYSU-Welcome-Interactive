# D-108：统一入场、现场流程简化与电影式谢幕

## 当前授权与恢复点

2026-09-17 用户确认实施 D-108，并授权配套更新 GitHub、服务器和主持人对稿材料。
D-107 源码基线为 `bb313d484cb353d87f66915e39d708ebfcd48b26`，开始实施时工作区干净。
恢复归档位于项目外 `.welcome-recovery/d108/d107-bb313d4-source.zip`，SHA-256：
`4a9b5afe7a0de4e042872d5eeed2c486505e7205fcbdf0952e74a82bb4b0fa9c`。

## 已确认规则

- 正式学生仅使用最新名单「总分班情况」的 256 条姓名、8 位学号；增加 50 个独立工作人员账号。名单、口令及派生数据存放在项目外受保护目录。
- 工作人员初始 100 动力，只参与弹幕、礼物与个人记录，不计入正式节目动力、排行与评奖，不参加投票和抢答。权限由服务器判定。
- 容量升至 400；300 人合成排练目录保留。正式运行库独立创建，初始 READY，不覆盖旧库。
- 互动一保留抢答；互动二由主控设置 2～12 个独立选手标签后开放投票；互动三线下进行。
- 25 项节目、19 项表演不变；《光年之外》演出信息改为查无此队及完整成员，继续禁礼物。
- 六类校园奖项先导入未确认草稿，逐项确认、揭晓；摄影奖三人均颁奖。
- 主控从节目阶段确认结束，即刻锁定业务并播放约 165 秒电影片尾；恢复、减少动态效果直接显示终章定格。
- 报幕显示「2026迎新晚会」和「以星光作序，与未来相逢」，暂停送礼。两端报幕、节目观众星平时隐藏，礼物确认后局部短闪。
- 放大加粗标题、补齐方线体字形；手机保持 Noto Sans SC。手机开场衔接 2.4 秒，大屏弹幕限定上方三分之一、4 轨。
- schema 22 与新版代码配套交付；恢复时保留新库、新记录，配套切回 D-107 与旧排练配置。

## 验证记录

### 本地工程与业务

- 2026-09-17：58 个测试文件、531 项自动测试通过。包含 schema 21→22 保留升级、强制中途失败回滚、旧记录可读、306 / 400 人容量、同名核验、工作人员扣费但不计分、越权拒绝和独立选手投票。
- 构建与类型检查通过；正式前端使用 `PROTECTED` 配置和 `/welcomeparty/` 部署前缀。部署清单与文档链接检查通过。
- Chromium、Chrome、Edge：61 个实际执行用例通过；2 个视觉基线用例按配置仅在 Chromium 执行。最初 3 个手机转场用例在锁色入场动画未结束时切场，修正测试时序后全部通过；最终标题分组、手机衔接额外 6 项回归通过。
- 三端回归覆盖手动选手投票、互动三服务端拒绝抢答、报幕送礼限制、奖项揭晓、四档礼物、字体实际加载、超长文字、暂停 / 刷新 / 重连 / 减少动态效果及终章锁定。
- 正式资料只在外部受保护目录保存。提交候选、前端产物和公开对稿索引检查未发现学生学号、工作人员口令、后台口令或运行密钥。

### 正式资料核对

- 仅「总分班情况」256 人；学号无重复，同名组 1 组，通过学号区分。
- 50 个工作人员独立账号；正式总身份 306，容量 400。工作口令不进入 GitHub、前端和公开对稿材料。
- 六类校园奖草稿人数依次为 15、5、10、3、3、20，共 56 条奖项记录；摄影、创意原排名和作品名及积分原排名、分数保留。
- 独立正式库 schema 22，LIVE / READY，0 人入场，全部校园奖未确认、未揭晓；统一 HTTPS 入场映射核验通过。

### 主持对稿与实测

- 已完成 35 段 D-108 合成场次实录，包含全部 19 个节目、互动、报幕、四档礼物、手机衔接与完整片尾。片尾以真实时间运行至最终定格，未用加速时钟替代。
- OBS 32.2.1，1920×1080 / 60fps：2026-09-17 15:26:58 开始独立采样，持续 1801.68 秒；400 个生成的测试身份，受控循环 WAV，每 2 秒一次送礼、每 30 秒叠加 4 笔，共 1,140 笔全部成功。采样期间未运行构建或浏览器回归。
- 采样区间渲染 108,100 帧，新增渲染丢帧 0；平均渲染耗时采样均值 0.224ms、P95 0.333ms，最低采样 FPS 60.00，浏览器错误 0。设置 / 预热阶段的 3 帧不计入 30 分钟区间。
- 历史 D-104 对照为 300 人、同分辨率 / 帧率 / 送礼节奏，平均渲染采样 0.432ms、P95 0.533ms、新增丢帧 25。人数和系统状态不同，不据此宣称单项性能提升。`GetStats` 的渲染均值及其 P95 不是单帧延迟分布，也不是 LED 端到端时延。
- 原始记录：`output/deploy-d108/obs/d108-soak.json`，含逐次采样、浏览器音量事件与错误。该测试使用合成身份和受控音源，不连接正式库、不直播。
- 新增 38.75 秒带声音 OBS 实录，使用当前节目目录，覆盖安静、轻柔、较强节奏、透明层、音乐中星轨及停止回落。音轨核对：安静 -91dB、轻柔平均 -36.9dB、较强节奏平均 -18.5dB；确认音轨并非静音占位。
- 正式构建的手机页通过统一表单恢复第 400 个生成身份（工作人员），背景投影 400 人、工作人员权限提示及余额 100 均正常。截图保存在 `output/deploy-d108/obs/music-current/phone-400.png`；真实正式库仍保持 0 入场。
- OBS 测试后已恢复「迎新晚会·最新版」场景集合和「未命名」配置，停止测试录制与桥接程序。
- 微信 iOS / Android、真实调音台接线和场馆 LED 仍需现场联排。

### 产物体积与恢复

- 前端不含主持录像与离线包：D-107 为 12,349,174 字节；D-108 为 12,337,157 字节，未新增动画运行时依赖。
- 35 段对稿视频共 19,816,298 字节；包含截图封面、口令与节目索引的离线包为 66,568,852 字节，完整服务器发布包为 156,130,260 字节。媒体位于独立目录，手机和控台首屏不请求录像或离线包。
- 2026-09-17 16:12 公网验收通过，D-108 已发布为 `20260917-d108-faf816d0cf47`。正式库为 LIVE / READY、306 个身份、0 人入场；六类校园奖共 56 条记录均未确认、未揭晓。
- 819 项候选文件逐项哈希一致；公网手机、大屏、后台和对稿页通过，正式管理员登录与 Secure / HttpOnly 会话正常。35 段公网视频均可访问，片尾和音乐实录 Range 返回 206。离线文件页面在 1440px 与 390px 通过播放加载、定位和无页面横向溢出检查。
- 公开产物核对全部 308 个私有凭据值（306 个登录号码、管理员密码和运行密钥），未发现泄漏；离线包没有身份表、数据库或口令清单。
- 本地 D-107 源码归档见上文。服务器保留 D-107 release 与 schema 21 排练库；停服期间创建一致性备份，原库 52 张表逐项保持。仅重启本项目服务，其他项目及 Nginx 进程与配置保持。
- 发布及恢复采用代码与配置配套切换。保留新正式库及之后产生的新记录，不让 D-107 读取 schema 22。
- 发布附件、文件哈希、入口与完整恢复索引见 [D-108 发布与恢复](./D108_RELEASE_AND_RECOVERY.md)。
