# D-102：代码与产物清理审计

2026-09-15，负责人要求审计并删除不必要代码，避免项目包过重，同时保留重要版本供参考。

## 基线与授权范围

从干净的 feature/screen-six-stage-experience / fb45d4c 开始。该提交业务代码等同服务器 D-100 的 e80db41。本轮仅本地清理，不提交、推送、发布或改变运行数据。

## 审计结论

- 清理前 Git 跟踪 409 份文件，共 15,651,757 字节；其中 frontend/src 共 10,766,134 字节。
- 本地 output 共 3,029,297,861 字节，主要为历史录屏、媒体审查和验收产物；它已被 Git 忽略，不属于网页构建内容。
- 当前前端仍依据协议能力选择 v1/v2 页面。旧协议、数据库迁移、兼容契约和现有回归测试有实际作用，全部保留。
- dependency / import 搜索没有证明可整体删除的第三方包。未改变任何 package.json、锁文件或运行依赖版本。

## 本次清理

1. 将无页面引用的早期 mobile-galaxy-renderer.js 和 12 份退役美术/来源文件移到 [历史参考目录](./archive/visual-assets/README.md)，合计 9,715,248 字节。保留完整原文件与 SHA-256，测试仍验证归档纯函数；生产源码目录不再混入历史素材。
2. 删除无任何引用的 hero.png 模板残留，以及无调用的 STELLAR_ART 常量。已备份原文件，Git 发布基线也保留。
3. cinema 预览的来源清单改为当前实际使用的 starship-nocturne.svg；原银白星舰仍在归档可查。
4. 删除 13 份完全相同的自动命名录像副本，保留可读名称副本；删除前更新相关 index.html / manifest.json 引用并备份原索引。释放 264,484,304 字节。跨目录且有历史指标引用的一份录像保留。
5. 删除无入站依赖引用的 frontend/node_modules/.pnpm 旧独立安装，共 61,564,008 字节；其 199 个链接先单独移除，根级有效依赖保留。旧安装元数据单独备份后清理。

归档移动不宣称减少全仓磁盘体积；重复录像/缓存删除才释放空间。退役素材在清理前已被 Vite 排除，因此不把源码目录减重宣称为手机网络下载提速。

## 保留的重要版本与证据

- Git 提交历史、全部数据库迁移与协议契约。
- D-100 当前发布包、D-095 与其他旧 release、发布清单及备份。
- 既有银河各版本、前端 preview、原始图像与生成来源、唯一录像、命名截图与验收报告。
- 刚恢复前备份的其他 agent 改动；本地/服务器数据库与 OBS 素材和配置。

## 验证

- pnpm build、pnpm typecheck、pnpm typecheck:e2e：通过。
- pnpm test：54 个文件 / 495 项全部通过。
- pnpm deploy:check 与 pnpm docs:check：通过。
- 历史 cinema 预览重新构建成功，468,782 字节。
- Chromium 三端、入场视觉和实时互动三份既有测试共 6 项全部通过，无失败、无跳过；覆盖生命周期、抢答/抽人投票/礼物/弹幕/谢幕及静态与中断恢复。
- 隔离对照构建的全部 10 份文件哈希复现清理前结果。清理后的主 JS 仅删除未引用颜色常量的 86 字节初始化，HTML 仅更新主 JS 哈希名；其余 8 份文件完全一致，包括 CSS、图片、字体、Vue 与 GSAP。
- 13 份归档文件和 13 份保留录像 SHA-256 复核通过；验收索引指向现存副本。
- git diff --check：通过。当前变更尚未提交、推送或部署；真机、OBS 和现场验证未重跑。

## 减重口径

运行源码 frontend/src 从 10,766,134 降至 1,037,711 字节，减少 90.36%；其中 9,715,248 字节移入历史归档，仍在仓库，不能算作全仓空间释放。

删除的重复录像、旧依赖缓存、旧安装元数据和模板合计 326,092,944 字节。此处为移除文件的逻辑大小，未计 NTFS 分配/硬链接差异，也未扣除新建审计文档、备份和验证产物。网页主 JS 仅减少 86 字节，不代表手机访问显著提速。

## 本机恢复与审计证据

工作区 output/code-cleanup-20260915（项目目录外）保存源文件清单、tracked-before.zip、媒体索引备份、重复文件删除→保留映射和缓存清理记录。

从归档恢复文件前核对原路径和哈希；重复录像可从映射中的保留文件复制回原位置。不要直接覆盖后续工作。删除的旧依赖缓存是可再生成产物，项目使用根级 pnpm install --frozen-lockfile 管理有效依赖。
