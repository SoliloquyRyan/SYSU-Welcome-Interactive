# D-087：D-086 发布与全流程审查

2026-09-09。负责人在认可 D-086 后明确要求更新服务器，同时检查整个流程。当前 checkout 为 `feature/screen-six-stage-experience` / `6bc38222a15b8b9f7d1eb3745729eed5ad582c0c`，存在此前未提交修改，完整保留。本轮运行代码仅发布已认可的 D-086 前端，不改后端业务、活动记录和其他站点。

## 已发布

- 地址：`https://sysuzgxytj.top/welcomeparty/`；release：`/opt/sysu-welcome-internal/releases/20260909-d086-22255b85640d`。
- 前端归档 SHA-256：`22255b85640dbee5d5abae7c5542dbdaaf80e02e4d9710bc329aacb81bfdac89`，403,494 字节、9 个静态文件。16 个审片来源文件与 D-086 冻结预览哈希一致；合并 release 清单 430 项，旧版与新增文件逐项校验。
- 原子切换 `current`，没有停服、数据库迁移或重置。后台 PID 3697176、旧晚会项目 PID 1387993、Nginx PID 1286、3000 端口监听与 Nginx 配置保持；根站 200。9 个公网静态文件内容哈希与发布包匹配。
- `resetEpoch=8`、`publicSeq=37`、`REHEARSAL / RUNNING / PROGRAM_SUPPORT`、`runRevision=15`、`presentation=NONE` 保持。只读查询确认 21 个启用节目、当前第 2 项、礼物 1/5/10/20。公开大屏快照不含完整目录，不能把缺失 `programs` 字段误判为目录为空。
- 旧版 `/opt/sysu-welcome-internal/releases/20260908-d075-bae0599aaf81` 完整保留。若仅回退本次前端，核对 `current` 仍指向本次 release 后原子切回旧版即可，不恢复数据库、不重启其他服务。

发布证据：`output/deploy-d086-flow/release.json`、`deployment-checks.json`、`live-readonly-smoke.json`。线上探针只访问页面和公开接口，不登录参与者/管理员，不推进场景。

发布后公网 `/welcome`、`/screen`、`/admin` 均为 200、Vue 完成挂载，无页面异常或失败请求。未登录手机/后台各一次预期 401；普通节目大屏中心 alpha 为 255，间隔 900ms 的 Canvas 摘要变化，确认线上背景在运行。该结果不代表已在实体 OBS 中复验。

## 验证结果与修正

- 全量单元、API、集成测试：**462/462 通过**。首轮 461 通过、1 失败，原因是旧单测仍强制要求已退役的 `nebula-master.webp`；删除该过时资产断言，保留共享渲染器、隐私与完成契约检查后全量复验通过。
- Chromium 首轮 8 项，6 通过、2 失败。节目目录用例仍把首次 1 动力礼物后的余额写成 100，修正为 99 后，导入、并发目录冲突、串场禁礼与重启持久化完整复验通过。连抽用例首轮失败，未修改其业务或测试逻辑，隔离复验通过；另在 Chrome 与 Edge 复验连抽、刷新恢复、静态协同和一次结束确认均通过。首轮失败保留，不宣称一次运行全绿；短暂动效断言的偶发性仍应在联排关注。
- 其余已通过：正常首次入场及布局稳定；320/375/390 和横屏操作可达；字体失败回退；礼物数量、个人赠礼与弹幕档案；现场完整生命周期；无 GSAP 情况的动态/静态弹幕；转场中断和排练终章不修改完成状态。
- 后端构建、E2E 类型检查、部署静态检查、`/welcomeparty/` 前端生产构建通过。浏览器记录分别为 `browser-first-pass.json`、`browser-recheck.json`、`browser-chrome-edge.json`，截图在 `output/deploy-d086-flow/browser/`。
- 修正主控文档入口，新增 [现行主控操作单](./LIVE_OPERATOR_GUIDE.md)，把旧内测单标为历史，避免 8.4 秒/12 秒、默认透明/星海底图、免费首礼/完整扣费、待开始/实际运行中这些口径混用。

实体手机、OBS 实机、场馆网络、LED 亮度与长时运行未在本轮代验。全流程写入测试使用临时合成数据库，没有在服务器上从头重跑或清空现有场次。

## 后续修改优先级

| 优先级 | 发现与影响 | 建议修改 |
|---|---|---|
| 1 | **演职员数据与运行节目目录脱节**：`program-credits.js` 按标题精确匹配本地 JSON，目录编辑只有标题、类型、形式、时长；改节目名会丢失表演者。`ClosingCredits.vue` 直接遍历固定 JSON，后台重新排序或移除节目不会同步片尾 | 把表演者作为按稳定节目 ID 维护的目录字段；手机、报幕与片尾共用实际目录。涉及契约/后端维护，另行实施，不混入本次视觉补丁 |
| 1 | **片尾未完成整体美术统一**：实际画面仍以蓝色光晕、四格统计和右侧字幕为主，与已认可的黑色深空银河有差别；字幕每 100 秒无限循环，缺少明确的最终停留镜头 | 沿用 D-086 背景，按“协同余光—今晚节目/互动回忆—致谢—定格”编排，统计收为次要信息。字幕播一轮后停留；照片/视频待提供后接入 |
| 1 | **静态片尾内容被截断**：字幕容器 84vh、`overflow:hidden`，减少动态仅取消动画并去掉顶空，后续演职员仍在可见范围之外 | 静态模式用完整可读的分栏名单或分页，不依赖滚动动画才能看到后半段；保持真正静态 |
| 1 | **现场主控须先定编排**：21 项中未定义抽奖轮次、奖项、名额、领奖方式，也未给协同/结束语/OBS 切片尾的统一口令。现场推进不可回退，完成立即禁写 | 主控按现行操作单联排；每项用“下一项”，只在最后节目结束后推进协同，先点亮后结束；补一张含奖项与口令的 cue 表 |
| 2 | **抽奖功能已有，但产品信息不足**：连抽、无放回、记录及刷新恢复均有；当前主要是星号滚动、轨道装饰和中奖历史，没有奖项/轮次/领奖确认字段 | 先补奖项和轮次信息、主持人逐位确认，再与 D-086 统一背景和揭晓动效。是否允许同一人跨轮中奖须明确，当前整场不重复 |
| 2 | **动力预算固定且共享**：每人 100 动力，送礼不返动力，渐变弹幕每种解锁扣 10。18 个表演各送 5 动力需 90，三种渐变另需 30；偏好高档礼物的观众会提前花完 | 先在手机说明余额用途和本场总预算，给出明确不足提示。若需要增加发放/额度，另定规则；不恢复用户已取消的免费礼物机制 |
| 2 | **操作单旧记录容易被当现状**：交接表此前落后到 D-074，旧测试单仍称待开始、8.4 秒且默认全透明 | 本轮已补当前发布记录和主控入口；后续历史段落保持明确日期，现场只使用新版操作单 |
| 2 | **联排仍缺设备证据**：浏览器像素运动与流程通过不等于 OBS/LED/手机长时稳定；连抽短动效断言也出现过一次波动 | 用实际 OBS 浏览器源与实体手机跑完整节目→抽奖→协同→结束，再做至少一次持续联排，记录连接、帧率、温升和投屏亮度 |
| 延期 | 第 7/14 项只有串场占位，无线上猜歌/答题；第 20 项竞拍按负责人此前决定延期 | 维持线下主持执行。后续沟通后再加玩法，不把抽奖或动力扣费当竞拍 |

以上为检查结论与建议，不表示已实现新业务或已获新规则确认。银河 D-086 保持为已认可视觉基线。
