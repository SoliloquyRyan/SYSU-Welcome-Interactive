# D-119 最终现场修复

## 用户最新决定

- 游客“核验学生身份”仅在手机“档案”页提供；现场聊天页不再显示该入口。
- 执行本对话全部修正并部署；不重置数据库、现场轮次或已有名单。

## 修复

- 手机聊天区域统一纵向排列、全宽输入；身份核验使用独立可滚动表单，可返回档案。
- 修复首次实时连接取消入场流星的问题；大屏到场人数只在尚未执行任何节目时显示，报幕后刷新不会重新出现。
- 互动页将主控、节目选择、答案、音频和互动按钮放在同一滚动区域；避免卡片各占全屏和隐藏重复操作。触控目标至少 44px。
- 颁奖后台恢复两个颁奖流程入口，并随当前奖项同步选中项。
- 公共 stage 数据在服务端只保留姓名、排名及空 detail，内部备注留在管理端；分页只按姓名长度决定。
- 保留 D-118 的 HOST cue 到 `00 开场与集结`、节目按唯一 programId 映射、失败拒绝猜测、播放开始同步 10 秒抢答倒数。

## 原始文件核对

- 节目单 DOCX SHA-256 与 `event-program-2026.json` 来源一致。
- 校园最终榜单 SHA-256：`336bcd4facf9bfa26bbd5a7a9dabcbcff823445672b8d9dcf2cd3656e4110a27`。
- 积分前 20、三条路线、两条路线、一条路线、创意、摄影人数分别 20/15/5/10/3/3；逐姓名与顺序和线上一致。
- 将积分前 20 从草稿重新确认为正式名单，其他已确认名单保留；揭晓仍由主控操作。

## 验证与发布

- `tests/e2e/d119-final-live.spec.ts` 使用合成名单独立数据库，验证四种手机视口、错误/成功核验、弹幕发送、入场流星、节目后人数隐藏、互动主控、20 人颁奖三页及刷新、公共数据不含备注。
- 集成测试覆盖 HOST cue、返回节目 cue、获奖数据隐私；既有 OBS 映射和音频测试复用。
- 正式子路径构建，清单逐文件哈希验证、保留数据库、原子切换版本；实际 release/hash 记录在部署证据 `output/deploy-d119/bundle.json`。
- 发布后刷新 OBS 两个浏览器源并读取真实输出截图；已连接物理屏幕的清晰度仍需现场目视确认。
