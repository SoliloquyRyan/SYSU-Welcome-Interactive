# D-037 OBS、局域网与 vivo X300 人工签核表

> 当前状态：`PENDING`。D-037 取消寄语/时光胶囊并增加节目中场个人抽奖，因此 2026-08-15 的 V2-10 / D-036 `PASS` 只是变更前历史证据，不能代签本表。只有项目负责人在实际设备和 OBS 显示链上的操作结果可以关闭此门；桌面自动化、截图或 AI 观察不构成签核。

> 数据边界：只使用 `pnpm preview:v2:field` 建立的 OS 临时 `V2_ACTIVE` 合成库；不读取、升级、切换或重置 `backend/.data/`，不记录真实姓名、学号、邀请令牌、后台口令或正式活动内容。

## 1. 执行元数据

- 执行日期与时间：`PENDING`
- 执行人：`PENDING`
- Git HEAD / 工作树标识：`PENDING`
- Windows、Chrome/Edge、OBS 版本：`PENDING`
- vivo X300 系统、默认浏览器、CSS 视口与 DPR：`PENDING`
- `prefers-reduced-motion` 实际值：`PENDING`
- 局域网类型与本机私网 IPv4（不记录 Wi-Fi 密码）：`PENDING`
- 开始/结束时间：`PENDING`

## 2. 安全启动

1. 确认电脑与 vivo X300 在同一可信私网，关闭 VPN/代理，确认没有真实参与者数据。
2. 在 PowerShell 运行：

   ```powershell
   $env:DEMO_HOST = '192.168.x.x'
   pnpm preview:v2:field
   ```

3. 确认启动器拒绝公网、虚拟接口和 `0.0.0.0`，不自动改防火墙；手机扫码后地址栏令牌立即清除。
4. 正常动效首次镜头使用本次临时栈的新鲜邀请和无旧会话浏览器；同一身份的刷新/返回只能验证静态恢复。
5. 结束后按 `Ctrl+C` 或关闭预览浏览器，确认临时数据库和端口已清理，实际 `backend/.data/` 未变。

## 3. 三端局域网旅程

逐项填写 `PASS`、`FAIL` 或 `BLOCKED`；失败时只记录匿名现象、复现步骤和恢复方式。可设置 `$env:DEMO_FIELD_CHECKLIST='1'` 后运行预览命令，用终端检查单辅助记录。

| 编号 | 检查项 | 结果 | 证据/备注 |
|---|---|---|---|
| F01–F05 | 新鲜邀请的约 5.4s 寻星、恒星连续性、控件交接、切后台中断和 reduced-motion/刷新静态终态 | `PENDING` | |
| F06–F08 | 锁色后约 1.0s 闪烁直接衔接约 4.2s 入轨；三端无寄语/胶囊入口；READY 无旧任务 | `PENDING` | |
| F09–F11 | ASSEMBLY 收敛；`START_STAR` 首次 40 星光且晚到不补奖；PROGRAM_SUPPORT 节目三端一致 | `PENDING` | |
| F12–F15 | 抽奖状态/角色门禁、无放回、后台合成姓名+公开星号、Screen 只显示公开星号、恢复/自动收屏/记录保留与排练清空边界 | `PENDING` | |
| F16–F20 | 软键盘、礼物面板、弹幕/礼物匿名上屏、Wi-Fi 断线禁写/恢复、刷新/返回不重播 | `PENDING` | |
| F21–F24 | 协同点亮/终章、COMPLETED 只读、退出确认/草稿清除与连续操作稳定性 | `PENDING` | |

## 4. OBS 实际合成

| 编号 | 检查项 | 结果 | 证据/备注 |
|---|---|---|---|
| O01 | Browser Source 透明 alpha 与节目视频真实合成正确 | `PENDING` | |
| O02 | 中心安全区、边缘星点、标题、弹幕、礼物与抽奖在实际显示链可读 | `PENDING` | |
| O03 | 浏览器源无网页音频，节目音视频只由 OBS 控制 | `PENDING` | |
| O04 | 抽奖/故障/场景/互动/终章优先级符合协议；大屏无合成姓名或其他私密字段 | `PENDING` | |
| O05 | 连续切场、隐藏/显示源、全屏预览无黑底闪烁或残帧 | `PENDING` | |

## 5. 关闭判定

- 任一 S0/S1、真实数据泄漏、写入越权、抽奖重复/私密字段上屏、OBS 中心不透明、手机核心操作不可达、断线后错误提交或临时数据未清理，均判 `FAIL`。
- 最终结论：`PENDING`
- 项目负责人签核：`PENDING`
