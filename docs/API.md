# API 边界草案

本文只描述预期能力，不代表接口已经冻结。

## P0 能力

- 身份激活与会话状态。
- 个人动力值查询与应援提交。
- 寄语提交和审核状态。
- 大屏聚合状态。
- 管理员阶段触发和降级切换。

## 基本约束

- 所有写操作必须验证身份、限频并具备重复提交保护。
- 大屏只读取聚合数据和已审核的匿名内容。
- 管理接口必须单独鉴权并记录操作日志。
- 接口不得返回完整学号、名单或不必要的身份字段。
- 正式接口、错误码和数据模型由后端方案确认后另行评审。
