# D-086 四周轻微尘埃涌动

**发布（2026-09-09）**：负责人随后授权更新服务器，已随 D-087 发布至 `/welcomeparty/`。发布与完整流程审查见 [D-087 记录](./D087_DEPLOYMENT_FLOW_REVIEW.md)；下方保留本地制作与认可时的验证记录。

**负责人认可（2026-09-09）**：负责人审看 `http://127.0.0.1:8957/preview/index.html` 后明确“这版可以了”。D-086 的整体银河、背景层次及轻微尘动作为后续视觉调整基线。本次仅记录认可，不修改渲染代码；发布状态仍为本地，实体设备及现场验证范围保持下文记录。

2026-09-09，负责人认可 D-085 后要求周围增加不明显的尘埃涌动氛围。本轮保留既有背景构图、配色与细碎远星，让外围遮光薄尘随低速流场轻微散聚，中央银灰银河继续作为主体。

- 背景缓存仍只生成一次；在外围叠入缓慢游移的稀薄遮光层。各处浓淡变化不同步，不整体平移、旋转背景或移动背景星点。透明度最多 14%，通常远低于上限，不新增亮光或彩色色斑。
- 尘埃与主银河分别计算，中央区域遮罩为零。仅调用所属手机/公屏渲染器已有时间，不新增 RAF、计时器、监听器或依赖。第 0 秒严格保留 D-085 静帧；暂停使用冻结时间，减少动态继续静止。
- WebGL 使用已有噪声纹理；Canvas 回退使用横竖屏各一个 128px 遮罩，最高 12Hz 更新，避免每帧重新生成完整背景。人数、身份、星色、星体尺寸、转场结果及 OBS 透明交接规则保持。
- 本条替代 D-085 关于正常模式背景始终静止的限制，保留其美术层次与静态模式规则。

## 验证

- 前端生产构建、E2E 类型检查通过；4 个相关单元测试文件共 49 项通过。
- `output/galaxy-d086/visual-audit.json` 的 12 项浏览器检查通过，包括手机动态、暂停/恢复、减少动态、Canvas 回退、人数与装饰分离、节目星体与 OBS 透明终点；无页面异常。已查看手机、大屏与背景截图。
- `dust-check.json` 在横竖屏、WebGL/Canvas 四种组合中确认外围在 30 秒后变化，中央区域不变，相同时间重复绘制及归零完全稳定。外围 RGB 平均绝对差约 0.26–0.30（8 位色阶），最大单通道差 4–5；相邻 1/12 秒平均差约 0.0024–0.0028，控制了变化强度。这些是截图像素测量，不是实体屏幕亮度或现场美术验收。
- 尘埃函数在多个时间点（包括第 4 小时采样）检查透明度上限、中心保护和帧间连续性。离散时间采样不代表真实 4 小时运行验证。
- 冻结动态预览、30 秒运行样片与来源一致性检查分别记录在 `preview-check.json`、`final-check.json`。实体手机、OBS、现场 LED、长期运行和业务 E2E 本轮未重验。

本地审片入口：`output/galaxy-d086/preview/index.html`。源码备份：`output/backups/d086-before/`。后续服务器发布与回滚按上方 D-087 记录；未提交或推送 Git。
