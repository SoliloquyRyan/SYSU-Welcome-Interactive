# Demo v0 G0～G4 提交绑定验收记录（D-022）

> 状态：`PASS`（提交绑定冻结基线，2026-08-10）
> 被验收提交：`63e3d5094c49a5635f4ebf7ccbd0c9da07b7d58a`
> Git tree：`1cf870688e5fc13231988b6aa3eec7afc70de5da`
> 分支：`feature/demo-v0`
> 范围：本地 Demo v0 的 G0～G4 网页、服务、共享契约、测试与运行文档
> 重要说明：本记录绑定上面的代码提交；包含本记录的后续文档提交不是被验收提交，也不替代上面的完整 SHA。

## 1. 冻结结论

D-021 保留 2026-08-10 对提交前工作树的人工与自动化总验收事实，但不绑定任何提交。项目负责人随后明确授权创建 G0～G4 冻结候选提交，并形成：

- 提交：`63e3d5094c49a5635f4ebf7ccbd0c9da07b7d58a`；
- tree：`1cf870688e5fc13231988b6aa3eec7afc70de5da`；
- 提交时间：`2026-08-10T12:55:10+08:00`；
- 标题：`feat(demo): 完成 G0-G4 可操作网页 Demo 冻结候选`。

提交前，120 个候选路径完成范围、隐私与差异审查；运行数据库、凭据、测试报告、构建产物、依赖目录、真实环境文件、日志、证书、密钥、备份和临时文件均未进入提交。提交形成后，先确认 HEAD 为上述完整 SHA 且工作树干净，再实际执行根级 `pnpm verify:g4`。命令以退出码 0 完成，随后再次确认工作树干净、测试临时目录清除且端口释放。

因此，上述提交可在 D-006～D-018 定义的本地 Demo v0 范围内称为“已通过提交绑定复验的 G0～G4 冻结基线”。这不是发布批准，也不扩大 D-017、D-018 和本文第 8 节的延期边界。

## 2. 根级门禁证据

在被验收提交的干净工作树执行：

```text
pnpm verify:g4
```

实际结果：

| 项目 | 结果 |
|---|---|
| 根命令退出码 | `0` |
| 观察总时长 | 约 `511.7 秒` |
| Vitest | 20 个测试文件、119 条测试，全部通过 |
| 共享契约构建与类型检查 | 通过 |
| 后端构建与类型检查 | 通过 |
| 前端生产构建 | 通过 |
| G3 Playwright | 9 个唯一场景 × 3 个浏览器项目，共 27/27 |
| G4 协议负载 | `status=passed` |

复验开始前与结束后均满足：

- HEAD 为 `63e3d5094c49a5635f4ebf7ccbd0c9da07b7d58a`；
- tracked、staged 和非忽略 untracked 记录均为 0；
- 没有推送，也没有创建 PR。

## 3. G3 浏览器证据

被忽略的本地脱敏报告：`tests/reports/g3-browser.json`

- SHA-256：`2312A917C69B1388E8EF51DD6E00857DF80FCED3B8165D62E1AF7CC99290ADE1`；
- 状态：`passed`；
- 时间：`2026-08-10T05:02:40.242Z` 至 `2026-08-10T05:07:41.688Z`；
- 报告窗口：`301446 ms`；
- 环境：Windows `10.0.26100` x64、Node.js `24.16.0`、pnpm `11.16.0`、Playwright `1.62.1`。

| 浏览器项目 | 版本 | 结果 |
|---|---|---:|
| `chromium-ci` | Chromium `151.0.7922.34` | 9/9 |
| `chrome` | Chrome `151.0.7922.76` | 9/9 |
| `msedge` | Edge `151.0.4129.72` | 9/9 |

共 9 个唯一场景、27 次浏览器执行，全部通过。D-021 中由项目负责人完成的 vivo X300 默认浏览器人工旅程仍作为同一 G0～G4 功能基线的人类验收依据；其浏览器独立版本号和实际 CSS 视口未单独采集的限制继续保留。

## 4. G4 协议负载证据

被忽略的本地脱敏报告：`tests/reports/g4-load.json`

- SHA-256：`179756B87B29973F4554876C9C3B75470A5D29A6335328A930133F3A88A6446D`；
- 状态：`passed`；
- 时间：`2026-08-10T05:07:45.598Z` 至 `2026-08-10T05:10:01.281Z`；
- 负载时长：`135684.5 ms`；
- `applicationVersion=0.1.0`、`protocolVersion=1`、`seedVersion=demo-v0-g1-v1`；
- Windows loopback HTTP/WebSocket；
- 300 个固定合成参与者、301 条目标 WebSocket；
- 主要 HTTP 写入并发为 24；
- 目标传播阈值为 p95 不超过 `2000 ms`。

| 操作 | 尝试/成功/失败 | p50 (ms) | p95 (ms) | max (ms) |
|---|---:|---:|---:|---:|
| 激活 | 300/300/0 | 160.63 | 253.43 | 265.48 |
| 私密寄语 | 300/300/0 | 1296.63 | 1519.65 | 1525.25 |
| 阶段 | 20/20/0 | 44.47 | 50.57 | 105.68 |
| 星星 | 300/300/0 | 1356.73 | 1428.96 | 1430.57 |
| 礼物（四档合计） | 1200/1200/0 | 1316.06 | 1434.59 | 1503.57 |
| 微光 5 | 300/300/0 | 1323.24 | 1434.30 | 1436.20 |
| 信标 10 | 300/300/0 | 1363.74 | 1501.20 | 1503.57 |
| 星轨 20 | 300/300/0 | 1284.23 | 1394.97 | 1396.29 |
| 星舰 50 | 300/300/0 | 1270.13 | 1367.97 | 1369.02 |
| 弹幕 | 300/300/0 | 1513.73 | 1664.35 | 1670.97 |
| 协同点亮 | 300/300/0 | 892.32 | 1121.22 | 1127.39 |

全部目标操作成功率为 100%，四类核心传播以及四档礼物各自的 p95 均低于 2 秒。

## 5. 实时、恢复与业务不变量

- WebSocket 目标、初始打开、服务重启断开和恢复打开均为 301；
- 大屏原始帧 7228、参与者原始帧 1988400、协议错误 0；
- 20 个阶段主样本全部被 300 个参与端观察，6000 次已提交事件观测成功、0 次失败；
- 2 个重启阶段样本全部被 300 个参与端观察；
- 快照请求 2848 次，其中参与者运行态广播额外快照为 0；
- 5 次幂等重放均未改变持久状态；
- 数据库重启、300 份参与者快照、后台快照、大屏快照、重置事件、旧参与者会话拒绝、旧 epoch 重同步和固定凭据再激活全部通过。

数据库不变量全部通过：

- 300 份参与者状态、3000 条账本、1200 份礼物、300 条已发布弹幕、300 次协同点亮；
- 总剩余动力 4500、总星光 30000、节目热度 25500；
- 错误参与者状态、错奖、重复账本、重复命令、重复幂等记录、未匹配或孤立扣减、负余额、越界星光和旧 epoch 行均为 0；
- `failures={}`，`topLevelFailure=null`。

## 6. 清理与隐私

报告与独立复核共同确认：

- `temporaryDirectoryRemoved=true`、`portReleased=true`；
- 用户临时目录与系统临时目录中的 `sysu-welcome-e2e-*`、`sysu-welcome-g4-*` 和 `sysu-welcome-playwright-output*` 均为 0；
- TCP 3000、5173 监听均为 0；
- G4 报告不包含凭据、显示姓名、私密寄语正文或弹幕正文，`auditPassed=true`；
- 两份报告的额外高置信扫描未发现私钥、供应商令牌、JWT、邮箱、手机号、身份证号、凭据赋值、带凭据查询参数或本机绝对路径；
- 两份报告继续由 Git 忽略，不属于版本交付物。

## 7. 证据解释

两份 JSON 报告本身没有嵌入 Git HEAD、tree 或源码摘要。本记录通过以下连续证据把它们绑定到被验收提交：

1. 运行前确认完整 HEAD 与干净工作树；
2. 在该工作树执行根级 `pnpm verify:g4`；
3. 根命令退出码为 0，并在同一连续运行中生成上述报告；
4. 记录报告 SHA-256；
5. 运行后再次确认完整 HEAD、干净工作树、临时目录和端口清理状态。

报告哈希证明本次记录引用的本地报告内容未被混淆，但报告仍不进入 Git。根级 Vitest、构建和命令退出码来自本次实际控制台证据，不能只由两份 JSON 推导。提交与 tree 可以重新检出源码，但系统 Chrome/Edge 版本会随环境变化，vivo 人工证据也未记录独立浏览器版本和 CSS 视口，因此 D-022 不承诺未来任意环境能得到完全相同的时长或报告哈希。被验收提交中十份状态文档条件式使用的“可复现冻结版本”，自 D-022 起统一解释为“G0～G4 提交绑定冻结基线”，不表示跨环境复现承诺、发布批准、G5 完成或正式环境验收。CI 已配置但尚无远端运行证据，因此 D-022 不引用 CI。

## 8. 明确不在冻结范围内

D-022 不证明或批准以下事项：

- G5 Orbital Signal 三端视觉、动效和邀请函统一；
- 30 分钟 soak、300 次写请求同一瞬间并发、分布式真实 LAN 或场馆网络；
- 公网 HTTPS、生产托管、监控、备份、灾备和正式安全审计；
- 实体 NFC 写卡、二维码印刷、短网址、打样和到货验收；
- 真实名单、真实个人数据、外部 AI 审核、正式节目数据和学院审批；
- iPhone、微信内置浏览器、现场灯光、音视频 Cue 和硬件联排；
- 推送、PR、合并或正式发布。

## 9. 版本与后续变更规则

- G0～G4 提交绑定冻结基线的权威提交是 `63e3d5094c49a5635f4ebf7ccbd0c9da07b7d58a`；
- 保存 D-022 的下一次本地文档提交必须以 `63e3d5094c49a5635f4ebf7ccbd0c9da07b7d58a` 为唯一直接父提交，且只包含 `docs/DECISIONS.md` 与 `docs/ACCEPTANCE_G0_G4_D022.md`；该证据封装提交不改变被验收 tree，也不成为新的被验收提交；
- 若保存 D-022 前 HEAD 不再是上述完整 SHA，或暂存范围包含上述两份文档以外的路径，则不得按本记录提交，必须停止并重新审查；
- 自该证据封装提交之后，任何改变被验收源码 tree 的业务代码、共享契约、迁移、依赖、构建配置或测试基线变化，或任何改变 D-022 绑定 SHA/tree、验收范围、结论或证据链含义的文档变化，都必须形成新的冻结候选，重新执行适用门禁，并以新的决策和验收记录绑定；不得沿用 D-022；
- G5 应从已记录的冻结版本继续，但不得把视觉实现描述成 D-022 已验收范围；
- 本次授权只允许把上述两份治理文档作为 `63e3d5094c49a5635f4ebf7ccbd0c9da07b7d58a` 的直接子提交保存在本地；不授权其他文件变化、推送、PR、合并或发布。
