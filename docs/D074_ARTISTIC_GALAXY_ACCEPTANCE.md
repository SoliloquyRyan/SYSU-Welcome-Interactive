# D-074 艺术化星河与暗部层次

2026-09-07。负责人反馈 D-073 照片式银河显得虚假、与星体不融合且背景过黑，要求尝试艺术化。工作树为 `feature/screen-six-stage-experience`，HEAD `6bc38222a15b8b9f7d1eb3745729eed5ad582c0c`，已有未提交修改全部保留；本轮修改前备份位于 `output/backups/d074-before`。

## 本轮设计

- 手机主银河改为代码绘制的极光细丝、点描星尘和暖白光心，青色与淡紫色在外盘交融。停止加载 D-073 写实图片，旧原图与 WebP 留档。此次没有重新生成图片或添加运行依赖。
- 手机光带与本人星体、轨道尘埃使用同一中心、倾斜、缩放和时间轴旋转，消除照片平面与动态星体之间的几何割裂。光度纹理只生成一次并缓存，手机继续使用 Canvas2D 和 DPR 上限 2。
- 大屏七层光场改用相同色系和流线形态，光场的基础旋转周期与星体一致；小亮核与较宽的淡色光晕取代坚硬白圆点，让星体的光融入环境。真实 formationSlot、锁定星色和数量不变。
- 大小屏抬高深靛蓝暗部，加入宽幅青紫环境光；手机边缘遮罩从最暗处 64% 调低至 18%，取消大面积压黑。文字和消息区保留足够对比，字号、布局和操作方式不变。
- Canvas 兼容/静态路径同样使用艺术光场，避免故障或减少动态时回到旧照片画风。开场仍为 12 秒，星舰仍为手机 5.8 秒、公屏 7.2 秒；OBS `media=overlay` 节目稳态仍透明并停止绘制。D-073 已入场人数规则与全部 schema 17 业务保持。

## 验证

- 44 项银河、镜头、个人旅程和视觉系统单元测试通过；E2E 类型检查、前端 `/welcomeparty/` 构建和文档检查通过。
- Chromium 6 项三端与布局回归全部通过，报告 `output/galaxy-d074/browser-chromium.json`。覆盖完整生命周期、到场计数、聊天/礼物、OBS 透明稳态、静态弹幕、缺少 GSAP、转场中断、排练片尾、抽奖队列，以及 320/375/390、横屏、模拟键盘空间、字体失败和个人纪念页。
- 手机正常状态在 800ms 间隔内 Canvas 像素发生变化，减少动态状态两帧完全一致；背景取样为 RGBA `[35,54,83,255]`，不是纯黑。艺术画面不请求旧银河/云雾图片。实际截图位于 `actual`、`mobile`；手动检查开场 46%、90% 和 100% 帧，终点保持新画风。预览源哈希与当前实现一致，记录见 `output/galaxy-d074/visual-checks.json`。
- 预览 `output/galaxy-d074/preview/index.html` 使用正式组件和合成视觉数据，手机与大屏可切换。旧 D-073 预览与实拍完整保留供对照。
- 实体手机、OBS、投影黑位/亮度和长时温升仍需现场确认。

## 发布

前端 10 文件补丁已部署到 `/opt/sysu-welcome-internal/releases/20260907-d074-c59863c4026a`，SHA-256 `c59863c4026af81f3c423c3f1f33b50554d8ae5317fa3d7e2ac2da7bf67b85ab`，合并 release 清单 425 项。原版本与补丁逐项哈希通过；公网页面和 JS 哈希匹配。后端 PID 3697176、其他站点 PID 1387993、Nginx PID 1286 均保持，Nginx 配置哈希不变，根站 200。epoch 8 / publicSeq 37 / REHEARSAL / RUNNING / PROGRAM_SUPPORT / runRevision 15 / presentation NONE 保持。

本轮无数据库迁移、重置或后台重启。D-073 `/opt/sysu-welcome-internal/releases/20260907-d073-23adf3214539` 保留，回退只原子切回 current，不恢复数据库。

发布后三个公网入口均 200、Vue 正常渲染，无页面异常或请求失败；未登录手机/后台各一次预期 401。线上公屏为 cinematic-depth-field，环境目标 30Hz、中心 alpha 255。报告 `output/galaxy-d074/live-readonly-smoke.json`；该只读检查没有改变线上节目场景，入场、抽奖和结束行为由本轮隔离浏览器测试验证。
