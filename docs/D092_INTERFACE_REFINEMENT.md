# D-092：现场界面收敛与互动节奏

2026-09-11，按负责人本轮指令实施。覆盖 D-090 星舰材质、D-089 抢答开放方式和礼物点击即发送、D-032 星光展示及此前积分累积行为。保留动力和历史账目。

- 开场交接后，大屏使用深色舞台底图，不继续展示中央银河；集结、协同与谢幕保留各自场景。
- 星舰改为暗色细线舰体、克制冷光与星尘尾迹；每个节目最多两次完整特效，由服务器按礼物批次决定，后续照常记账和显示礼物值。
- 星光入口移除，新增互动不再增加星光。既有值不删除；零增量事件保留兼容审计链。
- 抢答和投票使用与系统一致的紧凑面板，置于手机聊天记录之后、弹幕输入之前；开始抢答先显示服务端统一三秒倒计时，服务端拒绝提前抢答。
- 礼物流程改为选礼物、选数量、发送。节目礼物计数随新赠送出现弹跳反馈，减少动态时保持静态。
- 弹幕色板为八种（移除独立星紫选项，保留我的星色与三个高级款）；聊天气泡更薄、更紧凑。
- 精简重复引导语，确认弹窗使用返回与确定，保留价格、余额、不可撤销等必要信息。

当前为本地实现并验证，未发布服务器，未迁移或清空正式库。

## 验证

- `pnpm test`：50 个文件、469 项全部通过。新增每节目两个星舰批次上限、跨节目重新计数、三秒内拒绝抢答覆盖；更新取消新增星光后的账目预期。
- `pnpm build`、`pnpm typecheck:e2e`、`pnpm docs:check`、`git diff --check` 通过。
- 独立合成数据库中完成手机、主控、大屏联动：选择礼物不扣费，发送才扣费；前两个星舰播放，第三个只累加礼物值；色板八项；高级款返回不扣费，确定扣费并发送。
- 抢答倒计时期间直接提交返回 409，倒计时结束才能锁定；抽取两位合成观众并完成投票和结果揭晓。
- 320×568 手机视口下标题、互动卡、输入框和导航不重叠，无横向溢出；主控确认支持 Esc 返回与确定提交；减少动态模式下星舰没有运行中的动画。
- 截图、星舰录屏和机器可读检查记录位于 `output/playwright/d092-refinement/`，均来自本地合成数据。预览入口 `index.html`，录屏 `video/starship.webm`。

本轮浏览器覆盖 320/375/390 像素手机模拟视口、1920×1080 大屏和主控；未宣称真实手机、现场投影设备或正式服务器已验收。
