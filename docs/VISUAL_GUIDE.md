# 三端视觉与邀请函指南

> D-099（2026-09-13，本地完成，未发布）：精修手机/主控/大屏共用面板与操作反馈，修复登录错误、弹幕确认焦点、谢幕全量回顾和撤下内容重现；独立合成场次已执行完整流程，495项测试及三浏览器各10项流程共30项全部通过；44张截图与关键录屏已整理。schema 21保持。见 [D-099](./D099_UI_AND_FULL_FLOW.md)。

> D-098（2026-09-13，本地完成，未发布）：手机节目超新星改为热核、云气喷流与碎光余辉；节目/主持/颁奖舞台加入学院标志与层叠银蓝光带。16份媒体已审查，建议 OBS 本地播放并叠加系统互动，节目对应差异待确认。schema 21及现有业务保持。见 [D-098](./D098_STAGE_CRAFT_AND_MEDIA.md)。

> D-097（2026-09-12，本地完成，未发布）：节目颁奖自动取有效动力值前三名，后台可调整节目动力值并保留原始礼物与调整记录；收紧独立环节间距，手机与大屏采用统一银蓝节目舞台，手机实时开场增加一次可跳过的超新星转场。代码目标 schema 21，线上仍以 D-095 / schema 19 为准。见 [D-097](./D097_PROGRAM_RANKING_AND_STAGE.md)。

> D-096（2026-09-12，本地完成）：新增节目颁奖、负责人讲话与校园图鉴颁奖；目录共 25 项，互动/颁奖/讲话使用无字母编号的独立分隔行，校园图鉴在手机节目单只显示标题；《光年之外》关闭礼物，主控支持黑金主持背景、草稿名单、确认揭晓和分页。代码目标 schema 20，线上仍为 D-095 / schema 19；未部署或复位线上场次。验证和操作见 [D-096](./D096_AWARDS_AND_STAGE.md)。

> D-090（2026-09-11，本地实现，未发布）：星舰采用原创银白金属双舷舰体、香槟金细节和独立双引擎，近景展示后加速跃迁；公屏及手机共用贴图与组件，静态直接呈现舰体和数量。覆盖 D-089 星舰具体美术，现行银河、礼物账目及总时长保持。见 [星舰重设计](./D090_STARSHIP_REDESIGN.md)。

> D-088（2026-09-10，本地实现，未发布）：表演者按节目 ID 纳入后台目录，同步手机、报幕和片尾；片尾沿用 D-086 银河，字幕只播一轮后定格，静态名单可翻页。维护为 schema 17→18，保留场次和互动记录。实现与验收见 [演职员目录与银河片尾](./D088_CREDITS_ACCEPTANCE.md)。线上仍以 D-087 发布记录为准。

> D-087（2026-09-09，已发布）：当前银河美术以负责人认可的 D-086 为基线，手机与大屏已更新服务器。片尾、抽奖仍需进一步统一，发现与优先级见 [全流程审查](./D087_DEPLOYMENT_FLOW_REVIEW.md)；本次没有自行重设计这些环节。

> D-086（2026-09-09，本地轻微尘动）：按负责人要求，四周薄尘以低强度缓慢游移散聚，保留 D-085 配色、层次和中央银河。两端复用原时钟，暂停及静态模式保持；未发布服务器。验证见 [D-086 记录](./D086_SUBTLE_DUST_MOTION.md)。

> D-085（2026-09-09，本地远景层次）：按负责人要求，在 D-084 柔和背景上补充细碎远星尘、小片星团及被近处暗尘遮挡的薄雾，维持既有配色与白边强度。无参与者或业务变化；未发布服务器。验证见 [D-085 记录](./D085_BACKGROUND_DEPTH.md)。

> D-084（2026-09-09，本地四周尘云）：负责人要求背景更深、增加银白轮廓，并延伸到四周。以气体云、暗尘埃和局部受光边缘呈现；按后续“喧宾夺主”反馈降低雾团与白边对比。横竖屏分别取景，中央动态银河保持。未发布服务器；验证见 [D-084 记录](./D084_SURROUNDING_DUST_CLOUDS.md)。

> D-083（2026-09-09，本地背景柔化）：按负责人要求，将 D-082 突兀的纯色色斑淡化，改为疏密不一、边缘不规则的薄雾远景。维持黑色主调及背景与银灰动态星云的分层。未发布服务器；验证见 [D-083 记录](./D083_SOFT_BACKGROUND_ART.md)。

> D-082（2026-09-09，本地纠正）：负责人澄清只要求背景显色，流动星云仍是独立主体。纠正 D-081 彩色远景与银河旋臂重叠的观感，将颜色移至外围深空并柔化纹理，主星云保持银灰、暖白与原有运动。未发布服务器；验证见 [D-082 记录](./D082_BACKGROUND_SEPARATION.md)。

> D-081（2026-09-09，本地显色调整）：负责人反馈 D-080 仍太浅，将局部紫色、冷青和暖金增强至清楚可见。保留 D-079 黑色底调与既有色彩分布；仅调整三处材质颜色，无业务变更。未发布服务器；验证见 [D-081 记录](./D081_VISIBLE_NEBULA_COLOR.md)。

> D-080（2026-09-09，本地微调）：按负责人批注，在 D-079 黑色底调上稍增强局部暗紫、冷青和暖金。仅校准共享背景材质三处色彩强度，保持原有分布、银河与星体。未发布服务器；验证见 [D-080 记录](./D080_ACCENT_COLOR_ACCEPTANCE.md)。

> D-079（本地黑色深空配色 / 审片）：负责人明确要求黑色为主体，局部点缀深空颜色。背景改为中性炭黑、墨黑与灰银云气，暗紫/冷青/暖金仅局部出现；主星云适度去蓝。覆盖 D-078 的大面积墨蓝配色，星色、尺度、人数与散聚动效保持。未发布服务器；实现与验证见 [D-079 记录](./D079_BLACK_SPACE_ACCEPTANCE.md)。

> D-078（2026-09-08，本地背景精修 / 审片）：保留 D-077 已认可银河、星色、手机尺度与云层聚散，增加低对比远景云气、细碎星尘和银蓝/暖金层次。两端与静态回退共用缓存程序材质，参与者星体和人数语义保持。本轮未发布服务器；实现与验证见 [D-078 记录](./D078_REFINED_SKY_ACCEPTANCE.md)。

> D-077（2026-09-08，本地精修 / 审片）：延续 D-076 已认可构图，强化大屏各人已选星色，手机轨道个人星缩小约 29%，星云沿原旋臂局部消散后重新凝聚，两端背景暗部抬亮。身份、人数、静态与 OBS 透明交接规则保持。本轮未发布服务器；实现与验证见 [D-077 记录](./D077_CLOUD_AND_STAR_ACCEPTANCE.md)。

> D-076（2026-09-08，本地实现 / 审片）：以原版倾斜盘面和留白为基础，统一手机与大屏的深墨蓝、银蓝及暖白光色。背景、五层流动星云与稳定星体使用共享路径和取景模型；大屏恢复左下标题，保留真实入场人数。替代本地 D-075 柔光图片美术，业务与 12 秒开场等现行时长保持。本轮未发布服务器；最近既有发布记录仍见 D-075。具体实现、审片与验证见 [D-076 记录](./D076_FLOWING_GALAXY_ACCEPTANCE.md)。

> D-075（既有发布记录；本地美术由 D-076 替代）：采用柔光星河，中央宽幅银蓝/珍珠暖光、边缘暗，背景降低细节反差以衬托动态星体。手机与大屏共享有界运镜、前后景视差和取景，集结标题位于左上暗部；覆盖 D-073 / D-074 具体美术，人数、身份、业务、12 秒开场、静态与 OBS 透明契约保持。实现与最终验证见 [D-075 验收](./D075_SOFT_STAR_RIVER_ACCEPTANCE.md)。

> D-072（2026-09-07）：当前美术以 [电影感修订与验收](./D072_CINEMATIC_ACCEPTANCE.md) 为准，覆盖下文旧 8.4 秒开场和 D-071 星舰美术。开场现为 12 秒；星舰手机 5.8 秒、公屏 7.2 秒；普通节目背景使用持续流动的正式银河，只有 `media=overlay` 保持透明并停止绘制。手机内容页去整面模糊，档案收紧布局。修复同一连接中快照先于礼物消息导致的漏播，状态与计数不回退。schema 17、四档礼物、身份、审核和抽奖规则保持。

> D-070：手机常用页面改为内容优先布局，取消导航标题重复逐字，保留首次入场 5.4s/1.0s/4.2s 与 C 字体。节目名称/表演者为主标题、列表与档案使用扩展正文；键盘态隐藏导航与选色，输入独立保留。礼物四宫格、44px 色块点击区、抽奖提示、协同进度和个人纪念卡已接入；业务与数据库不变。
> 状态：D-037/D-051/D-053/D-055 现行视觉规范。D-030 的约 5.4s/1.0s/4.2s 手机视觉语言与 D-031/D-032 信息架构继续作为基线，但寄语停留已移除，锁色确认后直接衔接入轨；节目中场新增个人抽奖层。D-055 将正式大屏满场参考改为 220 人，把集结态收束为盘面离散星主导的低倾角恒星盘、420 颗无身份中性底星、连续未解析盘面光的有界密度补偿和环境层克制呼吸；每颗实时新星以一次画外流星沿切线进入稳定轨道。D-048 让等待态保持 30Hz、流星和进入节目高速段以 60Hz 为目标并预热有界 WebGL2；D-051 整体退役黑洞路线，改用错峰恒星坍缩、极小高温核心、一次非对称超新星和连续白场透明接管；D-053 让三端共用 Orbital Signal 语义表面、状态、字体和小圆角，同时保持手机个人触控、大屏电影叙事与后台高密度运营三种强度。协议与状态以 [PROTOCOL_V2.md](./PROTOCOL_V2.md) 为单一权威；D-036 及更早证据只覆盖变更前版本。
> 适用范围：`/welcome` 手机端、`/screen` 舞台大屏端、`/admin` 管理端，以及 A6 横版邀请函首轮预览。

## 1. 目标与边界

Issue #5 的旧令牌和组件继续作为基础设施；D-037 将手机端收敛为“身份激活与选色 → 锁色直接准入 → 流动星系中的当前现场场景”，并在 `/admin`/`/screen` 增加中场个人抽奖。D-030 的同一恒星镜头、D-031 的稳定流动星系和学院入口、D-032 的本机字体回退、唯一顶部逐字标题、非对称微角操作坞与本人私有档案交互继续生效。D-046/D-047 建立的逆时针真实星流、摄影式逐星材质、0 人底星和逐人画外流星仍是基础；D-055 用盘面离散星、连续未解析盘面光、420 颗更小更暗的底星和人数有界环境补偿替换显式恒星棒/旋臂构图，并让环境层以非同步低振幅变化形成呼吸、流星沿目标轨道切线完成捕获。D-048 保留约 8.4 秒单镜头及 60Hz 目标、首帧前管线预热与有界后处理；D-051 将其叙事重排为由当前星流连续出现引力预兆，真实星和底星按不同曲率与到达时序螺旋汇聚，压缩成极小高温核心，再经过一次非对称超新星与电影曝光把 Canvas 平滑交给透明 OBS 节目源。D-049/D-050 则继续约束跨端低饱和色谱与手机背景。早期 Issue #5 的占位范围不得再限制当前大屏质感路线。视觉仍必须服务于进入简单、状态一致、公共内容可处置、故障可恢复和数据最小化。D-032/D-036 的既有自动与人工结果不代签 D-037/D-051/D-055；按 D-052，当前入场须在至少一台代表实际上线访问方式的实体智能手机上签核，品牌、型号和操作系统不作阻塞限制，三端抽奖和 OBS 仍须按实际结果另行记录。

现行边界为：

- 建立颜色、字体、间距、圆角、组件和动效令牌；
- 提供 `BaseButton`、`BaseCard`、`StatusPill` 的共享表现规则；
- 三页继续复用公共组件，但 `/screen` 可以在现有协议和视觉优先级内完整实现 300 星现场动画；
- 环境动效必须可暂停、可降级，页面隐藏和 reduced-motion 直接收敛；
- D-051 的错峰螺旋汇聚、非对称超新星与白场透明接管，D-048 的 30Hz 等待态/60Hz 目标高速段/预热有界后处理，以及 D-055 的盘面主导银河、420 底星、环境呼吸和逐人流星轨道捕获，都在唯一可见 Canvas 内实现；只允许一个 DOM 外原生 WebGL2 后处理 Canvas 和 Canvas2D 回退；这些授权不等于允许逐星 DOM、逐星 GSAP 或动画驱动业务状态；
- 不新增登录、身份核验、积分、审核、节目切换、实时连接或数据写入逻辑；
- 当前不新增大型动画库，不复制去年项目或授权不明的字体、图片、代码和素材；若实时路线经实际显示链仍不足，预渲染视频必须按 D-043 另行完成权利、编码、预载、OBS 与恢复验收。

入口、公开展示和隐私边界以 [PROJECT_CONTEXT.md](./PROJECT_CONTEXT.md)、[REQUIREMENTS.md](./REQUIREMENTS.md)、[DATA_PRIVACY.md](./DATA_PRIVACY.md) 和 [RUNBOOK.md](./RUNBOOK.md) 为准。视觉方向只有在负责人确认并记录到 [DECISIONS.md](./DECISIONS.md) 后，才可视为当前开发基线。

## 2. 设计原则

### 2.1 三端同源、密度有别

- **同源**：三端使用同一套语义颜色、字号阶梯、间距、圆角、焦点样式和状态词。
- **手机端清楚**：单列优先，触控目标足够大，说明简短，不出现横向滚动。
- **大屏端远距可读**：核心数字和状态优先，避免细线、过小文字、滚动区域和高频闪动。
- **管理端可判断**：状态、权限和危险操作必须明确；不能用氛围效果弱化异常信息。
- **降级仍完整**：关闭动画、断开实时链路或切换静态背景后，标题、状态和操作仍可理解。

### 2.2 语义优先于装饰

- 颜色必须表达语义，而不是仅用于“更炫”；同一状态在三端保持同一色义。
- 所有状态都必须有可见文字；图标和颜色只能辅助，不能单独承担信息。
- 动力值与星光值必须使用不同名称、图标、颜色和说明，不得只靠数值区分。
- 大屏只表现聚合、礼物事件、规则通过的已发布弹幕，以及抽奖中奖公开星号；不为姓名、完整学号、内部身份映射、联系方式或入口令牌预留展示位。

### 2.3 原创且可追溯

- 第一版优先使用 CSS、SVG 基础图形和系统字体，不下载来历不明的视觉资源。
- 未取得书面授权前，不使用学院标志图形、去年项目资产、商业字体、图库图片或第三方角色形象。
- 如后续引入外部素材，必须记录名称、来源链接、作者、许可证、允许用途和修改情况。
- D-031 是一项窄范围例外：项目负责人提供并授权白色透明学院 PNG 仅用于 Demo `/welcome` 左上入口；原图保持本地打包、不修改，来源为项目负责人于 2026-08-14 提供，SHA-256 为 `5E665D84994508A6A5BF106EF87A22E0CC443EA63634B5384A310EF3A498C008`，点击目标为学院官方站点 `https://ise.sysu.edu.cn/`。该例外不扩张到其他页面、正式矢量母版、品牌色或线下物料。

### 2.4 D-025 / D-030 / D-031 / D-032 手机端构图

D-058 实现稿在上述基线上统一标题与内容左轴，扩大场景标题，操作坞保留顶部细边和本人星色短线，减轻完整外框；主操作使用星白与深空底色。光谱轨道从既有色温函数生成，手柄颜色与同一颗持久恒星同步，不改变服务端星色映射；Kelvin、44px 触控高度、键盘和值描述继续保留。正常已连接不再占据状态行，离线、重连、暂停、失败仍清楚显示。高透操作区不可用时回退实色面板；短屏由操作区内部滚动，导航固定可达，不靠裁切底部按钮解决溢出。

完成后的手机主视图改为私有纪念页：本人姓名、星号、已锁定星色，接着是实际完成的抵达/启动/首次礼物/首次弹幕/协同片段，再给出告别与档案入口；尚未完成的项目不显示为已参与。未锁色者必须说明星色尚未确认，不能画出虚构的入场回忆。详细动力与星光仍在档案。此稿尚待负责人审美反馈；没有改变舞台终章的内容或时长，也不代表大屏电影片尾完成。

- 目标视口约 390×844，页面外壳不产生纵向滚动；安全区、短视口和软键盘必须纳入布局计算。
- NFC 或备用核验成功即完成身份激活并进入选色；星色锁定事务直接准入并进入服务端当前现场场景。
- 主页面中央是流动星系，不再显示六阶段直线进度。协议技术上最多 300 颗真实恒星，正式目录与正式视觉满场参考为 220 人；真实星使用服务端稳定位置，自己的恒星突出并在下方显示公开编号，其他恒星不显示标签。
- 每个新视图只保留一个页面级顶部大标题；大标题逐字显现并保留可见光标，下方操作坞只写当前任务的上下文标签，不重复同名标题，也不添加无信息量英文。
- 页面下半部只允许一个克制的高透玻璃操作坞，承载当前场景的主要操作并明显透出星系背景；面板、控件和弹层使用接近直角的非对称微角。禁止玻璃卡片墙、层叠透明面板、通用大圆角和持续动画模糊/滤镜；不支持透明效果时退化为高对比不透明面板。
- 本人档案可以显示私有 `displayName` 和预分配 `personalStarCode`；锁色前的星号不等于公共恒星已存在。姓名不得进入大屏、后台或公共事件，星号只有在锁色后才按既有 `publicStarId` 规则成为公共主键。
- 色温控件必须显示 Kelvin 数值、当前位置和清晰焦点，并向辅助技术暴露完整名称和值；用户选色不设倒计时。
- 正常首次核验成功、服务端明确返回新建身份与 `NEEDS_COLOR` 后，先等待页面稳定 `visible`，再参考 D-030 播放约 5.4 秒沉入、接近与捕获；权威锁色并返回 `ADMITTED` 后，参考约 1.0 秒完成同一恒星色温闪烁并直接衔接约 4.2 秒拉远入轨。中间不显示寄语/胶囊页；正常首次流程不提供动画跳过按钮，手机端不迁移 GSAP。
- 寻星、选色、锁色确认和入轨共用同一颗持久恒星与同一坐标系。正式页与认可预演使用同一生产渲染器，可组合经压缩验收的本地原创星云纹理、前/中/后 Canvas 合成层、局部颗粒和确定性本机轨道尘；不得让 DOM 星消失后再以 Canvas 圆环或另一颗星冒充连续交接。
- 选色和主场景控件可以提前渲染，但在对应权威状态到达前保持 `inert`、不可聚焦和不可操作；视觉时间线只能消费权威事实，不得触发或延迟锁色、准入或实时连接。
- 开播前暂不可见时等待；开播后进入后台或被中断则立即收束到权威静态终态且返回不重播。返回、刷新、断线恢复、失败、超时和 `prefers-reduced-motion: reduce` 也直接显示静态终态。
- 普通成功提示约 3.5 秒后收回并由页面持久状态接替；错误、离线、暂停和已结束提示持续显示。退出每次确认，草稿只存当前内存并按完成、退出、会话重置及场景离开规则清理。
- 正常状态不显示“实时同步”、裸露的 `RUNNING`、六阶段编号或常驻身份/数值行；异常状态保留清楚文字。
- 怀士堂线稿、旧 1.8/0.6 秒可跳过转场和 D-024 的约 0.9 秒首帧可跳过均为历史方向，不进入 v2。

## 3. 设计令牌

令牌定义在 `frontend/src/styles/tokens.css`。新代码使用语义名称；旧页面继续通过兼容别名工作。颜色不得在组件中重复散落为无说明的十六进制值。

### 3.1 颜色

| 基础令牌 | 现行值 | 用途 |
|---|---:|---|
| `--color-orbit-midnight` | `#01030a` | 三端同源的最深空间底色 |
| `--color-orbit-deep` | `#07101f` | 星云亮度结构与纵深过渡 |
| `--color-orbit-signal` | `#427eee` | 低权重技术信号蓝、透镜色散 |
| `--color-orbit-signal-soft` | `#7eb0ff` | 柔和蓝白光与高光过渡 |
| `--color-orbit-cyan` | `#4adbe9` | 克制青色气氛与焦散，不作霓虹主色 |
| `--color-orbit-star` | `#eef4ff` | 中性摄影式星核与空间白光 |
| `--color-orbit-warm` | `#ffd79a` | 暖星与低权重吸积高光 |
| `--color-orbit-surface-1/2/3` | `#050b16 / #091426 / #10213a` | 深空画布、面板与抬升表面 |
| `--color-orbit-text-primary` | `#eef4ff` | 主标题、正文重点 |
| `--color-orbit-text-secondary` | `#b7c6df` | 辅助说明与状态正文 |
| `--color-orbit-text-tertiary` | `#8e9fbd` | 次要元信息 |
| `--color-orbit-border-subtle/strong` | `rgba(126,176,255,.18/.40)` | 分隔线与强调边界 |
| `--color-orbit-success` | `#71d9bd` | 成功、已连接、通过 |
| `--color-orbit-warning` | `#ffd79a` | 暂停、注意、处理中 |
| `--color-orbit-danger` | `#ff8ea2` | 离线、拒绝、危险操作 |
| `--color-orbit-disabled-*` | `#71819e / rgba(92,111,145,.12)` | 可读但不可操作的独立禁用态 |
| `--color-orbit-focus` | `#9bc8ff` | 键盘焦点轮廓，不与警告色混用 |

使用规则：

- 共享组件只消费 `--color-*` 语义角色；各路由可把这些角色映射到上述 Orbital Signal 基础令牌，不在组件内复制页面主题。
- `/welcome` 和 `/screen` 由各自沉浸容器直接消费 Orbital Signal；`/admin` 在 `.route-admin` 显式映射深空画布、面板、控件和状态角色。旧页面根级纸张/油墨值只作兼容，不是三端 v2 的目标外观。
- 主操作使用品牌蓝；危险操作只能使用危险色，不以品牌色伪装。
- 焦点使用独立柔蓝；暖白只承担注意/暂停和暖色星光，均须配合文字或图标。
- Orbital Signal 色谱只建立跨端空间气氛，不覆盖参与者的服务端 `displayColor`；Canvas/WebGL 通过 `frontend/src/styles/orbital-signal.js` 消费与 CSS 同值的颜色源。
- 手机用约 32 KB `nebula-master.webp` 形成低饱和亮度结构，再叠加 Orbital Signal 气氛；不得下发约 1.85 MB 大屏尘埃图，也不得让阶段交接更换背景系统。
- 大屏暗星云、尘埃、汇聚预兆、超新星冲击折射、丝状喷流和白场只低权重取色，禁止整屏高饱和蓝青霓虹化。
- 动力值首轮使用暖金方向，星光值首轮使用青色方向；最终图标和色值待产品负责人确认。
- 邀请函可使用同一冷暖关系，但印刷前必须根据纸张和打样结果重新校色。
- 协议 v2 的 `/welcome` 只在底部操作坞使用半透明表面；其余主体不堆叠玻璃卡片，并必须提供不透明降级色。

### 3.2 字体与字号

**D-060 手机现行组合**：负责人于 2026-09-06 选择 D-059 对照稿 C，并允许优化具体字体选型。`/welcome` 的 v2 页面采用 Noto Sans SC 2.004 可变字体的本地 UI 子集，重命名为 `Welcome Sans SC`，通过同族的字号、字重与字距区分标题、正文、按钮和数字。D-059 像素标题保留为历史探索，应用不采用该样稿；大屏和后台继续使用下方 D-053 系统字体角色。

字体随应用同源提供，当前 WOFF2 为 152876 bytes（约 149 KiB，D-061 固定文案补字后），字重范围 400–700，覆盖固定公开界面文字和本场公开节目标题的 520 个码位。生成器只读取列明的公开源码与节目转录，不读取名单、数据库或用户内容；维护上限为 200 KiB。它不是完整中文字库：姓名、输入和未来文案中的子集外字符仍使用系统回退。`font-display: swap` 保证加载中或加载失败时先显示系统字体，字体加载不参与入场、动画或命令条件。不得依赖用户预装字体，也不使用运行时字体 CDN。

来源、固定版本、校验值、重建方式与 OFL 见 [`字体资源说明`](../frontend/src/assets/fonts/README.md)；生产包保留 [`版权和许可`](../frontend/public/licenses/welcome-sans-sc-OFL.txt)。本地字库仅是 D-060 手机字体选型的一项授权资源，不构成引入其他字体、依赖或远程素材的许可。

| 手机角色 | 字号 / 行高 | 字重与字距 | 用途 |
|---|---|---|---|
| 页面标题 | `clamp(1.8rem, 8.2vw, 2rem) / 1.45`；短屏降至 `1.5rem` | 500，`0.045em` | 唯一顶部任务标题；390px 视口约 32px |
| 正文说明 | `0.875rem / 1.8` | 400，克制字距 | 入场、节目和纪念说明 |
| 辅助标签 | 不低于 `0.75rem` | 通常 400–500 | 状态、元信息和必要操作提示 |
| 节目与纪念主信息 | `1rem / 1.5` 左右 | 500 | 当前节目、个人纪念主信息 |
| 表单输入 | `1rem` | 400 | 中文输入；不继承较小的表单标签字号 |
| 按钮 | 沿用控件字号与 44px 触控门 | 500 | 主次操作 |
| 数值和编号 | 沿用各信息层级 | 同族字体，`tabular-nums` | Kelvin、余额、星号和计数 |

D-053 的全局字体令牌不变；它们仍是另外两端的角色来源，也是手机子集缺字或失败时的渐进回退：

```css
:root {
  --font-family-cjk: "HarmonyOS Sans SC", "OPPO Sans", MiSans, "vivo Sans",
    "Noto Sans SC", "Source Han Sans SC", "PingFang SC", "Microsoft YaHei UI",
    system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --font-family-display: "Cascadia Mono", "Microsoft YaHei UI",
    ui-monospace, monospace;
  --font-family-signal: Bahnschrift, "Aptos Display", "Segoe UI Variable Display",
    Inter, system-ui, sans-serif;
  --font-family-data: Bahnschrift, "Cascadia Mono", "SFMono-Regular", Consolas,
    ui-monospace, monospace;
}
```

共享角色中，`--font-family-display` 供标题和少量信号文字使用；中文正文、按钮和表单使用 `--font-family-cjk`，关键短标签可使用 `--font-family-signal`，编号和数值使用 `--font-family-data`。v2 手机局部将这些用途收敛到 `Welcome Sans SC`，没有修改全局令牌。任一前置字体缺失时必须能落入后续系统字体；D-060 以外仍不得擅自捆绑字库或从去年项目复制字体。

以下是其他端和共享组件的基础字号；v2 手机标题与正文以上表为准：

| 层级 | 建议令牌 | 大小 / 行高 | 用途 |
|---|---|---|---|
| 辅助 | `--font-size-xs` | `0.75rem / 1.5` | 标签、时间、次要元信息 |
| 小号 | `--font-size-sm` | `0.875rem / 1.55` | 状态、按钮辅助文字 |
| 正文 | `--font-size-md` | `1rem / 1.7` | 正文和表单说明 |
| 强调 | `--font-size-lg` | `1.125rem / 1.55` | 面板上下文、重要说明 |
| 小标题 | `--font-size-xl` | `1.5rem / 1.3` | 分区标题 |
| 页面标题 | `--font-size-2xl` | `clamp(2rem, 6vw, 4.7rem) / 1.06` | 大屏、后台等共享标题基础 |

共享正文默认字重 400–500，标签和按钮 600–700，超大数字不超过 700；v2 手机按 D-060 减轻为上表权重。中文正文不使用强制大写或过宽字距；英文只在具有真实语义时保留，短标签可使用有限字距。

`/welcome` 进入新视图时，只让唯一的页面级顶部标题按字符依次显现，并在末尾保留可见光标；“找到属于你的星”也遵守该规则。长标题允许用 `displayTitle` 明确换行，但辅助技术直接获得 `title` 的完整语义。操作坞、档案和弹层不得再重复同一句大标题，只能使用较低层级的上下文标签。字符动画是纯表现：页面隐藏、刷新、重连、失败恢复和 `prefers-reduced-motion: reduce` 直接显示完整静态文本；静态路径的光标不得持续闪烁，标题完成也不能触发、延迟或伪造业务状态。新字体、200% 网页字号与模拟短屏的自动检查不能代签实体手机字体、系统字号和软键盘效果。

### 3.3 间距

采用 4px 基准阶梯：

| 令牌 | 值 | 常见用途 |
|---|---:|---|
| `--space-1` | `4px` | 图标内部微调 |
| `--space-2` | `8px` | 紧凑元素间隔 |
| `--space-3` | `12px` | 标签与正文、紧凑列表 |
| `--space-4` | `16px` | 默认组件内间距 |
| `--space-5` | `20px` | 按钮横向、普通卡片 |
| `--space-6` | `24px` | 内容组间距 |
| `--space-8` | `32px` | 卡片组、区块间距 |
| `--space-10` | `40px` | 大区块间距 |
| `--space-12` | `48px` | 大屏安全区、页面段落 |
| `--space-16` | `64px` | 桌面页面主留白 |

手机端页面左右安全间距首轮为 18px；桌面和大屏内容区首轮为 28px 以上。组件内部只使用上述阶梯，特殊视觉定位须在代码旁说明原因。

### 3.4 圆角、边框与阴影

| 令牌 | 值 | 用途 |
|---|---:|---|
| `--radius-sm` | `2px` | 小按钮、紧凑控件 |
| `--radius-md` | `4px` | 默认按钮、状态胶囊 |
| `--radius-lg` | `8px` | 普通卡片 |
| `--radius-xl` | `12px` | 必须抬升的弹层 |
| `--radius-pill` | `4px` | 短状态标签，不用于大面积容器 |
| `--border-subtle` | `1px solid var(--color-border-subtle)` | 默认边界 |
| `--shadow-panel` | `none` | 默认不靠投影堆层级；弹层按页面局部定义 |

上表是 D-053 的共享代码权威；D-032 的 `/welcome` 操作区继续使用接近直角的非对称微角令牌：

| 手机微角令牌 | 值 | 用途 |
|---|---:|---|
| `--shape-panel` | `8px 2px 8px 2px` | 唯一底部操作坞 |
| `--shape-control` | `6px 2px 6px 2px` | 按钮与表单控件 |
| `--shape-item` | `7px 2px 7px 2px` | 档案项与礼物项 |
| `--shape-sheet` | `12px 3px 12px 3px` | 必须浮起的底部弹层 |

微角用于轻微切开轮廓，不做夸张剪角，也不缩小内容、焦点轮廓或 44×44px 触控目标。`/welcome` 的操作坞以高透明表面、细边缘信号和轻量模糊透出星系；模糊不可用时使用可读的不透明降级色。胶囊形只用于真正的短状态标签，不用于普通按钮或大面积容器；退出操作采用无普通描边的文字/图标与科技括角提示，仍必须提供可见焦点。圆角用于建立共享语言，不用于掩盖层级；管理端危险确认、错误框和数据表必须保持清晰边界。阴影只用于区分层级，不制造高亮文字周围的大面积光晕。

## 4. 旧 CSS 变量兼容

现有页面仍使用以下变量。第一版不得删除或改变其语义；`tokens.css` 应将旧名设为新语义令牌的别名。

| 旧变量 | 新语义令牌 | 兼容要求 |
|---|---|---|
| `--ink` | `--color-text-primary` | 保留，继续表示主文字色 |
| `--muted` | `--color-text-secondary` | 保留，继续表示次要文字色 |
| `--line` | `--color-border-subtle` | 保留，继续表示弱边框 |
| `--panel` | `--color-surface-panel` | 保留，继续表示通用面板 |
| `--blue` | `--color-brand-primary` | 保留，继续表示品牌蓝 |
| `--cyan` | `--color-brand-cyan` | 保留，继续表示信标青 |
| `--gold` | `--color-accent-warm` | 保留，继续表示暖色强调与焦点 |

实现形式示例：

```css
:root {
  --color-text-primary: #eaf2ff;
  --color-text-secondary: #9fb0cc;
  --color-border-subtle: rgba(149, 178, 225, 0.2);
  --color-surface-panel: rgba(11, 28, 55, 0.78);
  --color-brand-primary: #5c89ff;
  --color-brand-cyan: #61d9ff;
  --color-accent-warm: #ffd071;

  --ink: var(--color-text-primary);
  --muted: var(--color-text-secondary);
  --line: var(--color-border-subtle);
  --panel: var(--color-surface-panel);
  --blue: var(--color-brand-primary);
  --cyan: var(--color-brand-cyan);
  --gold: var(--color-accent-warm);
}
```

加载顺序应为 `tokens.css` → `motion.css` → 原有 `style.css`。新组件只使用新语义令牌；旧选择器可以继续使用旧变量。只有在后续单独 Issue 完成全量迁移和回归检查后，才可考虑移除旧别名。

## 5. 公共组件

### 5.1 `BaseButton`

第一版用于原生按钮，不承担路由链接多态和业务权限判断。

- 变体：`primary`、`secondary`、`ghost`、`danger`。
- 尺寸：`sm`、`md`、`lg`；默认 `md`。
- 默认触控尺寸不小于 44 × 44px；手机端主操作可占满容器宽度。
- 必须透传原生 `type`、`disabled`、`aria-*` 和事件。
- `disabled` 不可点击且保留可读标签；不能只用降低透明度模拟禁用。
- 加载状态保留原标签并增加忙碌标记，设置 `aria-busy`；本 Issue 仅做视觉能力，不发起请求。
- 键盘 `:focus-visible` 使用 3px 暖金色轮廓和 3px 外偏移，不得移除。
- 危险按钮必须写明动作，例如“清屏”，不能只显示无语义图标。

### 5.2 `BaseCard`

- 默认是普通信息容器，可通过 `as` 保留 `div`、`article`、`section` 或 `ul` 等正确语义。
- 变体：`glass`、`solid`、`outline`；不得把所有卡片都做成强光和重阴影。
- `/welcome` 的 v2 主场景不得用 `glass` 变体搭建卡片墙；液态玻璃只属于单一底部操作坞。
- 大屏与管理端默认使用弱边框、面板色和 `--radius-lg`，大屏指标卡可使用 `--radius-xl`；`/welcome` 的操作坞和档案项按 D-032 使用 `--shape-*` 微角令牌。
- 交互卡片必须使用真正可聚焦的按钮或链接，不能仅给 `div` 添加点击事件。
- 标题、正文、状态和操作区应有稳定顺序；装饰层设置 `pointer-events: none`。

### 5.3 `StatusPill`

状态胶囊必须始终显示文字；图标与颜色只作辅助。

| 状态 | 建议色调 | 说明 |
|---|---|---|
| 演示数据、信息 | `info` | 说明当前为占位或模拟状态 |
| 待开发、已下屏 | `neutral` | 非错误、当前不活动 |
| 暂停、重连中、已降级 | `warning` | 需要注意或等待处理 |
| 通过、在线、已完成 | `success` | 成功或正常可用 |
| 拒绝、离线、失败 | `danger` | 错误、停止或危险状态 |

静态说明不默认设置 `role="status"`。只有未来真实动态状态变化时，才按业务场景决定是否使用 live region，避免屏幕阅读器重复播报。

## 6. 三端差异

### 6.1 `/welcome` 手机端

- 以 320px 为最小支持宽度，主要检查宽度为 390px；单列优先。
- 页面必须可读、按钮可点、无横向溢出；约 390×844 下页面外壳不得纵向滚动，节目单内部滚动除外。
- 每个新视图只突出一个主要任务和一个顶部逐字标题；标题完成后保留可见光标，下方操作坞不得重复同名标题。减少动态、页面隐藏和恢复路径直接显示完整静态标题。
- NFC 个性入口为主，二维码/短网址承载同一随机令牌。退出确认会撤销当前会话并回到人工重新认证界面；该界面只收“学生姓名 + 恰好 8 位学号后缀”，当前合成 Demo 由客户端补固定前缀 `2026` 后调用既有人工协助激活接口。格式错误不发送请求，核验失败使用通用提示；成功必须恢复同一合成身份与档案，不能生成第二个参与者，也不能声称接入了真实学生名册。
- 入场视图以“找到属于你的星”为唯一顶部标题，并配合本人私有姓名、预分配星号和迎新晚会欢迎语；`displayName` 和预锁色阶段的 `personalStarCode` 只可出现在本人已认证页面。锁色前不得把该星号表现为公共恒星已建立；姓名和完整学号不得进入 `/screen`、`/admin`、公共事件、埋点或错误文案。
- 选色锁定后直接进入当前现场场景；晚到者不绕过核验与选色顺序。
- 主场景保留星系与一个底部操作坞；只有自己的恒星显示公开编号，其他恒星不显示标签。
- 星际档案按“档案者姓名 → 星星编号 → 星色 → 动力 → 星光”的信息顺序展示，不增加“已完成入场”等可由页面状态推断的冗余行，也不保留寄语/胶囊空位。动力解释为赠送虚拟礼物的可消费额度；星光解释为由服务端权威奖励累积、不可消费或兑换的个人成长记录。两项使用可点击、可聚焦的解释触发器和单一临时浮层，不使用 hover-only 交互。
- 下半部只有一个高透、非对称微角操作坞，背景星系仍可见；退出采用无普通边框的科技括角提示。高透、括角和微角不能牺牲文字对比、焦点或触控面积。
- 不依赖自动播放音频或环境动效完成流程。
- 使用 `100dvh` 时保留 `100vh` 回退，并考虑刘海屏安全区。
- 非核心动画在弱网或减少动态效果模式下直接关闭。
- 成功提示可自动收回，异常提示不可自动消失；退出始终确认，内存草稿按 D-025 的清理点清除。

### 6.2 `/screen` 舞台大屏端

- 第一版以 1920 × 1080、16:9 全屏作为视觉检查基线；这不是正式场馆规格。
- 背景可铺满视口，关键信息保持安全边距；不得出现浏览器控件、滚动条、裁切、黑边或文字溢出。
- 流动星系的协议技术上限为 300 颗真实恒星，正式视觉以 220 人为满场参考；使用服务端稳定位置和幂等 upsert，不生成假参与者星填充画面，也不因人数变化缩放或重排真实星。星系应读作盘面离散星主导的低倾角恒星盘：中央核球、短棒和两组宽而弱的密度波只提供层次，不得形成发光 S 形旋臂、粗线或霓虹轨道。每颗真实星采用近白亚像素核心与极薄保色光晕，沿用该参与者自己的服务端 `displayColor`；尺寸/亮度必须重尾，衍射芒只属于极少数最亮真实星，禁止硬边彩珠、同相呼吸和奶油光球。正常可见的集结中，已公开星从抵达首帧起以逆时针恒星物质流持续运行，并与较慢盘面密度波保持相对位移；集结稳态及其离场不显示当前星数、目标数、`x / 220`、`x / 300`、满员率或到场进度。常规星系不显示参与者编号/星号，也不允许点击单星；只有活动抽奖层可以显示中奖公开星号。
- 远距阅读优先：集结、协同、连接与降级状态使用更高字号和明确标签；D-047 继续要求节目稳态不显示场景/节目大标题、网页节目板或常驻面板，只有实时新弹幕以单行正文短时飘屏。全场只运行 `ASSEMBLY`、`PROGRAM_SUPPORT`、`COOPERATIVE_LIGHT` 三个场景。
- 不展示个人排行、完整身份、中奖者姓名、内部身份映射、规则拒绝或已下屏内容。
- 抽奖只能由后台在 `RUNNING + PROGRAM_SUPPORT` 开启，不改变全局场景或运行状态；`COMPLETED` 后不再新抽取。
- 实时效果异常时必须能隐藏动效层并切换静态背景、简化布局或预渲染素材。
- LIVE 结束画面只投影服务端已确认的终态；REHEARSAL 的 finale 只允许显示持续可见的“排练预览”标记，不写 live `COMPLETED`。动画完成不得反向触发业务结束。
- `ASSEMBLY` 背景由一张本地打包且不含星点/文字/天体的低饱和暗星云纹理、确定性程序尘埃和连续未解析盘面光构成；背景不得伪造参与人数。技术上最多 300 颗、正式最多 220 颗真实星采用重尾亮度/尺寸分布：绝大多数是亚像素微光，极少数才有克制光晕或衍射；星色从各自 `displayColor` 派生，禁止同尺寸彩珠、统一灰白和塑料渐变底。
- `ASSEMBLY` 从 0 人首帧起绘制 420 颗确定性、更小更暗的中性色装饰底星，并与盘面密度波差速流动，使连续恒星盘、核球与宽而弱的密度层在场馆远距可见。底星不能有公开星号、身份、事件或参与状态，也不进入任何人数；真实星继续逐颗保留 `displayColor`。环境盘面光与底星可按 0～220 真实星数量做最大 18% 的平滑曝光补偿，真实星尺寸只允许最大 8% 的克制可读性补偿，220 人及以上回到基准。正常模式只让未解析盘面光以多个不同周期、最大约 2.6% 的曝光变化和远小于 1% 的尺度变化形成呼吸；禁止真实星同相脉冲，reduced-motion、页面隐藏和静态恢复直接固定。实时首次出现一颗新真实星时，只播放一次从画外掠入、末段沿逆时针轨道切线被盘面捕获并收束到其稳定星位的流星；快照、刷新、重连、已有星更新、隐藏恢复与 reduced-motion 不补播。
- 正常可见的 `ASSEMBLY → PROGRAM_SUPPORT` 使用约 8.4 秒连续镜头：触发瞬间继承每颗真实星的位置与切向速度；约 0.0～1.4 秒让当前星海只出现轻微中心引力预兆，约 0.8～4.8 秒让真实星与装饰底星保持角动量、按不同曲率和到达时序螺旋汇聚，约 4.0～5.6 秒压成极小高温核心，约 5.2～7.1 秒只形成一次非对称超新星，约 6.5～8.4 秒让低饱和冲击折射、丝状喷流、电影曝光与整幅 Canvas 透明化连续重叠，从白场显露外部 OBS 的第一个节目。汇聚途中保留真实星各自星色，只有进入高温核心后才向星白收敛；稀疏到场只由无身份尘埃/等离子体维持爆发尺度，不得伪造参与者。WebGL2 不可用时回退 Canvas2D；页面隐藏、中断、刷新、重连和 reduced-motion 直接到透明静态终态。禁止同速直线吸附、规整圆环、烟花粒子球、彩虹冲击波、霓虹描边、现成镜头光斑、纯白矩形硬盖、分段停顿、末帧清空或重复频闪。预渲染视频仍是未启用备选。
- `PROGRAM_SUPPORT` 作为 OBS 完全透明浏览器源：网页不渲染节目占位黑底、网页节目板、背景、大标题、节目标题、边缘星点、礼物或常驻弹幕面板；白光退去后看到的节目画面来自透明 Canvas 下方的外部 OBS 源。仅实时收到新的合规 `barrage.published` 时，把匿名正文从右向左短时飘过；正文自身也不带底条、卡片或边框，只用文字阴影维持舞台可读性。节点数量有界、离场即销毁，不显示作者、头像、编号或来源，也不从快照、刷新、重连或高优先级覆层期间补播。减少动态模式不执行位移动画。礼物业务仍可在手机端提交但不在大屏可视化；网页不拥有媒体、不输出音频。
- 抽奖层独占舞台画面；隐藏期间到达的弹幕不补播动画。视觉优先级为终态、排练终章、抽奖、故障、场景、互动，越高层必须清理越低层的临时动画。normal-motion 可为新中奖结果提供一次克制揭晓；reduced-motion 与重连直接显示持久结果。

- D-057：正常新中奖结果按服务端持久顺序排队，每位约 1.8 秒滚动、揭晓后至少约 1.4 秒停留；未揭晓结果不进入历史列表。恢复、隐藏与 reduced-motion 取消临时队列并直接呈现持久结果。协同阶段显示完成数/准入数和静态进度条，集体光场及星群亮度由该聚合比例驱动；实时新增可有约 1.2 秒有界柔光反馈，密集事件合并为单个反馈而不叠加粒子。静态路径不保留反馈动画；协同不能使用 `started` 判断个人完成，装饰星仍不代表参与者。该进度只出现在协同场景，集结及节目阶段沿用原有隐藏人数/透明规则。

### 6.3 `/admin` 管理端

- 使用 Orbital Signal 午夜蓝静态画布、三层深空表面、低权重网格/信号线与共享字体/小圆角；信息密度可高于手机端，但不复制大屏电影镜头或手机个人星云。
- 不加入持续星流、粒子、强模糊、强发光或会竞争操作判断的氛围动画；环境层在 reduced-motion 下保持同一静态信息结构。
- 首屏优先呈现实时连接、运行 tuple、活动投影和主要控制；窄屏压缩品牌标题、开发导航和页脚占高，核心操作保持至少 44px 高。
- 已发布、规则拒绝、已下屏、来源屏蔽及连接/降级状态必须清晰可扫读。
- 危险操作、暂停、清屏、抽奖清空等控件必须有文字、确认和权限边界；抽奖开启/抽取/关闭与排练清空的状态差异必须可扫读。
- `NONE`、`RAFFLE`、`FINALE_PREVIEW` 等内部枚举必须映射成操作者可读中文，不得原样泄露到界面；连接、状态和危险语义不能只靠颜色。
- 窄屏时列表内容允许自然换行，中间内容设置 `min-width: 0`，不得因状态胶囊造成横向溢出。
- 管理员凭据、令牌和真实名单不得出现在页面、截图或演示数据中。

## 7. 动效系统

动效数值令牌定义在 `frontend/src/styles/tokens.css`，关键帧、动效工具类与降级规则定义在 `frontend/src/styles/motion.css`。只动画 `transform` 和 `opacity` 等低成本属性，避免循环改变布局、滤镜或大面积阴影。D-026 允许 V2-07 `/screen` 动态加载 GSAP core 编排少量 DOM 覆层；恒星仍由单 Canvas 绘制，禁止逐星 GSAP 和任何插件。

| 令牌 | 第一版值 | 用途 |
|---|---:|---|
| `--motion-duration-fast` | `140ms` | 按压、悬停、焦点反馈 |
| `--motion-duration-base` | `220ms` | 普通状态变化 |
| `--motion-duration-slow` | `420ms` | 卡片或区域进入 |
| `--motion-duration-ambient` | `24s` | 环境层低速漂移 |
| `--motion-ease-standard` | `cubic-bezier(0.2, 0, 0, 1)` | 常规过渡 |
| `--motion-ease-emphasized` | `cubic-bezier(0.2, 0.8, 0.2, 1)` | 单次强调，不用于无限循环 |

### 7.1 四层方案

1. **组件反馈层**：按钮、链接和焦点的 140–220ms 反馈。状态变化后立即结束，不阻塞输入。
2. **页面状态层**：卡片出现、区域切换和数据刷新，220–420ms。进入、暂停、完成、失败必须有文字状态。
3. **环境氛围层**：低透明度星点或光晕以 18–30 秒周期缓慢漂移。纯装饰、`aria-hidden="true"`、`pointer-events: none`，三端只挂载一份。
4. **舞台场景层**：星星集结、节目互动和协同点亮分别映射 `ASSEMBLY`、`PROGRAM_SUPPORT`、`COOPERATIVE_LIGHT`；`COMPLETED` 是终态而非第四个场景。抽奖是节目支持阶段的 presentation，动画不得决定中奖者、推进状态机或触发结束。

### 7.2 减少动态效果

D-066：手机和后台继续跟随 `prefers-reduced-motion: reduce`；专用 v2 大屏默认完整动效，可由主控通过 `motion=system` 跟随系统或 `motion=reduced` 强制静态。下列减少动态规则对大屏指最终选定的静态模式，覆盖历史大屏无条件跟随系统的条款。完整模式的共享 CSS 例外只在大屏挂载期间存在，离开该页面必须恢复。主控设置仅在 `settings=1` 时显示，不进入正常 OBS 成片。

选定静态模式后：

- 环境层停止循环动画和位移，但可保留静态、低透明度背景；
- 页面进入和组件过渡立即完成，不使用闪烁、缩放或视差；
- 状态变化继续通过文字、颜色和布局即时呈现；
- 不把无限动画简单压缩到 `0.01ms`，以免形成高频循环；应明确设置 `animation: none`；
- 所有核心流程在完全无动画时仍可使用。

大屏静态弹幕保留正文，最多同时 3 条，左侧留白内排布、允许换行，每条约 10～12 秒自动移除；禁止通过隐藏整层来降级。完整模式保留最多 24 条右向左飘屏和 CSS 回退，动画结束或兜底定时器都能清理节点。刷新/重连/隐藏及覆层期间不补播历史弹幕。静态模式切换不重新触发业务命令或中奖。

### 7.3 v2 手机端动效与反馈

- 正常首次流程采用 D-037 的受控个人旅程：新身份核验后、页面稳定可见才开始约 5.4 秒寻星，权威锁色并直接准入后参考约 1.0 秒闪烁并衔接约 4.2 秒拉远入轨，不出现寄语停留。
- 开播后真正进入后台或被中断时立即收束且返回不重播；返回、刷新、重连、失败、超时和减少动态效果路径不播放叙事时间线，直接渲染同一权威静态终态。
- 本机装饰星云、颗粒和轨道尘只负责氛围，不代表参与者数量；真实恒星事实只来自服务端。动画实现应优先使用合成友好的变换、透明度与单 Canvas 绘制，不得靠持续动画模糊、滤镜或大阴影制造液态玻璃。
- 普通成功提示的 3.5 秒收回是展示行为，不删除服务端事实；异常提示持续显示，直到权威状态恢复。

### 7.4 D-030 个人恒星连续镜头

- **视觉基线**：`motion-previsual-personal-star/personal-star-journey-animatic.webm`、同目录预演和十张关键帧保留沉入、接近、捕获、闪烁与拉远入轨的镜头语言；其中寄语停留已被 D-037 删除，不能照搬旧时间线。
- **持久主体**：从寻星首帧到选色、锁色确认及入轨只追踪同一颗本人恒星；表现载体可以跨 DOM/Canvas 层合成，但必须共享坐标、尺寸、颜色和运动轨迹，任何交接帧都不能闪断或跳位。
- **连续镜头**：速度、景深、星云层与轨道层在阶段边界互相覆盖，镜头由向前接近自然转为向后拉远；不得用互不相关的淡出/淡入、突然缩放或选中圆环替代真实连续运动。
- **无障碍交接**：后续文案与控件可预渲染，交接前 `inert`、不可聚焦和不可操作，权威状态与安全表现门都满足后立即可用。实时连接、快照和服务端允许动作不等待动画。
- **静态同构**：隐藏、中断、刷新、恢复、失败与 reduced-motion 都直接使用相应权威阶段的同构静态结构，不闪播、不补播，也不依赖视觉层取得业务结果。

## 8. A6 横版邀请函第一版

### 8.1 文件与尺寸状态

- 暂按 A6 横版比例 `148mm × 105mm` 制作正反面 SVG 和 PNG 预览。
- SVG 建议使用等比例画布，例如 `viewBox="0 0 1480 1050"`；PNG 预览保持同一比例。
- 本轮文件是视觉方向样稿，不是可直接下单的印刷生产文件。
- 正式尺寸、出血、裁切安全区、圆角、纸张、覆膜、印刷色彩和 NFC 标签规格必须在打样前确认。

### 8.2 正面信息层级

1. 活动/学院文字标识；未授权前只用文字，不描摹官方标志。
2. “星际信标 / 星际成长”主视觉和简短邀请语。
3. 明确标注为示例的虚构姓名，例如“示例姓名 · 林星遥”。
4. 清晰的 NFC 轻触区及短指引，例如“手机解锁后轻触此处”。
5. 不显示学号、班级、邀请码、入口令牌或可反推个人身份的编号。

### 8.3 背面入口层级

按兼容优先级展示三种进入方式：

1. **轻触 NFC**：主入口；
2. **扫描二维码**：NFC 不支持、未开启或识别失败时使用；
3. **输入短网址**：无法轻触或扫码时手动进入。

三者必须指向同一个占位 `/welcome`。第一版使用保留的无效域名 `https://welcome.example.invalid/welcome`，并在图面标注“示例入口 / 不可用于正式活动”。二维码不能被描述为网站宕机或现场断网的备用方案。

### 8.4 NFC 与二维码制作规则

- NFC 视觉触点避开折痕、裁切线、金属油墨、烫金烫银和磁吸结构。
- 视觉标记不等于最终标签位置；实际位置需结合标签、纸张、覆膜和装配小样测试。
- 本轮预览使用明确标注“二维码占位｜不可扫描”的非标准方块纹理，且不绘制标准定位图形，避免被误当作正式入口；正式二维码另行生成后须保持正方形、高对比、无拉伸并保留完整静区。
- 正式二维码、短网址和 NFC NDEF URI 必须来自同一受控 HTTPS 入口，并按“写入—读取—锁定—成品复测”验收。
- 普通 NFC 标签只是访问入口，不得在邀请函上宣称其本身完成身份认证或防复制。

### 8.5 占位数据与素材授权

- 姓名必须虚构并明确标注“示例”；不使用任何真实姓名、学号、手机号或邀请码。
- URL 使用保留示例域名，不使用真实活动令牌、私有链接或临时开发地址。
- 第一版背景、轨道、星点和 NFC 图标使用原创 SVG 基础图形或 CSS 绘制。
- 不复制去年项目的字体、图片、代码、装饰纹样或其他授权不明资产。
- 后续若使用官方标志、商业字体、摄影或插画，必须先取得许可并登记来源。
- D-031 已登记的 `/welcome` 白色透明学院 PNG 是当前唯一已获项目负责人明确授权的学院图形资产；前端文件为 `frontend/src/assets/brand/sysu-intelligent-engineering-white.png`，不得从 `C:\Windows\TEMP` 或学院网站运行时加载。

## 9. 可访问性基线

- 普通正文与背景对比度目标不低于 4.5:1；大号文字及关键图形目标不低于 3:1。
- 所有交互元素具有可见 `:focus-visible` 样式，键盘顺序与视觉顺序一致。
- 手机端触控目标不小于 44 × 44px，相邻危险与普通操作保持足够间距。
- 状态、错误、成功和连接情况必须有文字，不得仅以颜色、闪烁或位置表达。
- 装饰组件使用 `aria-hidden="true"`，不能拦截鼠标、触摸或键盘操作。
- 使用原生按钮、链接和标题层级；禁止用不可聚焦的普通容器模拟控件。
- 禁用状态必须使用原生 `disabled`；动态忙碌状态需提供可理解的文本。
- 逐字标题必须向辅助技术暴露一次完整标题，不得逐字触发 live region；减少动态和恢复路径直接呈现完整标题，静态光标不循环闪烁。
- 动力与星光解释触发器使用原生按钮并提供 `aria-expanded`、`aria-controls` 和可关联说明；浮层打开时使用单一 `role="tooltip"`，按 `Escape`、切换到另一项或移出相关焦点范围均可关闭，焦点留在触发按钮上。
- 320px 宽度不得横向溢出；200% 缩放后主要内容和操作仍可访问。
- 不使用高频闪烁、快速视差、自动播放音频或必须依赖动画才能理解的提示。

## 10. 视觉验证矩阵

第一版完成后至少执行以下检查。所有截图和记录只使用模拟数据。

| 对象 | 环境 / 尺寸 | 必查项 |
|---|---|---|
| 构建 | 仓库根目录执行 `pnpm --dir frontend build` | 构建成功；无新增大型依赖；不提交 `dist/` |
| `/welcome` | 320 × 568、390 × 844、短视口/软键盘 | 无横向或整页纵向溢出；每个视图只有一个顶部逐字标题且下方不重复；本机字体缺失时回退协调；自己的恒星与单一高透微角操作坞清楚；退出括角、主要操作和 44px 触控目标不被遮挡；透明效果失败时不透明降级完整 |
| `/welcome` 本人身份与档案 | 新鲜邀请、退出后人工重新认证、键盘与屏幕阅读器；自动截图只用合成账号 | 本人页显示当前目录姓名与预分配星号；锁色前不产生公共恒星；姓名与完整学号不出现在 `/screen`、`/admin` 公共区域、响应或事件；姓名加 8 位学号恢复同一身份，7 位/9 位不发请求且提示 8 位要求；档案字段顺序正确且无冗余入场状态；动力/星光可点击、可聚焦、可用 `Escape` 关闭并获得不同解释 |
| `/welcome` 动效 | normal-motion 新鲜邀请、D-030 视觉基线、开始前隐藏→可见、开播后后台中断、返回、刷新、重连、失败、超时、减少动态效果 | 正常首次在稳定 `visible` 后参考约 5.4 秒寻星，权威锁色并直接准入后约 1.0 秒闪烁衔接约 4.2 秒连续拉远入轨；中间无寄语/胶囊停留且无动画跳过按钮；同一恒星无闪断、跳位或替身圆环，其余路径收敛到对应权威静态终态 |
| `/screen` | 1920 × 1080 全屏，0/40/80/120/160/220 颗正式真实星；300 颗仅压力端点 | 无滚动条、裁切、黑边和文字溢出；真实恒星数量与数据精确一致，420 颗装饰底星及连续未解析盘面光明确不计人数且无业务语义。0 人首帧即有可见但克制的逆时针流动；盘面离散星主导的低倾角恒星盘连续可辨，核球、短棒和宽弱密度波只作层次，不得读作 S 形旋臂或霓虹轨道。人数补偿在各档连续递减，220 人回到基准；环境层有低振幅非同步呼吸但真实星不同时脉冲，reduced-motion 帧保持静止。真实星以摄影式点扩散保留各自服务端星色，无硬边彩珠或奶油光球。每颗实时新星只触发一次画外流星，末段沿轨道切线进入稳定星位；快照/刷新/重连/隐藏/reduced-motion 不补播，画面不显示人数或目标。等待态约 30Hz；到场流星及约 8.4 秒集结→节目高速段以垂直同步 60Hz 为目标。切场首帧延续当前星流；真实星与底星按不同曲率和时序螺旋汇聚，深度压缩前保留真实星色，随后形成高温小核心与一次非对称超新星，再由冲击折射、丝状喷流和电影曝光连续过渡到整幅 Canvas 透明。WebGL2 与最长边 1280px 的有界缓冲在首帧前预热，静态星云预先缓存；不得有首用编译顿挫、同速直线吸附、规整圆环、烟花粒子球、霓虹冲击波、纯白矩形硬盖、阶段停顿或末帧清空。只保留一个可见 Canvas，WebGL2 失败时有 Canvas2D 回退；节目稳态无网页节目板、背景、标题、星点、礼物或常驻弹幕面板，只有实时新弹幕短时飘屏；抽奖层不改全局场景且只显示中奖公开星号；刷新/重连/reduced-motion 直接得到透明静态终态 |
| `/admin` | 1366 × 768、390 × 844 | 状态可扫读；正式模式显示现场登录文案并移除 Demo 重置卡；抽奖开启/抽取/关闭权限明确；中奖者目录姓名仅在受控后台显示，大屏仍只有公开星号；危险/禁用样式明确；窄屏列表可换行 |
| 三端共享组件 | 常规、悬停、键盘焦点、禁用 | `BaseButton`、`BaseCard`、`StatusPill` 在三端语义一致 |
| 减少动态效果 | 浏览器启用 `prefers-reduced-motion: reduce` | 环境循环停止；无高频闪动；状态仍立即可读 |
| 键盘与缩放 | 仅键盘、200% 缩放 | 焦点可见；顺序合理；主要操作和文本不丢失 |
| 邀请函正反面 | SVG 原尺寸、PNG 100% 与缩略图 | 文字无裁切；正反面方向一致；占位标识清楚；无真实信息 |
| 二维码预览 | SVG 与 PNG 100% | 占位图明确写“不可扫描”，不具备标准二维码定位图形；正式扫码测试留待入口确认后执行 |
| 素材检查 | 仓库新增资源清单 | 只含原创、明确许可或占位素材；无去年项目复制物 |

D-027/D-028 的自动数字与 D-030～D-032/D-036 的通过均只保留各自历史范围。D-037 删除了寄语停留并新增抽奖层，必须按上表重新取得自动证据；至少一台代表实际上线访问方式的实体智能手机上的新鲜邀请视觉、三端局域网抽奖与 OBS 合成也须由负责人单独签核。

D-032 于 2026-08-15 取得的聚焦 Vitest、构建、typecheck、三项目 E2E 和个人旅程数据是 D-037 之前的可追溯基线；其中寄语步骤不再是现行验收目标。当前收口必须记录新的命令结果，且任何桌面证据都不代签 D-052 实体手机的字体回退、触控、软键盘和主观视觉观感。

1920 × 1080 是 Demo v0 的大屏验证尺寸。正式场馆分辨率、真实硬件和全屏环境按 [TEST_PLAN.md](./TEST_PLAN.md) 的正式版延期验收另行复核。

## 11. 尚未确认的设计决定

以下内容必须保留“待确认”状态，并在负责人确认后更新 [DECISIONS.md](./DECISIONS.md)：

1. 除 D-031 已授权的 Demo `/welcome` 白色透明 PNG 与学院官网入口外，正式品牌色、学院矢量母版、其他页面与线下物料的视觉资产授权。
2. 正式版是否另行捆绑开源字体；D-032 当前手机实现只使用已确认的本机/系统回退栈，任何未来捆绑仍须确认具体字体、许可证和中文回退方案。
3. 正式活动名称、日期、邀请语、姓名展示格式和公开文案。
4. 正式域名、短网址、二维码最终内容及实体卡片生产方式；“一人一卡一码”的随机令牌方向已由 D-005 确认。
5. 邀请函最终尺寸、出血、安全区、圆角、纸张、覆膜、印刷色、NFC 标签型号与位置。
6. 场馆大屏实际分辨率、比例、导播链路和安全显示区域。
7. 主持口令、音乐、灯光及 `PROGRAM_SUPPORT → COOPERATIVE_LIGHT` 等尚未确认的最终镜头细节；D-046～D-055 已确认盘面主导银河、420 颗零人底星、人数有界环境补偿、低振幅非同步呼吸、逐人单次流星轨道捕获、隐藏人数进度、30Hz 等待态、60Hz 目标高速段、预热有界 WebGL2、约 8.4 秒错峰螺旋汇聚/高温核心/一次非对称超新星/白场透明接管、跨端 Orbital Signal 色谱、手机背景同源重置、Canvas2D 回退，以及节目稳态无网页节目板/常驻层、仅实时弹幕飘屏；视频路线仍未启用，场景集合与终态语义仍由 D-025 冻结。
8. 动力值、星光值的最终图标、色值和公开展示规则。
9. 正式无障碍验收范围及是否需要额外高对比模式。

在这些事项确认前，代码和预览应使用语义令牌、占位文案与可替换接口，避免把临时选择固化为业务或印刷事实。
